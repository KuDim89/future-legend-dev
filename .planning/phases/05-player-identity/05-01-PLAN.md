---
phase: 05-player-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/player.ts
  - content/gallery.ts
  - dictionaries/en.json
  - dictionaries/ua.json
  - app/[lang]/layout.tsx
autonomous: true
requirements:
  - IDENT-01
  - IDENT-02

must_haves:
  truths:
    - "Every section displaying player name shows 'Artem Kukharuk', not 'Dmytro Kovalenko'"
    - "Hero section position label reads 'Defender' (EN) / 'Захисник' (UA)"
    - "Club section shows 'Viva Cup' with a coherent Viva Cup description — not Dynamo Kyiv"
    - "About section bio reads as a scout-focused defender profile for a 9-year-old"
    - "Trophies section shows exactly one trophy: Starballs CUP 2026"
    - "Gallery alt text contains no 'Dmytro' or 'Dynamo Kyiv U21' references"
    - "Contact form error message contains no hardcoded email address"
    - "Page metadata title and description reference Artem Kukharuk, Defender, Viva Cup"
  artifacts:
    - path: "content/player.ts"
      provides: "Typed player data — name, DOB, defender stats, single trophy"
      contains: "fullName: 'Artem Kukharuk'"
    - path: "dictionaries/en.json"
      provides: "EN locale strings — position, bio, club, trophies, contact fallback"
      contains: "\"Defender\""
    - path: "dictionaries/ua.json"
      provides: "UA locale strings — same keys in Ukrainian"
      contains: "\"Захисник\""
    - path: "content/gallery.ts"
      provides: "Gallery entries with updated alt text — no Dmytro or Dynamo refs"
    - path: "app/[lang]/layout.tsx"
      provides: "Page metadata referencing Artem Kukharuk, Defender, Viva Cup"
  key_links:
    - from: "content/player.ts"
      to: "AboutSection"
      via: "player prop passed from page.tsx"
      pattern: "fullName.*Artem Kukharuk"
    - from: "dictionaries/en.json"
      to: "HeroSection"
      via: "dict.hero.position"
      pattern: "\"position\".*\"Defender\""
    - from: "dictionaries/en.json"
      to: "TrophiesSection"
      via: "dict.trophies.items"
      pattern: "trophies.*items.*Starballs"
---

<objective>
Replace all Dmytro Kovalenko placeholder content with Artem Kukharuk's real identity across every text-bearing file: player data, both locale dictionaries, gallery alt text, and page metadata.

Purpose: The site currently presents a fictional midfielder profile. This plan makes every visible text element represent the real player — name, position, club, age, and bio in both languages.
Output: Five files updated; no new files created; no structural changes.
</objective>

<execution_context>
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-player-identity/05-CONTEXT.md
@.planning/phases/05-player-identity/05-RESEARCH.md

<interfaces>
From content/player.ts (current — executor reads the file, these are the key fields to update):

Player interface stays unchanged — only data values change.

Fields to update:
  fullName: string          → "Artem Kukharuk"           (was "Dmytro Kovalenko")
  dateOfBirth: string       → "2017-01-23"                (was "2006-03-14")
  stats.pace: number        → 72  (was 78 — higher for defender is okay, 60–78 range)
  stats.dribbling: number   → 62  (was 82 — lower for defender)
  stats.shooting: number    → 61  (was 71 — lower for defender)
  stats.passing: number     → 65  (was 88 — moderate)
  stats.physical: number    → 68  (was 74 — moderate)
  stats.defending: number   → 76  (was 69 — highest for defender)
  trophies: Trophy[]        → single entry: [{ year: 2026 }]   (was 3 entries)

Fields that stay unchanged: workingFoot, nationality, club.logo, team.logo

From dictionaries/en.json — keys to update (all others unchanged):
  hero.position             → "Defender"
  hero.ctaAriaLabel         → "Contact Artem Kukharuk"
  about.bio                 → Claude writes 3-sentence scout-focused bio for 9-year-old defender
  trophies.items            → single-item array (see action for exact structure)
  club.name                 → "Viva Cup"
  club.description          → Claude writes Viva Cup youth football description
  team.name                 → "National Team — TBD"
  team.description          → Claude writes neutral placeholder (no Dmytro/national team refs)
  contact.errorSubmit       → generic fallback, no email address
  highlights.comingSoon     → "Highlights coming soon"  (NEW KEY — does not exist yet)

From dictionaries/ua.json — same key paths, Ukrainian values:
  hero.position             → "Захисник"
  hero.ctaAriaLabel         → "Зв'язатися з Артемом Кухаруком"
  about.bio                 → Ukrainian version of the scout-focused bio
  trophies.items            → [{ "name": "Учасник Starballs CUP", "competition": "Starballs CUP — категорія 2016 р.н." }]
  club.name                 → "Viva Cup"
  club.description          → Ukrainian version of Viva Cup description
  team.name                 → Ukrainian neutral placeholder
  team.description          → Ukrainian neutral placeholder
  contact.errorSubmit       → generic Ukrainian fallback, no email
  highlights.comingSoon     → "Відео з'являться незабаром"  (NEW KEY)

From content/gallery.ts — alt text fields to update:
  photo-01.webp alt  → "Match action — Artem driving forward"
  photo-02.webp alt  → "Match intensity — Artem competing for the ball"
  photo-03.webp alt  → no change (no Dmytro reference)
  photo-04.webp alt  → "Training focus — Artem working on defending patterns"
  photo-05.webp alt  → "Official photo — Artem in Viva Cup kit"
  photo-06.webp alt  → "Official portrait — Artem, Viva Cup"

From app/[lang]/layout.tsx (lines 13–16):
  metadata.title        → "Artem Kukharuk — Football Player"
  metadata.description  → "Scout profile for Artem Kukharuk, Defender. View stats, trophies, and club info."
  Structure stays: export const metadata: Metadata = { ... } — no generateMetadata, no locale awareness
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update player.ts and gallery.ts data values</name>
  <files>content/player.ts, content/gallery.ts</files>
  <read_first>
    - content/player.ts — read the full file to see current values before editing; all 6 stat fields must be replaced (none carry over from the midfielder profile)
    - content/gallery.ts — read all 6 entries; 5 of the 6 alt strings need updating
  </read_first>
  <action>
    In content/player.ts, update the player data object — do NOT change any interface or type definitions:
    - fullName: set to "Artem Kukharuk" (per D-01)
    - dateOfBirth: set to "2017-01-23" (per D-02)
    - stats.pace: set to 72
    - stats.dribbling: set to 62
    - stats.shooting: set to 61
    - stats.passing: set to 65
    - stats.physical: set to 68
    - stats.defending: set to 76
    - trophies: replace the existing 3-item array with a single entry: [{ year: 2026 }] (per D-06)
    - workingFoot, nationality, club.logo, team.logo: leave exactly as-is

    In content/gallery.ts, update the alt text for 5 of the 6 entries — src paths are unchanged:
    - photo-01.webp: alt set to "Match action — Artem driving forward"
    - photo-02.webp: alt set to "Match intensity — Artem competing for the ball"
    - photo-03.webp: alt left unchanged ("Training session — technical drills on the pitch")
    - photo-04.webp: alt set to "Training focus — Artem working on defending patterns"
    - photo-05.webp: alt set to "Official photo — Artem in Viva Cup kit"
    - photo-06.webp: alt set to "Official portrait — Artem, Viva Cup"
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - content/player.ts line with fullName contains "Artem Kukharuk"
    - content/player.ts line with dateOfBirth contains "2017-01-23"
    - content/player.ts defending value is 76 (highest stat in the object)
    - content/player.ts trophies array has exactly one entry with year: 2026
    - content/player.ts dribbling value is 62 (lower than defending — confirms defender profile)
    - content/gallery.ts contains no occurrences of the string "Dmytro"
    - content/gallery.ts contains no occurrences of the string "Dynamo Kyiv"
    - content/gallery.ts contains no occurrences of the string "Ukraine U18 National Team"
    - TypeScript compilation exits with no errors
  </acceptance_criteria>
  <done>player.ts holds Artem Kukharuk's identity with a defender stat profile and single 2026 trophy. Gallery alt text contains no Dmytro or Dynamo Kyiv references. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update both locale dictionaries</name>
  <files>dictionaries/en.json, dictionaries/ua.json</files>
  <read_first>
    - dictionaries/en.json — read the full file to see every key before editing; note existing structure of trophies.items array (currently 3 items, must become 1) and contact.errorSubmit (currently ends with dimakyh@ukr.net)
    - dictionaries/ua.json — read the full file; same structural points apply
  </read_first>
  <action>
    Update dictionaries/en.json — change only the values listed below, leave all other keys exactly as-is:

    hero.position: change from "Central Midfielder" to "Defender" (per D-12)
    hero.ctaAriaLabel: change from "Contact Dmytro Kovalenko" to "Contact Artem Kukharuk" (per D-13)
    about.bio: replace the 3-sentence Dmytro/Dynamo bio with a new 3-sentence bio for Artem Kukharuk, age 9, playing as a Defender at Viva Cup. Write for an audience of scouts and coaches — tone professional. Cover: current defensive qualities for a 9-year-old (positional awareness, reading the game, competitive intensity), club context (Viva Cup youth programme), future potential as a developing defender. Do not mention any specific match or statistic. Example framing: "At nine years old, Artem Kukharuk..." (per D-14, D-21)
    trophies.items: replace the entire 3-item array with a single-item array: [{ "name": "Starballs CUP Participant", "competition": "Starballs CUP — 2016 Birth Year Category" }] (per D-15)
    club.name: change from "Dynamo Kyiv U21" to "Viva Cup" (per D-16)
    club.description: replace the Dynamo Kyiv description with a 2–3 sentence description of Viva Cup as a youth football competition/programme for Artem's age group. Factual tone — describe it as a competitive environment for young players developing their skills. (per D-16)
    team.name: change from "Ukraine U18 National Team" to "National Team — TBD" (per D-17)
    team.description: replace the Dmytro/УЄФА description with a neutral 1-sentence placeholder, such as: "National team selection not yet confirmed — this section will be updated when Artem represents his country." (per D-17)
    contact.errorSubmit: change from "Something went wrong. Please try again or email us directly at dimakyh@ukr.net" to "Something went wrong. Please try again or contact us via the form." (per D-18 — no email address visible)
    highlights.comingSoon: add this NEW key under the "highlights" object with value "Highlights coming soon" — the key does not exist yet, add it alongside the existing title and intro keys

    Update dictionaries/ua.json with matching Ukrainian values for the same key paths:
    hero.position: "Захисник" (per D-12)
    hero.ctaAriaLabel: "Зв'язатися з Артемом Кухаруком" (per D-13)
    about.bio: Ukrainian translation of the new Artem bio (same content, audience, structure as EN) (per D-21)
    trophies.items: [{ "name": "Учасник Starballs CUP", "competition": "Starballs CUP — категорія 2016 р.н." }] — single item array (per D-15)
    club.name: "Viva Cup" (same in Ukrainian — proper noun)
    club.description: Ukrainian version of the Viva Cup description
    team.name: Ukrainian neutral placeholder, e.g. "Збірна — уточнюється"
    team.description: Ukrainian version of the neutral national team placeholder
    contact.errorSubmit: Ukrainian generic fallback with no email address, e.g. "Щось пішло не так. Спробуйте ще раз або зв'яжіться через форму."
    highlights.comingSoon: "Відео з'являться незабаром" (NEW KEY — must be added under "highlights" in ua.json)

    CRITICAL: Both trophies.items arrays must have exactly 1 item — not 3. The TrophiesSection renders by index-based zip across both locales. A count mismatch causes runtime errors in UA locale.
  </action>
  <verify>
    <automated>node -e "const en=require('./dictionaries/en.json'); const ua=require('./dictionaries/ua.json'); console.log('EN bio includes Artem:', en.about.bio.includes('Artem')); console.log('EN trophies count:', en.trophies.items.length); console.log('UA trophies count:', ua.trophies.items.length); console.log('EN position:', en.hero.position); console.log('UA position:', ua.hero.position); console.log('EN comingSoon:', en.highlights.comingSoon); console.log('UA comingSoon:', ua.highlights.comingSoon); console.log('EN errorSubmit has email:', en.contact.errorSubmit.includes('@')); console.log('UA errorSubmit has email:', ua.contact.errorSubmit.includes('@'));"</automated>
  </verify>
  <acceptance_criteria>
    - en.json hero.position equals "Defender"
    - ua.json hero.position equals "Захисник"
    - en.json hero.ctaAriaLabel equals "Contact Artem Kukharuk"
    - ua.json hero.ctaAriaLabel equals "Зв'язатися з Артемом Кухаруком"
    - en.json about.bio contains "Artem" and does not contain "Dmytro"
    - ua.json about.bio contains "Артем" and does not contain "Дмитро"
    - en.json trophies.items is an array of length 1
    - ua.json trophies.items is an array of length 1
    - en.json trophies.items[0].competition contains "Starballs CUP"
    - ua.json trophies.items[0].competition contains "Starballs CUP"
    - en.json club.name equals "Viva Cup"
    - ua.json club.name equals "Viva Cup"
    - en.json team.name does not contain "Ukraine U18 National Team"
    - ua.json team.name does not contain "Збірна України U18"
    - en.json contact.errorSubmit does not contain "@" (no email address)
    - ua.json contact.errorSubmit does not contain "@" (no email address)
    - en.json highlights.comingSoon exists and equals "Highlights coming soon"
    - ua.json highlights.comingSoon exists (non-empty string)
    - Both JSON files are valid JSON (node -e "require('./dictionaries/en.json')" exits 0)
  </acceptance_criteria>
  <done>Both locale dictionaries fully updated — Artem Kukharuk identity, defender position, Viva Cup, single trophy entry, no email in error fallback, and highlights.comingSoon key present in both locales. JSON is valid and all key counts match between locales for index-zipped arrays.</done>
</task>

<task type="auto">
  <name>Task 3: Update page metadata in layout.tsx</name>
  <files>app/[lang]/layout.tsx</files>
  <read_first>
    - app/[lang]/layout.tsx — read the full file; the metadata export is at lines 13–16. Confirm the exact current string values before replacing. Do NOT change the export structure — only update the string values inside the metadata object.
  </read_first>
  <action>
    In app/[lang]/layout.tsx, update only the two string values inside the existing static metadata export:

    metadata.title: change from "Dmytro Kovalenko — Football Player" to "Artem Kukharuk — Football Player" (per D-20)
    metadata.description: change from the current Dmytro/Central Midfielder string to "Scout profile for Artem Kukharuk, Defender. View stats, trophies, and club info." (per D-20)

    Do NOT: introduce generateMetadata, make metadata dynamic, add locale-awareness, or alter any import, export, or structural element. The static "export const metadata: Metadata = {...}" pattern is intentional — both locales share the same English metadata title. Update values only.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - app/[lang]/layout.tsx contains the string "Artem Kukharuk — Football Player" in the title field
    - app/[lang]/layout.tsx contains the string "Artem Kukharuk, Defender" in the description field
    - app/[lang]/layout.tsx does not contain "Dmytro Kovalenko" anywhere
    - app/[lang]/layout.tsx does not contain "Central Midfielder" in the metadata description
    - The file still exports `export const metadata: Metadata` (static export unchanged — no generateMetadata)
    - TypeScript compilation exits with no errors
  </acceptance_criteria>
  <done>Page metadata title and description reference Artem Kukharuk as Defender at Viva Cup. Static metadata export structure is unchanged. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content files → Build | JSON and TS content files are compiled at build time — no runtime trust boundary in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | dictionaries/en.json contact.errorSubmit | mitigate | Remove dimakyh@ukr.net from error message per D-18 — this is the primary security action in this plan |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase — audit not applicable |
</threat_model>

<verification>
After all three tasks complete:

1. Run `npx tsc --noEmit` — must exit 0 with no errors
2. Run `npm run build` — static export must complete without error; both `/ua/` and `/en/` locales must pre-render
3. Spot-check built output: `out/en/index.html` must contain "Artem Kukharuk" and "Defender" — not "Dmytro Kovalenko" or "Central Midfielder"
4. Spot-check: `out/ua/index.html` must contain "Захисник" — not "Центральний півзахисник"
</verification>

<success_criteria>
- content/player.ts: fullName is "Artem Kukharuk", dateOfBirth is "2017-01-23", defending (76) is the highest stat, trophies array has exactly 1 entry with year 2026
- dictionaries/en.json: position is "Defender", ctaAriaLabel is "Contact Artem Kukharuk", bio references Artem (not Dmytro), trophies.items has 1 entry (Starballs CUP), club.name is "Viva Cup", errorSubmit has no "@", highlights.comingSoon key exists
- dictionaries/ua.json: same structure with Ukrainian values; trophies.items also has 1 entry; highlights.comingSoon key exists
- content/gallery.ts: zero occurrences of "Dmytro", "Dynamo Kyiv U21", "Ukraine U18 National Team"
- app/[lang]/layout.tsx: title is "Artem Kukharuk — Football Player", no "Dmytro Kovalenko" anywhere in the file
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/05-player-identity/05-01-SUMMARY.md` when done
</output>
