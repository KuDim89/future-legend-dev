---
phase: 05-player-identity
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - content/videos.ts
  - components/ui/VideoCard.tsx
  - components/ui/VideoCard.module.scss
  - components/sections/HighlightsSection.tsx
autonomous: true
requirements:
  - VIDEO-01

must_haves:
  truths:
    - "Highlights section renders exactly 3 cards — none are clickable YouTube tiles"
    - "Each placeholder card shows a greyed/muted visual with a clock icon"
    - "Each placeholder card title displays the 'coming soon' label from the locale dictionary"
    - "No network request is made to img.youtube.com when viewing placeholder cards"
    - "Real YouTube video cards (isPlaceholder false or absent) continue to work exactly as before"
    - "The grid layout is unchanged — 3 cards in the same positions as before"
  artifacts:
    - path: "content/videos.ts"
      provides: "VideoEntry interface with isPlaceholder? field; all 3 entries flagged"
      contains: "isPlaceholder?: boolean"
    - path: "components/ui/VideoCard.tsx"
      provides: "Disabled placeholder variant that renders when isPlaceholder is true"
      exports: ["VideoCard"]
    - path: "components/ui/VideoCard.module.scss"
      provides: "Muted placeholder styles (.placeholderSlot, .placeholderIcon)"
      contains: ".placeholderSlot"
    - path: "components/sections/HighlightsSection.tsx"
      provides: "Conditional comingSoonLabel prop passed to VideoCard when isPlaceholder"
  key_links:
    - from: "content/videos.ts"
      to: "components/ui/VideoCard.tsx"
      via: "VideoEntry.isPlaceholder checked in VideoCard render"
      pattern: "video\\.isPlaceholder"
    - from: "dictionaries/en.json"
      to: "components/sections/HighlightsSection.tsx"
      via: "dict.highlights.comingSoon (added in 05-01)"
      pattern: "dict\\.comingSoon"
    - from: "components/sections/HighlightsSection.tsx"
      to: "components/ui/VideoCard.tsx"
      via: "comingSoonLabel prop passed conditionally"
      pattern: "comingSoonLabel"
---

<objective>
Add video placeholder behavior: extend VideoEntry with an isPlaceholder flag, implement a disabled card variant in VideoCard, and wire HighlightsSection to render placeholder cards for all 3 current video entries.

Purpose: Real YouTube highlight IDs are not yet available for Artem. Rather than hiding the section (losing grid structure) or showing broken YouTube tiles, 3 muted "coming soon" cards preserve the layout and communicate clearly to visitors. When real IDs arrive, swapping isPlaceholder: false is the only change needed.
Output: VideoCard gains a placeholder branch; all 3 video entries are flagged; section renders correctly in both locales.
</objective>

<execution_context>
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-player-identity/05-CONTEXT.md
@.planning/phases/05-player-identity/05-RESEARCH.md
@.planning/phases/05-player-identity/05-01-SUMMARY.md

<interfaces>
From content/videos.ts — current state (executor reads file before editing):

Current VideoEntry interface (no isPlaceholder field yet):
  videoId: string
  title: string
  category: 'match' | 'training' | 'skills'

After this plan, interface gains:
  isPlaceholder?: boolean   // optional — existing code paths treat undefined as falsy

All 3 entries must have isPlaceholder: true added.
Update stale title values to neutral text (they appear as aria-label in the non-placeholder path — still in the interface, just cleaned up):
  entry 1: title → "Match Highlights — Coming Soon"
  entry 2: title → "Training Session — Coming Soon"
  entry 3: title → "Skills Compilation — Coming Soon"

From components/ui/VideoCard.tsx — current state:
  Props: { video: VideoEntry }
  Render: thumbnailSlot (16/9 aspect) → motion.button (thumbnail + play overlay) OR motion.div (YouTube iframe), controlled by isPlaying state
  cardTitle paragraph below the slot renders video.title

After this plan:
  Props: { video: VideoEntry; comingSoonLabel?: string }
  Guard at top of render body: if (video.isPlaceholder && comingSoonLabel) → return placeholder JSX
  Placeholder JSX structure:
    <article className={styles.card}>
      <div className={styles.thumbnailSlot}>
        <div className={styles.placeholderSlot} aria-hidden="true">
          <Clock size={32} className={styles.placeholderIcon} />
        </div>
      </div>
      <p className={styles.cardTitle}>{comingSoonLabel}</p>
    </article>
  Import Clock from 'lucide-react' (already a project dependency — Play is imported there now)
  Existing play/iframe render path is UNCHANGED below the guard

From components/ui/VideoCard.module.scss — existing classes used by VideoCard:
  .card, .thumbnailSlot, .thumbnailBtn, .thumbnail, .playOverlay, .playCircle, .playIcon, .playerWrapper, .youtubePlayer, .youtubeIframe, .cardTitle

New classes to add:
  .placeholderSlot — fills the thumbnailSlot area, muted visual
  .placeholderIcon — icon color styling

From components/sections/HighlightsSection.tsx — current state:
  Props: { videos: VideoEntry[]; dict: Dictionary['highlights'] }
  Render: <VideoCard video={video} /> (no additional props)

After this plan (single line change inside the map):
  <VideoCard video={video} comingSoonLabel={video.isPlaceholder ? dict.comingSoon : undefined} />
  No props interface change on HighlightsSection itself — dict.highlights already has comingSoon after 05-01 added the key

Dictionary type note: Dictionary['highlights'].comingSoon is now typed as string after 05-01 added the key to en.json. TypeScript will enforce this.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend VideoEntry interface and flag all entries in videos.ts</name>
  <files>content/videos.ts</files>
  <read_first>
    - content/videos.ts — read the full file; note the 3 current entries with their videoId, title, and category values. The videoId values must be preserved (they serve as React keys in the grid). Only add isPlaceholder and update title values.
  </read_first>
  <action>
    In content/videos.ts, make two changes:

    1. Extend the VideoEntry interface by adding one optional field after the existing fields:
       isPlaceholder?: boolean
       Do not change or remove any existing interface fields.

    2. Update all 3 entries in the videos array:
       - Add isPlaceholder: true to each entry
       - Update the title field to neutral values (these become aria-labels; the current UCL/Dynamo titles are incorrect for Artem):
         Entry 1 (videoId: 'Y0H9y0l67bo'): title → "Match Highlights — Coming Soon"
         Entry 2 (videoId: 'kAvYK_gAr90'): title → "Training Session — Coming Soon"
         Entry 3 (videoId: 'Oj0nkoFJZws'): title → "Skills Compilation — Coming Soon"
       - Keep videoId and category values exactly as-is on every entry
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - content/videos.ts VideoEntry interface contains the line "isPlaceholder?: boolean"
    - content/videos.ts videos array has exactly 3 entries
    - All 3 entries have isPlaceholder: true
    - No entry's title contains "UCL", "Dynamo", "Dribbling Drills", or "Best Moments" (stale placeholder content removed)
    - All 3 original videoId values are still present ('Y0H9y0l67bo', 'kAvYK_gAr90', 'Oj0nkoFJZws')
    - TypeScript compilation exits with no errors
  </acceptance_criteria>
  <done>VideoEntry interface has isPlaceholder?: boolean. All 3 video entries are flagged isPlaceholder: true with neutral title text. videoId values are preserved as stable React keys. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Implement VideoCard placeholder variant and update HighlightsSection</name>
  <files>components/ui/VideoCard.tsx, components/ui/VideoCard.module.scss, components/sections/HighlightsSection.tsx</files>
  <read_first>
    - components/ui/VideoCard.tsx — read the full component; note the existing Props interface, the isPlaying state, the thumbnailSlot structure, and the cardTitle paragraph. The placeholder guard is inserted at the very top of the function body, before the useState call is used.
    - components/ui/VideoCard.module.scss — read all existing class definitions; note the design tokens used (--color-bg-elevated, --color-border, --color-text, --color-text-muted, --space-4, --space-2). New classes must use the same token convention — var(--token-name), not $scss-variables.
    - components/sections/HighlightsSection.tsx — read the full component; the only change is inside the map — the VideoCard render call gains the comingSoonLabel prop.
  </read_first>
  <action>
    In components/ui/VideoCard.tsx:

    1. Add Clock to the lucide-react import (alongside the existing Play import):
       import { Play, Clock } from 'lucide-react';

    2. Extend the Props interface to include the new optional prop:
       interface Props {
         video: VideoEntry;
         comingSoonLabel?: string;
       }
       Update the destructuring: ({ video, comingSoonLabel }: Props)

    3. Add a placeholder guard at the top of the function body, before the existing return — insert it directly after the useState(false) line so the hook is still called unconditionally:
       if (video.isPlaceholder && comingSoonLabel) {
         return (
           <article className={styles.card}>
             <div className={styles.thumbnailSlot}>
               <div className={styles.placeholderSlot} aria-hidden="true">
                 <Clock size={32} className={styles.placeholderIcon} />
               </div>
             </div>
             <p className={styles.cardTitle}>{comingSoonLabel}</p>
           </article>
         );
       }

    4. The existing play/iframe render path below is UNCHANGED. Do not modify any existing JSX, state logic, or YouTube embed code.

    In components/ui/VideoCard.module.scss:

    Add two new classes at the end of the file (after the existing .cardTitle rule). Use the existing SCSS comment style and the var(--token) convention:

    .placeholderSlot — fills the 16/9 thumbnailSlot area:
      position: absolute
      inset: 0
      background: var(--color-bg-elevated)
      display: flex
      align-items: center
      justify-content: center
      flex-direction: column
      gap: var(--space-2)
      opacity: 0.65
      cursor: default

    .placeholderIcon — styles the Clock icon:
      color: var(--color-text-muted)

    In components/sections/HighlightsSection.tsx:

    Change only the VideoCard call inside the map. Update the single line from:
      <VideoCard video={video} />
    to:
      <VideoCard video={video} comingSoonLabel={video.isPlaceholder ? dict.comingSoon : undefined} />

    No other changes to HighlightsSection. The dict prop type (Dictionary['highlights']) already includes comingSoon after 05-01 updated en.json.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/ui/VideoCard.tsx imports Clock from 'lucide-react'
    - components/ui/VideoCard.tsx Props interface has "comingSoonLabel?: string"
    - components/ui/VideoCard.tsx contains the string "video.isPlaceholder && comingSoonLabel" (the guard condition)
    - components/ui/VideoCard.tsx does not construct img.youtube.com URL inside the isPlaceholder branch (the early return exits before reaching that code)
    - components/ui/VideoCard.module.scss contains ".placeholderSlot" class definition
    - components/ui/VideoCard.module.scss uses "var(--color-text-muted)" in the .placeholderIcon rule
    - components/sections/HighlightsSection.tsx contains "dict.comingSoon" in the VideoCard prop
    - components/sections/HighlightsSection.tsx does not contain "<VideoCard video={video} />" (the old bare call — now it has the comingSoonLabel prop)
    - TypeScript compilation exits with no errors
    - npm run build exits 0 (both locales pre-render without error)
  </acceptance_criteria>
  <done>VideoCard renders a muted placeholder card (Clock icon + comingSoon label) when isPlaceholder is true and comingSoonLabel is provided. The existing play/YouTube path is unchanged. HighlightsSection passes the label conditionally. Both locales build without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content/videos.ts → VideoCard render | isPlaceholder flag gates which render branch executes — no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | VideoCard thumbnail img src | mitigate | Placeholder guard (early return) prevents img.youtube.com requests for stale videoIds — implemented in Task 2 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; Clock icon is from existing lucide-react dependency |
</threat_model>

<verification>
After both tasks complete:

1. Run `npx tsc --noEmit` — must exit 0
2. Run `npm run build` — static export must complete; both `/ua/` and `/en/` highlight sections pre-render
3. Verify no network request to img.youtube.com during placeholder rendering: open browser DevTools Network tab, navigate to highlights section — no youtube thumbnail requests should appear
4. Verify both locales show the coming soon text:
   - EN highlights section: each card title shows "Highlights coming soon"
   - UA highlights section: each card title shows "Відео з'являться незабаром"
5. Verify layout is unchanged: 3 cards remain in the grid, spacing and dimensions identical to before
</verification>

<success_criteria>
- content/videos.ts: VideoEntry interface has isPlaceholder?: boolean; all 3 entries have isPlaceholder: true; no UCL/Dynamo titles
- components/ui/VideoCard.tsx: Clock imported, comingSoonLabel? prop added, placeholder guard present before the existing render path
- components/ui/VideoCard.module.scss: .placeholderSlot and .placeholderIcon classes added using var(--color-*) tokens
- components/sections/HighlightsSection.tsx: comingSoonLabel passed conditionally using dict.comingSoon
- npm run build exits 0 (VIDEO-01 satisfied — no raw YouTube IDs visible to visitors; placeholder messaging shown instead)
</success_criteria>

<output>
Create `.planning/phases/05-player-identity/05-02-SUMMARY.md` when done
</output>
