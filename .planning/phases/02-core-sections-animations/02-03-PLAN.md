---
phase: 02-core-sections-animations
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - components/sections/AboutSection.tsx
  - components/sections/AboutSection.module.scss
  - components/sections/TrophiesSection.tsx
  - components/sections/TrophiesSection.module.scss
  - components/sections/ClubSection.tsx
  - components/sections/ClubSection.module.scss
  - components/sections/TeamSection.tsx
  - components/sections/TeamSection.module.scss
  - app/[lang]/page.tsx
autonomous: true
requirements:
  - PLAYER-01
  - PLAYER-02
  - PLAYER-03
  - PLAYER-04
  - SECT-01
  - SECT-02
  - SECT-03

must_haves:
  truths:
    - "Visitor can read the player's complete profile: name, position, working foot (Footprints icon), nationality, date of birth, narrative bio, and 6 stat bars"
    - "Stat bars animate from 0% to their value% via GSAP on scroll entry (power2.out, 1.0s, stagger 0.15s)"
    - "Visitor can view 3 trophy cards in a grid (name, year, competition) with crimson left stripe"
    - "Visitor can view current club name, logo placeholder (160×160px), and description"
    - "Visitor can view team name, logo placeholder (160×160px), and description"
    - "All four content sections (About, Trophies, Club, Team) animate in via useScrollReveal() on scroll"
    - "page.tsx assembles all 8 sections in nav order; npx next build exits 0"
  artifacts:
    - path: "components/sections/AboutSection.tsx"
      provides: "Bio grid (dl/dt/dd), narrative bio, 6 FIFA-style stat bars with GSAP animation"
      exports: ["AboutSection"]
    - path: "components/sections/TrophiesSection.tsx"
      provides: "Trophy grid (3 cards) with useScrollReveal scroll entrance"
      exports: ["TrophiesSection"]
    - path: "components/sections/ClubSection.tsx"
      provides: "Club info block with logo placeholder and useScrollReveal"
      exports: ["ClubSection"]
    - path: "components/sections/TeamSection.tsx"
      provides: "Team info block with logo placeholder and useScrollReveal"
      exports: ["TeamSection"]
  key_links:
    - from: "AboutSection.tsx statRef"
      to: "GSAP [data-stat-fill]"
      via: "gsap.from('[data-stat-fill]', { width: '0%', duration: 1.0, stagger: 0.15, ease: 'power2.out' })"
      pattern: "data-stat-fill"
    - from: "TrophiesSection.tsx / ClubSection.tsx / TeamSection.tsx"
      to: "useScrollReveal"
      via: "useScrollReveal(containerRef) with .reveal-item class on title and content items"
      pattern: "useScrollReveal.*containerRef"
    - from: "app/[lang]/page.tsx"
      to: "all 5 section components"
      via: "imports and JSX props passing player data fields"
      pattern: "AboutSection.*data=.*player"
---

<objective>
Build the four player profile and supporting content sections (About, Trophies, Club, Team), then wire the complete 8-section page assembly into page.tsx. This completes the Phase 2 scout-facing homepage.

Purpose: After this plan, a scout can scroll through the full cinematic homepage — hero entrance, player profile with animated stat bars, trophies grid, club and team information, and stub placeholders for Phase 3 media and contact sections.

Output: AboutSection (bio grid + stat bars + narrative), TrophiesSection (trophy cards grid), ClubSection (logo placeholder + description), TeamSection (logo placeholder + description), all with GSAP scroll reveals; page.tsx updated with all 8 sections; `npx next build` exits 0.

Note on PLAYER-02: The HeroSection .heroBg element (built in Plan 02) supports background-image swap — the photo slot is structurally complete. Real player photo is deferred as a content update per D-01. PLAYER-02 is marked "Slot built — real asset content update pending."
</objective>

<execution_context>
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-sections-animations/02-CONTEXT.md
@.planning/phases/02-core-sections-animations/02-RESEARCH.md
@.planning/phases/02-core-sections-animations/02-UI-SPEC.md
@.planning/phases/02-core-sections-animations/02-PATTERNS.md
@.planning/phases/02-core-sections-animations/02-02-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 — used as props in all section components -->
From content/player.ts:
  export interface PlayerStats { pace: number; dribbling: number; shooting: number; passing: number; physical: number; defending: number; }
  export interface Trophy { name: string; year: number; competition: string; }
  export interface Club { name: string; logo: string | null; description: string; }
  export interface Team { name: string; logo: string | null; description: string; }
  export interface Player { fullName: string; position: string; workingFoot: 'Right' | 'Left' | 'Both'; dateOfBirth: string; nationality: string; bio: string; stats: PlayerStats; trophies: Trophy[]; club: Club; team: Team; }

<!-- Hook from Plan 01 — consumed by all four sections in this plan -->
From lib/animations/useScrollReveal.ts:
  export function useScrollReveal<T extends HTMLElement>(containerRef: RefObject<T | null>): void
  // Targets .reveal-item class within { scope: containerRef }

<!-- lucide-react — installed in Plan 01 -->
import { Footprints } from 'lucide-react';
// Usage: <Footprints size={16} aria-hidden="true" color="var(--color-text-muted)" />

<!-- GSAP stat bar pattern (from RESEARCH.md Pattern 3 and PATTERNS.md AboutSection) -->
// Two useGSAP blocks in AboutSection:
// 1. useScrollReveal(containerRef) — bio grid + narrative bio reveal
// 2. Separate useGSAP({ scope: statRef }) — stat bar fill animation
// stat bar HTML: data-stat-fill attribute, data-value={value}, style={{ width: `${value}%` }}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AboutSection with bio grid, narrative bio, and GSAP stat bars</name>
  <files>components/sections/AboutSection.tsx, components/sections/AboutSection.module.scss</files>
  <read_first>
    - .planning/phases/02-core-sections-animations/02-PATTERNS.md — "AboutSection.tsx" and "AboutSection.module.scss" sections (complete code patterns including stat bar GSAP block and bio grid dl/dt/dd accessibility pattern)
    - .planning/phases/02-core-sections-animations/02-RESEARCH.md — Pattern 3 "GSAP Stat Bar Animation" (complete useGSAP block with reduced-motion path using gsap.set)
    - .planning/phases/02-core-sections-animations/02-UI-SPEC.md — "About / Profile Section — Visual Contract" (layout, bio grid fields, stat bar anatomy, dimensions)
    - lib/animations/useScrollReveal.ts — hook signature to pass containerRef correctly
    - CLAUDE.md — animation ownership rule (stat bars: GSAP only, no CSS transition on .statFill)
  </read_first>
  <action>
    Create components/sections/AboutSection.tsx with 'use client' at the top.

    Imports: useRef from 'react'; useGSAP from '@gsap/react'; gsap from 'gsap'; ScrollTrigger from 'gsap/ScrollTrigger'; Footprints from 'lucide-react'; useScrollReveal from '@/lib/animations/useScrollReveal'; Player from '@/content/player'; styles from './AboutSection.module.scss'.

    At module scope: gsap.registerPlugin(ScrollTrigger, useGSAP).

    Props interface: { data: Player }.

    Inside component: declare containerRef as useRef<HTMLElement>(null) and statRef as useRef<HTMLDivElement>(null). Call useScrollReveal(containerRef) — this animates the section title and bio cells on scroll via .reveal-item class.

    Second GSAP block for stat bars — call useGSAP with scope: statRef. Inside: check window.matchMedia('(prefers-reduced-motion: reduce)').matches. If true, call gsap.set('[data-stat-fill]', { width: (i, el) => el.dataset.value + '%' }) and return. If false, call gsap.from('[data-stat-fill]', { width: '0%', duration: 1.0, stagger: 0.15, ease: 'power2.out', scrollTrigger: { trigger: statRef.current, start: 'top 80%', scrub: false } }).

    JSX structure: outer section with id="about", ref={containerRef}, className={styles.section}. Inside: h2 with className combining styles.sectionTitle and reveal-item class (section title animates first per D-08). Below heading: a div with className={styles.layout} containing two columns.

    Left column: bio grid as dl element with className={styles.bioGrid} and reveal-item class. Four div.bioCell children each containing: dt with className={styles.bioField} (field label) and dd with className={styles.bioValue} (field value). Fields and values: "Name" / {data.fullName}, "Position" / {data.position}, "Working Foot" / (Footprints icon + {data.workingFoot}), "Date of Birth" / {data.dateOfBirth}. For Working Foot dd: render Footprints component with size={16}, aria-hidden="true", and a style or color prop using "var(--color-text-muted)" as the color value. After the dl: a p element with className={styles.bio} and reveal-item class rendering {data.bio}.

    Right column: div with ref={statRef} and role="list". Inside: map over the six stat entries — derive the labels and values from data.stats. The six entries in order: Pace (data.stats.pace), Dribbling (data.stats.dribbling), Shooting (data.stats.shooting), Passing (data.stats.passing), Physical (data.stats.physical), Defending (data.stats.defending). For each stat: a div with role="listitem", aria-label="{label}: {value} out of 100", className={styles.statRow}. Inside: div.statHeader with span.statLabel (label text) and span.statValue (value number). Then div.statTrack containing div.statFill with data-stat-fill attribute, data-value={value}, style={{ width: `${value}%` }}. The statFill initial style width is set to the real value% — GSAP's from() overrides this to 0% during animation, preventing flash (RESEARCH.md Pitfall 5).

    Create components/sections/AboutSection.module.scss. All tokens via var(). Mixins auto-injected (no @use).

    .section: padding var(--space-16) var(--space-4) mobile; @include respond-to('lg'): padding var(--space-24) var(--space-8).

    .sectionTitle: font-family var(--font-heading), font-size var(--text-3xl) mobile, font-weight 700, text-transform uppercase, color var(--color-text), line-height 1.1, margin-bottom var(--space-12). @include respond-to('md'): font-size var(--text-4xl). ::after: content '', display block, width 40px, height 3px, background-color var(--color-accent), margin-top var(--space-2).

    .layout: display grid, grid-template-columns 1fr mobile; @include respond-to('lg'): grid-template-columns 1fr 1fr, gap var(--space-16).

    .bioGrid: display grid, grid-template-columns 1fr mobile; @include respond-to('md'): grid-template-columns 1fr 1fr, gap var(--space-6). Container: background var(--color-bg-elevated), padding var(--space-4), border 1px solid var(--color-border), border-radius 4px.

    .bioField: font-size var(--text-sm), color var(--color-text-muted), text-transform uppercase, letter-spacing 0.08em, font-family var(--font-body), margin-bottom var(--space-1).

    .bioValue: font-size var(--text-base), font-weight 700, color var(--color-text), font-family var(--font-body), display flex, align-items center, gap var(--space-1).

    .bio: font-size var(--text-base), color var(--color-text), line-height 1.6, margin-top var(--space-6).

    .statRow: margin-bottom var(--space-6).

    .statHeader: display flex, justify-content space-between, margin-bottom var(--space-1).

    .statLabel: font-size var(--text-sm), color var(--color-text-muted), font-family var(--font-body).

    .statValue: font-size var(--text-sm), font-weight 700, color var(--color-text), font-family var(--font-heading).

    .statTrack: width 100%, height 8px, border-radius 4px, background var(--color-border), overflow hidden.

    .statFill: height 100%, border-radius 4px, background var(--color-accent). CRITICAL: NO transition property — GSAP owns the width animation. Any CSS transition on this element violates the animation ownership rule.
  </action>
  <verify>
    <automated>cd /c/Users/dmytro.kukharuk/projects/stydy/future-legend-dev && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/sections/AboutSection.tsx starts with 'use client'
    - AboutSection.tsx contains gsap.registerPlugin at module scope
    - AboutSection.tsx has TWO separate refs: containerRef (HTMLElement) for useScrollReveal, statRef (HTMLDivElement) for stat bar GSAP
    - useScrollReveal(containerRef) is called — handles section title + bio grid reveal
    - Second useGSAP block uses { scope: statRef } and targets '[data-stat-fill]'
    - Reduced-motion path uses gsap.set('[data-stat-fill]', ...) to set final widths immediately
    - Bio grid uses dl/dt/dd HTML elements (not div/span table)
    - Footprints icon has aria-hidden="true" attribute
    - statFill div has data-stat-fill attribute, data-value={value}, and style={{ width: `${value}%` }}
    - AboutSection.module.scss .statFill class does NOT contain any `transition` property
    - Section h2 and bio grid dl both have the literal class `reveal-item` applied
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AboutSection renders bio grid with dl/dt/dd (Name, Position, Working Foot with Footprints icon, Date of Birth), narrative bio paragraph, and 6 FIFA-style stat bars. Stat bars animate from 0% to value% on scroll via GSAP. Bio grid and title animate via useScrollReveal. Reduced motion paths for both animation systems are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Build TrophiesSection, ClubSection, TeamSection with scroll reveals</name>
  <files>
    components/sections/TrophiesSection.tsx,
    components/sections/TrophiesSection.module.scss,
    components/sections/ClubSection.tsx,
    components/sections/ClubSection.module.scss,
    components/sections/TeamSection.tsx,
    components/sections/TeamSection.module.scss
  </files>
  <read_first>
    - .planning/phases/02-core-sections-animations/02-PATTERNS.md — "TrophiesSection.tsx" + ".module.scss", "ClubSection.tsx" + ".module.scss", "TeamSection.tsx" + ".module.scss" sections
    - .planning/phases/02-core-sections-animations/02-UI-SPEC.md — "Trophies Section — Visual Contract", "Club Section — Visual Contract", "Team Section — Visual Contract"
    - lib/animations/useScrollReveal.ts — confirm hook exists from Plan 01 before referencing it
    - CLAUDE.md — 'use client' boundary rule, content data flow rule (sections receive props, never import from content/)
  </read_first>
  <action>
    Create TrophiesSection.tsx with 'use client'. Imports: useRef from 'react'; useScrollReveal from '@/lib/animations/useScrollReveal'; import type { Trophy } from '@/content/player'; styles from './TrophiesSection.module.scss'. Props interface: { trophies: Trophy[] }. Inside component: containerRef as useRef<HTMLElement>(null); call useScrollReveal(containerRef). JSX: section with id="trophies", ref={containerRef}, className={styles.section}. Inside: h2 with className combining styles.sectionTitle and 'reveal-item' (literal bare class for GSAP). Below: ul with role="list" and className={styles.grid}. Map over trophies: each li with role="listitem", key={trophy.name + trophy.year}, className combining styles.card and 'reveal-item'. Inside card li: p with className={styles.trophyName} rendering trophy.name; p with className={styles.competition} rendering trophy.competition; p with className={styles.year} rendering trophy.year.

    Create TrophiesSection.module.scss. .section: padding mobile/desktop as in AboutSection. .sectionTitle: same pattern as AboutSection (Oswald, text-3xl mobile / text-4xl desktop, uppercase, crimson ::after). .grid: display grid, grid-template-columns 1fr mobile; @include respond-to('sm'): 1fr 1fr; @include respond-to('lg'): repeat(3, 1fr). gap var(--space-6), list-style none, padding 0. .card: background var(--color-bg-elevated), border 1px solid var(--color-border), border-left 4px solid var(--color-accent) (crimson left stripe per UI-SPEC), border-radius 4px, padding var(--space-6). .trophyName: font-family var(--font-heading), font-size var(--text-2xl), font-weight 700, color var(--color-text), margin-bottom var(--space-2). .competition: font-size var(--text-base), color var(--color-text), margin-bottom var(--space-1). .year: font-size var(--text-sm), color var(--color-text-muted).

    Create ClubSection.tsx with 'use client'. Imports: useRef from 'react'; useScrollReveal from '@/lib/animations/useScrollReveal'; import type { Club } from '@/content/player'; styles from './ClubSection.module.scss'. Props interface: { club: Club }. Inside component: containerRef as useRef<HTMLElement>(null); call useScrollReveal(containerRef). JSX: section with id="club", ref={containerRef}, className={styles.section}. Inside: h2 with className combining styles.sectionTitle and 'reveal-item'. Below: div with className combining styles.content and 'reveal-item'. Inside content div: two children — logo placeholder div and text div. Logo placeholder div with className={styles.logoPlaceholder}: if club.logo is null render a span with className={styles.logoCaption} text "Club crest"; if club.logo is non-null render img with src={club.logo}, alt={club.name + ' crest'}, className={styles.logoImage}. Text div with className={styles.clubText}: p with className={styles.clubName} rendering club.name; p with className={styles.description} rendering club.description.

    Create ClubSection.module.scss. .section: padding mobile/desktop, background var(--color-bg). .sectionTitle: same Oswald pattern as other sections with crimson ::after. .content: display grid, grid-template-columns 1fr mobile; @include respond-to('md'): grid-template-columns 160px 1fr, gap var(--space-8). .logoPlaceholder: width 160px, height 160px, background var(--color-bg-elevated), border 1px solid var(--color-border), border-radius 8px, display flex, align-items center, justify-content center. .logoCaption: font-size var(--text-xs), color var(--color-text-muted), font-family var(--font-body). .clubName: font-family var(--font-heading), font-size var(--text-3xl), font-weight 700, color var(--color-text), margin-bottom var(--space-4). .description: font-size var(--text-base), color var(--color-text), line-height 1.6.

    Create TeamSection.tsx — mirrors ClubSection.tsx exactly but with: 'use client'; import type { Team } from '@/content/player'; props interface { team: Team }; id="team"; section title "Team"; logo alt={team.name + ' crest'}; caption "Team crest"; render team.name and team.description. Import styles from './TeamSection.module.scss'.

    Create TeamSection.module.scss — mirrors ClubSection.module.scss exactly with one change: .section background is var(--color-bg-elevated) instead of var(--color-bg) (creates alternating rhythm with club section per UI-SPEC Team section surface specification).
  </action>
  <verify>
    <automated>cd /c/Users/dmytro.kukharuk/projects/stydy/future-legend-dev && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - TrophiesSection.tsx starts with 'use client' and calls useScrollReveal(containerRef)
    - TrophiesSection.module.scss .card contains `border-left: 4px solid var(--color-accent)` (crimson left stripe)
    - TrophiesSection.module.scss .grid contains `repeat(3, 1fr)` in a breakpoint for 3-column desktop layout
    - ClubSection.tsx starts with 'use client' and calls useScrollReveal(containerRef)
    - ClubSection.module.scss .section contains `background: var(--color-bg)`
    - ClubSection.module.scss .logoPlaceholder contains `width: 160px` and `height: 160px`
    - TeamSection.tsx starts with 'use client' and calls useScrollReveal(containerRef)
    - TeamSection.module.scss .section contains `background: var(--color-bg-elevated)` (different from ClubSection)
    - TrophiesSection, ClubSection, and TeamSection do NOT import the `player` runtime constant from '@/content/player' — `import type` for interface types (Trophy, Club, Team) is permitted
    - All section h2 headings apply the bare class 'reveal-item' alongside their styles class
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TrophiesSection renders 3 trophy cards in a responsive grid with crimson left border stripe. ClubSection and TeamSection each render a 160×160px logo placeholder alongside the club/team name and description. All three sections fade+slide in on scroll via useScrollReveal. Alternating background colors (bg / bg-elevated) create visual rhythm.</done>
</task>

<task type="auto">
  <name>Task 3: Complete page.tsx 8-section assembly and run final build</name>
  <files>app/[lang]/page.tsx</files>
  <read_first>
    - app/[lang]/page.tsx — current state from Plan 02 (has Hero + 3 stubs, needs About/Trophies/Club/Team added)
    - .planning/phases/02-core-sections-animations/02-UI-SPEC.md — "Page Assembly Contract (D-21)" for exact section order
    - CLAUDE.md — content data flow rule (page.tsx is ONLY importer of content/player.ts)
    - components/sections/ — confirm all 5 section components exist before importing
  </read_first>
  <action>
    Update app/[lang]/page.tsx to add the remaining four section imports and include them in the JSX. This file is a Server Component — no 'use client'.

    Add imports for AboutSection, TrophiesSection, ClubSection, and TeamSection from '@/components/sections/AboutSection', '@/components/sections/TrophiesSection', '@/components/sections/ClubSection', '@/components/sections/TeamSection'.

    Update the main element JSX to include all 8 sections in this exact nav order (per D-21): HeroSection with data={player}; AboutSection with data={player}; TrophiesSection with trophies={player.trophies}; ClubSection with club={player.club}; TeamSection with team={player.team}; SectionStub with id="highlights" title="Highlights"; SectionStub with id="gallery" title="Gallery"; SectionStub with id="contact" title="Contact".

    Keep the existing imports (player, Nav, HeroSection, SectionStub) from Plan 02. Do not add 'use client'. Do not import from content/ in any other file.

    After updating page.tsx, run `npx next build` and verify it exits 0. This is the cross-cutting Phase 2 build constraint from ROADMAP.md.

    PLAYER-02 acknowledgment: HeroSection's .heroBg div (built in Plan 02) is the photo slot. It currently shows the gradient placeholder. Real player photo can be added via background-image property change with no code changes. PLAYER-02 is partially satisfied — structural slot built, real photo deferred (D-01).
  </action>
  <verify>
    <automated>cd /c/Users/dmytro.kukharuk/projects/stydy/future-legend-dev && npx next build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - app/[lang]/page.tsx imports all 5 named section components: HeroSection, AboutSection, TrophiesSection, ClubSection, TeamSection
    - app/[lang]/page.tsx renders sections in this exact order inside main: HeroSection, AboutSection, TrophiesSection, ClubSection, TeamSection, SectionStub(highlights), SectionStub(gallery), SectionStub(contact)
    - app/[lang]/page.tsx does NOT have 'use client'
    - app/[lang]/page.tsx only imports from '@/content/player' (no section component imports content directly)
    - `npx next build` exits 0 with no TypeScript errors and no module-not-found errors
    - The build output contains /ua/ and /en/ as pre-rendered static HTML files
  </acceptance_criteria>
  <done>page.tsx assembles all 8 sections in D-21 nav order. `npx next build` exits 0. The complete Phase 2 scout-facing cinematic homepage is built and statically exportable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player.trophies[] → JSX rendering | Array content from TypeScript constant rendered as React text nodes |
| club.logo / team.logo → img src | String or null from content/player.ts — null handled with conditional rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | club.logo / team.logo img src attribute | accept | logo fields are null in Phase 2 (D-16, D-17) — img is conditionally hidden when logo is null; no user-supplied URLs; values are TypeScript constants |
| T-02-07 | Tampering | Trophy card content rendered from trophies[] | accept | Content is TypeScript array constants from content/player.ts — rendered as React text nodes, not HTML; no dangerouslySetInnerHTML |
| T-02-08 | Tampering | stat bar data-value attribute | accept | Values are number literals from PlayerStats interface (e.g. 78, 82); no user input; attribute used only for gsap.set() read in reduced-motion path |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — all packages verified in Phase 1 and Plan 01. slopcheck cleared for lucide-react in RESEARCH.md |
</threat_model>

<verification>
After all three tasks complete:
- `npx next build` exits 0 (mandatory Phase 2 constraint)
- grep confirms: TrophiesSection.module.scss contains 'border-left: 4px solid var(--color-accent)'
- grep confirms: ClubSection.module.scss contains 'background: var(--color-bg)'
- grep confirms: TeamSection.module.scss contains 'background: var(--color-bg-elevated)'
- grep confirms: AboutSection.module.scss .statFill contains NO 'transition' property
- grep confirms: AboutSection.tsx contains 'data-stat-fill' (GSAP selector)
- grep confirms: page.tsx imports all five named section components and renders them in D-21 order
- grep confirms: no section component imports the `player` runtime constant from '@/content/player' (i.e., no `import { player }` or `import * as` from content/player in section files; `import type` for interface types is permitted)
</verification>

<success_criteria>
- AboutSection: bio grid (dl/dt/dd with Footprints icon on working foot), narrative bio paragraph, 6 stat bars with GSAP fill animation (0%→value%, power2.out, 1.0s, stagger 0.15s, reduced-motion path via gsap.set)
- TrophiesSection: 3 trophy cards in responsive grid (1→2→3 columns), crimson left border stripe, useScrollReveal entrance
- ClubSection + TeamSection: logo placeholder (160×160px), club/team name, description, useScrollReveal, alternating background colors
- page.tsx: 8-section assembly in D-21 nav order, Server Component, sole content/player.ts importer
- `npx next build` exits 0 — the complete Phase 2 cinematic homepage is production-ready
- PLAYER-02: Structural photo slot exists in HeroSection .heroBg; real photo deferred as content update per D-01
</success_criteria>

<output>
Create `.planning/phases/02-core-sections-animations/02-03-SUMMARY.md` when done
</output>
