---
phase: 02-core-sections-animations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/player.ts
  - lib/animations/useScrollReveal.ts
autonomous: true
requirements:
  - PLAYER-01
  - PLAYER-02
  - PLAYER-03
  - PLAYER-04
  - HOME-04

must_haves:
  truths:
    - "content/player.ts exports typed Player interface with all required fields"
    - "useScrollReveal hook uses useGSAP with scope and reduced-motion guard"
    - "No section component needs to import from content/ — data flows through page.tsx"
  artifacts:
    - path: "content/player.ts"
      provides: "Player, PlayerStats, Trophy, Club, Team interfaces + realistic placeholder data"
      exports: ["PlayerStats", "Trophy", "Club", "Team", "Player", "player"]
    - path: "lib/animations/useScrollReveal.ts"
      provides: "Shared GSAP scroll-reveal hook consumed by all non-hero sections"
      exports: ["useScrollReveal"]
  key_links:
    - from: "lib/animations/useScrollReveal.ts"
      to: "app/[lang]/ScrollFadeSection.tsx"
      via: "direct extraction — same useGSAP + scope + start: 'top 80%' pattern"
      pattern: "useGSAP.*scope.*containerRef"
    - from: "content/player.ts"
      to: "app/[lang]/page.tsx"
      via: "import { player } from '@/content/player' — sole import point"
      pattern: "export const player: Player"
---

<objective>
Build the two foundation modules that every Phase 2 section depends on: the complete typed player data layer and the shared scroll-reveal GSAP hook.

Purpose: All section components in Plans 02 and 03 receive typed props from content/player.ts and call useScrollReveal() for their entrance animation. Neither can be built without these two files.

Output: content/player.ts (full Player interfaces + realistic Ukrainian player data), lib/animations/useScrollReveal.ts (extracted GSAP hook with reduced-motion guard).
</objective>

<execution_context>
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-sections-animations/02-CONTEXT.md
@.planning/phases/02-core-sections-animations/02-RESEARCH.md
@.planning/phases/02-core-sections-animations/02-UI-SPEC.md
@.planning/phases/02-core-sections-animations/02-PATTERNS.md

<interfaces>
<!-- Source of truth for the hook this plan extracts. Read before writing useScrollReveal.ts. -->
From app/[lang]/ScrollFadeSection.tsx:
  - useGSAP pattern: gsap.from('.fade-item', { opacity: 0, y: 40, duration: 0.8, stagger: 0.1, scrollTrigger: { trigger: containerRef.current, start: 'top 80%', end: 'top 40%', scrub: false } })
  - { scope: containerRef } passed as second argument to useGSAP
  - gsap.registerPlugin(ScrollTrigger, useGSAP) at module scope (outside component)

From content/player.ts (current stub — must be fully replaced):
  export const player = { name: 'Player Name', position: 'Midfielder' };
  // This 4-line stub must be replaced with the full typed interfaces and data from UI-SPEC.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lucide-react and rewrite content/player.ts</name>
  <files>content/player.ts</files>
  <read_first>
    - content/player.ts — current stub (4 lines) to understand what must be replaced
    - .planning/phases/02-core-sections-animations/02-UI-SPEC.md — "Content Data Shape" section for exact interface definitions and authored player data
    - .planning/phases/02-core-sections-animations/02-PATTERNS.md — "content/player.ts (model, transform)" section for target pattern
    - CLAUDE.md — content data flow rule (page.tsx is the sole importer)
  </read_first>
  <action>
    First, install lucide-react (required by Plan 02 AboutSection): run `npm install lucide-react` in the project root. Verify exit 0.

    Then completely replace content/player.ts with the full typed implementation. The file must contain no 'use client' directive — it is a pure TypeScript data module.

    Define five named exports in this order: PlayerStats interface (pace, dribbling, shooting, passing, physical, defending — all number), Trophy interface (name: string, year: number, competition: string), Club interface (name: string, logo: string | null, description: string), Team interface (name: string, logo: string | null, description: string), Player interface (fullName: string, position: string, workingFoot: 'Right' | 'Left' | 'Both', dateOfBirth: string in "YYYY-MM-DD" format, nationality: string, bio: string, stats: PlayerStats, trophies: Trophy[], club: Club, team: Team).

    Then export the typed player constant with this exact authored data (per D-05, UI-SPEC Copywriting Contract, and RESEARCH.md authored bio):
    - fullName: 'Dmytro Kovalenko'
    - position: 'Central Midfielder'
    - workingFoot: 'Right'
    - dateOfBirth: '2006-03-14'
    - nationality: 'Ukrainian'
    - bio: the 3-sentence authored narrative from UI-SPEC "Content Data Shape" section (starts with "A technically gifted midfielder...")
    - stats: pace 78, dribbling 82, shooting 71, passing 88, physical 74, defending 69
    - trophies: three entries — { name: 'Youth League Champion', year: 2024, competition: 'UPL Youth League U18' }, { name: 'Cup Winner', year: 2023, competition: 'Kyiv Regional Youth Cup' }, { name: 'Tournament MVP', year: 2023, competition: 'Dana Cup Denmark' }
    - club: name 'Dynamo Kyiv U21', logo: null, description from UI-SPEC
    - team: name 'Ukraine U18 National Team', logo: null, description from UI-SPEC

    PLAYER-02 note: The photo slot is structurally built (HeroSection's .heroBg supports background-image swap per D-01). PLAYER-02 is partially satisfied — slot built, real photo deferred as a content update.
  </action>
  <verify>
    <automated>cd /c/Users/dmytro.kukharuk/projects/stydy/future-legend-dev && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - content/player.ts contains `export interface PlayerStats` with six numeric fields (pace, dribbling, shooting, passing, physical, defending)
    - content/player.ts contains `export interface Player` with workingFoot typed as union 'Right' | 'Left' | 'Both'
    - content/player.ts contains `export const player: Player` with fullName 'Dmytro Kovalenko'
    - content/player.ts has NO 'use client' directive
    - content/player.ts does NOT import from any React or Next.js module
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
    - `npm ls lucide-react` shows lucide-react listed as a dependency (not missing)
  </acceptance_criteria>
  <done>content/player.ts exports all five interfaces and the typed player constant; lucide-react is installed; TypeScript compilation is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create lib/animations/useScrollReveal.ts</name>
  <files>lib/animations/useScrollReveal.ts</files>
  <read_first>
    - app/[lang]/ScrollFadeSection.tsx — the exact source to extract from (lines 1-30)
    - .planning/phases/02-core-sections-animations/02-PATTERNS.md — "useScrollReveal.ts (hook, event-driven)" section for exact delta from ScrollFadeSection
    - .planning/phases/02-core-sections-animations/02-RESEARCH.md — Pattern 1 for reduced-motion guard and generic type signature
    - CLAUDE.md — GSAP rule (all GSAP inside useGSAP() from @gsap/react, inside 'use client' components)
  </read_first>
  <action>
    Create the lib/animations/ directory if it does not exist. Create lib/animations/useScrollReveal.ts.

    This file does NOT have 'use client' at the top — it is a hook imported by 'use client' components, not a component itself.

    Import RefObject from 'react'. Import useGSAP from '@gsap/react'. Import gsap from 'gsap'. Import ScrollTrigger from 'gsap/ScrollTrigger'.

    At module scope (outside any function), register plugins: gsap.registerPlugin(ScrollTrigger, useGSAP). This is idempotent — safe in multiple files.

    Export a generic function useScrollReveal with signature: function useScrollReveal<T extends HTMLElement>(containerRef: RefObject<T | null>): void. The generic type prevents the TypeScript assignability error described in RESEARCH.md Pitfall 4.

    Inside the function body, call useGSAP with two arguments:
    1. A callback that: checks window.matchMedia('(prefers-reduced-motion: reduce)').matches and returns early if true; then calls gsap.from('.reveal-item', { opacity: 0, y: 40, duration: 0.8, stagger: 0.1, ease: 'power1.out', scrollTrigger: { trigger: containerRef.current, start: 'top 80%', scrub: false } })
    2. The options object: { scope: containerRef }

    Key deltas from ScrollFadeSection.tsx: class selector changes from '.fade-item' to '.reveal-item'; the 'end' property is removed from scrollTrigger; the reduced-motion guard is added; the function is generic.

    Do NOT add 'use client' to this file. The hook is imported by client components that already declare 'use client'.
  </action>
  <verify>
    <automated>cd /c/Users/dmytro.kukharuk/projects/stydy/future-legend-dev && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/animations/useScrollReveal.ts exists
    - File does NOT contain 'use client' directive
    - File contains `gsap.registerPlugin(ScrollTrigger, useGSAP)` at module scope (outside the function body)
    - Function signature matches: `export function useScrollReveal<T extends HTMLElement>(containerRef: RefObject<T | null>): void`
    - File contains `window.matchMedia('(prefers-reduced-motion: reduce)').matches` check with early return
    - GSAP call uses '.reveal-item' (not '.fade-item')
    - useGSAP is called with `{ scope: containerRef }` as second argument
    - scrollTrigger config uses `start: 'top 80%'` and has NO 'end' property
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>lib/animations/useScrollReveal.ts is created with the correct generic signature, reduced-motion guard, .reveal-item selector, top 80% trigger, and scope option. TypeScript compilation is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | lucide-react package installation from external registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | lucide-react npm install | mitigate | Package legitimacy audit complete (RESEARCH.md): npm registry, lucide-icons org, MIT license, 2M+/wk downloads, no suspicious postinstall script, slopcheck OK |
| T-02-02 | Tampering | content/player.ts bio string | accept | Bio is a TypeScript string constant rendered as React text node via {data.bio} — no dangerouslySetInnerHTML, no XSS vector |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck verified [OK] for lucide-react per RESEARCH.md Package Legitimacy Audit 2026-05-19 |
</threat_model>

<verification>
After both tasks complete:
- `npx tsc --noEmit` exits 0
- `npm ls lucide-react` confirms package installed
- grep confirms: content/player.ts contains `export const player: Player` with fullName 'Dmytro Kovalenko'
- grep confirms: lib/animations/useScrollReveal.ts contains `gsap.registerPlugin` at module scope
- grep confirms: lib/animations/useScrollReveal.ts contains 'reveal-item' (not 'fade-item')
- grep confirms: lib/animations/useScrollReveal.ts does NOT contain 'use client'
</verification>

<success_criteria>
- content/player.ts: five exported interfaces (PlayerStats, Trophy, Club, Team, Player) + typed player constant with Dmytro Kovalenko data, no 'use client', no React imports, TypeScript strict-clean
- lib/animations/useScrollReveal.ts: generic hook, module-scope registerPlugin, reduced-motion guard, .reveal-item selector, { scope: containerRef }, no 'use client'
- lucide-react installed in package.json dependencies
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
Create `.planning/phases/02-core-sections-animations/02-01-SUMMARY.md` when done
</output>
