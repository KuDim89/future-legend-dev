---
phase: 02-core-sections-animations
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - components/sections/HeroSection.tsx
  - components/sections/HeroSection.module.scss
  - components/sections/SectionStub.tsx
  - components/sections/SectionStub.module.scss
  - app/[lang]/page.tsx
  - app/[lang]/layout.tsx
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04

must_haves:
  truths:
    - "Visitor lands on a full-screen hero section showing the player's name, position, and a CTA button"
    - "Hero background parallaxes at 30% yPercent via GSAP scroll (scrub: 1) as user scrolls"
    - "Hero text (name, position, CTA) fades in on mount via Framer Motion stagger (0.2/0.5/0.8s delays)"
    - "CTA button links to #contact and scrolls smoothly via Lenis"
    - "SectionStub renders dark section with title only — no animation"
    - "page.tsx assembles Nav + HeroSection + 3 SectionStub placeholders with build passing"
  artifacts:
    - path: "components/sections/HeroSection.tsx"
      provides: "Full-screen hero with GSAP parallax (bgRef) + Framer Motion mount stagger (text layer)"
      exports: ["HeroSection"]
    - path: "components/sections/HeroSection.module.scss"
      provides: "Hero layout with overflow:hidden, heroBg at height:115%, heroContent at z-index:1"
      contains: "overflow: hidden"
    - path: "components/sections/SectionStub.tsx"
      provides: "Server component accepting id and title props, no animation"
      exports: ["SectionStub"]
    - path: "components/sections/SectionStub.module.scss"
      provides: "Stub section: min-height 60vh, centered title with crimson accent rule"
    - path: "app/[lang]/page.tsx"
      provides: "Page assembly with Nav + HeroSection + 3 SectionStubs, imports player from content/player"
    - path: "app/[lang]/layout.tsx"
      provides: "Updated metadata with player name"
  key_links:
    - from: "HeroSection.tsx bgRef"
      to: "GSAP ScrollTrigger"
      via: "gsap.to(bgRef.current, { yPercent: 30, scrub: 1 })"
      pattern: "gsap.to.*bgRef.*yPercent"
    - from: "HeroSection.tsx text layer"
      to: "Framer Motion"
      via: "motion.h1, motion.p, motion.a with initial/animate"
      pattern: "motion\\.(h1|p|a).*initial.*animate"
    - from: "app/[lang]/page.tsx"
      to: "content/player.ts"
      via: "import { player } from '@/content/player'"
      pattern: "import.*player.*content/player"
---

<objective>
Build the Hero section (the most complex component in Phase 2 — dual animation systems on separate DOM nodes), the SectionStub reusable component, and wire up the initial page.tsx assembly with Nav + HeroSection + three stubs. Delivers a working, buildable cinematic homepage landing.

Purpose: After this plan, a visitor can land on the site and experience the full hero entrance — parallax background, staggered text reveal, and a CTA button. Page.tsx assembles correctly and `npx next build` exits 0.

Output: HeroSection.tsx + HeroSection.module.scss (dual-system animated hero), SectionStub.tsx + SectionStub.module.scss (server component stub), page.tsx rewritten to 8-section assembly structure (with remaining sections stubbed — Plan 03 fills them), layout.tsx with updated metadata.
</objective>

<execution_context>
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-sections-animations/02-CONTEXT.md
@.planning/phases/02-core-sections-animations/02-RESEARCH.md
@.planning/phases/02-core-sections-animations/02-UI-SPEC.md
@.planning/phases/02-core-sections-animations/02-PATTERNS.md
@.planning/phases/02-core-sections-animations/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — types the executor needs for HeroSection props -->
From content/player.ts:
  export interface Player {
    fullName: string;
    position: string;
    workingFoot: 'Right' | 'Left' | 'Both';
    dateOfBirth: string;
    nationality: string;
    bio: string;
    stats: PlayerStats;
    trophies: Trophy[];
    club: Club;
    team: Team;
  }
  export const player: Player  // Dmytro Kovalenko data

From app/[lang]/ScrollFadeSection.tsx (existing — GSAP pattern reference):
  gsap.registerPlugin(ScrollTrigger, useGSAP);  // module scope
  useGSAP(() => { gsap.from(...) }, { scope: containerRef })

From components/layout/Nav.tsx (existing — Framer Motion mount pattern):
  import { motion, AnimatePresence } from 'framer-motion';
  initial={{ opacity: 0, y: -8 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.2 }}

From app/[lang]/layout.tsx (existing — metadata to update):
  export const metadata: Metadata = {
    title: 'Design System — Future Legend Dev',  // must be replaced
    description: 'Football player personal website',
  };
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SectionStub component (server component, no animation)</name>
  <files>components/sections/SectionStub.tsx, components/sections/SectionStub.module.scss</files>
  <read_first>
    - .planning/phases/02-core-sections-animations/02-PATTERNS.md — "SectionStub.tsx (component, static)" and "SectionStub.module.scss" sections
    - .planning/phases/02-core-sections-animations/02-UI-SPEC.md — "Stub Sections — Visual Contract" section (D-18 through D-20)
    - CLAUDE.md — 'use client' boundary rule (SectionStub needs NO 'use client')
    - components/layout/Nav.tsx — for reference on how existing components are structured
  </read_first>
  <action>
    Create components/sections/ directory if it does not exist.

    Create components/sections/SectionStub.tsx as a Server Component — NO 'use client' directive, NO useRef, NO animation imports. Import styles from './SectionStub.module.scss'. Define Props interface with id: string and title: string. Export named function SectionStub({ id, title }: Props) returning a section element with id={id} and className={styles.stub} containing an h2 with className={styles.title} rendering {title}.

    Create components/sections/SectionStub.module.scss. The .stub class: min-height 60vh, background var(--color-bg), display flex, align-items center, justify-content center. The .title class: font-family var(--font-heading), font-size var(--text-3xl) on mobile, font-weight 700, text-transform uppercase, color var(--color-text), line-height 1.1. Add @include respond-to('md') override: font-size var(--text-4xl). Add ::after pseudo-element to .title: content '', display block, width 40px, height 3px, background-color var(--color-accent), margin-top var(--space-2).

    Tokens must be used as var(--token-name) — NOT $scss-variables. The respond-to mixin is auto-injected via sassOptions.additionalData and needs no @use import.
  </action>
  <verify>
    <automated>cd /c/Users/dmytro.kukharuk/projects/stydy/future-legend-dev && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/sections/SectionStub.tsx exists
    - SectionStub.tsx does NOT contain 'use client'
    - SectionStub.tsx does NOT import from gsap, framer-motion, or react hooks
    - SectionStub component accepts exactly two props: id (string) and title (string)
    - SectionStub.module.scss .stub class contains `min-height: 60vh`
    - SectionStub.module.scss .title class contains `background-color: var(--color-accent)` in ::after pseudo-element
    - No SCSS variable syntax ($token-name) appears anywhere in SectionStub.module.scss
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SectionStub.tsx is a Server Component with id and title props, no animation. SectionStub.module.scss renders a dark centered section with Oswald title and 40×3px crimson accent rule.</done>
</task>

<task type="auto">
  <name>Task 2: Create HeroSection with dual-system animation (GSAP parallax + Framer Motion mount)</name>
  <files>components/sections/HeroSection.tsx, components/sections/HeroSection.module.scss</files>
  <read_first>
    - .planning/phases/02-core-sections-animations/02-PATTERNS.md — "HeroSection.tsx" and "HeroSection.module.scss" sections (complete code patterns)
    - .planning/phases/02-core-sections-animations/02-RESEARCH.md — Pattern 2 "Hero Section Animation (Dual-System)" (animation delay table and full code example)
    - .planning/phases/02-core-sections-animations/02-UI-SPEC.md — "Hero Section — Full Visual Contract" (layout, parallax architecture, text entrance table, CTA spec)
    - CLAUDE.md — animation ownership rule (GSAP on .heroBg, Framer Motion on text layer — NEVER same element)
    - app/[lang]/ScrollFadeSection.tsx — GSAP registerPlugin and useGSAP scope pattern
    - components/layout/Nav.tsx — Framer Motion initial/animate pattern
  </read_first>
  <action>
    Create components/sections/HeroSection.tsx with 'use client' at the top.

    Imports: useRef from 'react'; useGSAP from '@gsap/react'; gsap from 'gsap'; ScrollTrigger from 'gsap/ScrollTrigger'; motion and useReducedMotion from 'framer-motion'; Player from '@/content/player'; styles from './HeroSection.module.scss'.

    At module scope (outside the component): gsap.registerPlugin(ScrollTrigger, useGSAP).

    Props interface: { data: Player }.

    Inside the component: declare heroRef as useRef<HTMLElement>(null); bgRef as useRef<HTMLDivElement>(null); prefersReduced from useReducedMotion().

    GSAP block (background parallax — D-03): call useGSAP with a callback that checks if prefersReduced and returns early if true, then calls gsap.to(bgRef.current, { yPercent: 30, ease: 'none', scrollTrigger: { trigger: heroRef.current, start: 'top top', end: 'bottom top', scrub: 1 } }). Pass { scope: heroRef } as second argument.

    JSX structure: outer section element with id="home", ref={heroRef}, role="banner", className={styles.hero}. Inside: first child div with ref={bgRef}, className={styles.heroBg}, aria-hidden="true" (Layer 1 — GSAP-controlled, no Framer Motion on this element). Second child div with className={styles.heroContent} (Layer 2 — Framer Motion-controlled, no GSAP on these elements).

    Inside heroContent: motion.h1 with initial={{ opacity: 0, y: 20 }}, animate={{ opacity: 1, y: 0 }}, transition={{ duration: 0.7, delay: prefersReduced ? 0 : 0.2, ease: 'easeOut' }}, className={styles.heroName}, rendering {data.fullName}. Then motion.p with initial={{ opacity: 0, y: 20 }}, animate={{ opacity: 1, y: 0 }}, transition={{ duration: 0.6, delay: prefersReduced ? 0 : 0.5, ease: 'easeOut' }}, className={styles.heroPosition}, rendering {data.position}. Then motion.a with href="#contact", initial={{ opacity: 0, y: 20 }}, animate={{ opacity: 1, y: 0 }}, transition={{ duration: 0.5, delay: prefersReduced ? 0 : 0.8, ease: 'easeOut' }}, className={styles.heroCta}, aria-label={"Contact " + data.fullName}, rendering text "Contact Me".

    Create components/sections/HeroSection.module.scss. Tokens via var() only (no $scss vars). Mixins auto-injected (no @use needed).

    .hero class: position relative, min-height 100svh (add fallback min-height 100vh on the line above), display flex, align-items center, justify-content center, overflow hidden (CRITICAL — prevents parallax bleed per RESEARCH.md Pitfall 2).

    .heroBg class: position absolute, inset 0, height 115% (prevents edge exposure at parallax extremes per RESEARCH.md Pitfall 2), z-index 0, will-change transform (GPU layer). Background: linear-gradient(135deg, var(--color-bg) 0%, #1A0A0E 50%, rgba(229, 0, 43, 0.15) 100%).

    .heroContent class: position relative, z-index 1, text-align center, padding 0 var(--space-4). Breakpoint @include respond-to('md'): padding 0 var(--space-8).

    .heroName class: font-family var(--font-heading), font-size var(--text-5xl) (mobile), font-weight 700, color var(--color-text), line-height 1.0, margin-bottom var(--space-4). Breakpoint @include respond-to('md'): font-size var(--text-hero) (80px desktop).

    .heroPosition class: font-family var(--font-body), font-size var(--text-lg), color var(--color-text-muted), margin-bottom var(--space-8).

    .heroCta class: display inline-flex, align-items center, padding var(--space-4) var(--space-8), background var(--color-accent), color var(--color-text), font-family var(--font-body), font-size var(--text-base), font-weight 700, border-radius 4px, text-decoration none, min-height 48px (WCAG touch target), transition background-color 150ms ease. Hover: background var(--color-accent-hover). Focus-visible: outline 2px solid var(--color-accent), outline-offset 2px.
  </action>
  <verify>
    <automated>cd /c/Users/dmytro.kukharuk/projects/stydy/future-legend-dev && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/sections/HeroSection.tsx starts with 'use client'
    - HeroSection.tsx contains gsap.registerPlugin at module scope (before the component function)
    - HeroSection.tsx contains two separate refs: heroRef (HTMLElement) and bgRef (HTMLDivElement)
    - HeroSection.tsx uses useReducedMotion() from framer-motion
    - GSAP useGSAP block targets bgRef.current (NOT heroRef, NOT text elements)
    - Framer Motion motion.h1, motion.p, motion.a are used for text layer (NOT bgRef)
    - NO element is animated by both GSAP and Framer Motion (animation ownership rule satisfied)
    - HeroSection.module.scss .hero contains `overflow: hidden`
    - HeroSection.module.scss .heroBg contains `height: 115%`
    - HeroSection.module.scss .heroCta contains `min-height: 48px`
    - motion.a has href="#contact" (anchor link, not button)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>HeroSection.tsx implements dual-system animation: GSAP yPercent:30 scrub on .heroBg layer, Framer Motion stagger (0.2/0.5/0.8s) on text layer. No element is touched by both libraries. Build is TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite page.tsx with 8-section assembly and update layout.tsx metadata</name>
  <files>app/[lang]/page.tsx, app/[lang]/layout.tsx</files>
  <read_first>
    - app/[lang]/page.tsx — current demo page body (must be fully replaced, keep no demo content)
    - app/[lang]/layout.tsx — current metadata (title: 'Design System — Future Legend Dev' must be updated)
    - .planning/phases/02-core-sections-animations/02-PATTERNS.md — "app/[lang]/page.tsx (route, transform)" section
    - .planning/phases/02-core-sections-animations/02-UI-SPEC.md — "Page Assembly Contract (D-21)" section
    - CLAUDE.md — content data flow rule (page.tsx is the ONLY file importing from content/)
  </read_first>
  <action>
    Rewrite app/[lang]/page.tsx completely. This is a Server Component — do NOT add 'use client'. Remove the entire design system demo (swatch arrays, typography specimens, ScrollFadeSection, footer). Remove the import of ScrollFadeSection.

    New imports: player from '@/content/player'; Nav from '@/components/layout/Nav'; HeroSection from '@/components/sections/HeroSection'; SectionStub from '@/components/sections/SectionStub'. Do NOT import AboutSection, TrophiesSection, ClubSection, or TeamSection yet — those are added in Plan 03 when those components exist.

    Export default function HomePage() returning JSX with Nav (outside main element, same position as current) and a main element containing: HeroSection with data={player}; SectionStub with id="highlights" title="Highlights"; SectionStub with id="gallery" title="Gallery"; SectionStub with id="contact" title="Contact". About, Trophies, Club, and Team sections will be added in Plan 03.

    Also delete or clear page.module.scss imports — the new page.tsx does not use it. If page.module.scss has no other consumers, the file can be left as-is (it will be unused, which is not an error). Do not delete it in this task to avoid any risk.

    Update app/[lang]/layout.tsx: find the metadata export constant and update the title to 'Dmytro Kovalenko — Football Player' and description to 'Scout profile for Dmytro Kovalenko, Central Midfielder. View stats, trophies, and club info.' Do not change any other part of layout.tsx (generateStaticParams, RootLayout function, font imports, providers).

    Run `npx next build` after these changes to verify the build exits 0. If build fails due to missing section imports (AboutSection etc.), that is expected and will be resolved in Plan 03 — but those should NOT be imported in this plan since the components don't exist yet.
  </action>
  <verify>
    <automated>cd /c/Users/dmytro.kukharuk/projects/stydy/future-legend-dev && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/[lang]/page.tsx does NOT contain 'use client'
    - app/[lang]/page.tsx imports player from '@/content/player' (the only file that imports from content/)
    - app/[lang]/page.tsx imports HeroSection and SectionStub
    - app/[lang]/page.tsx renders HeroSection with data={player}
    - app/[lang]/page.tsx renders SectionStub for 'highlights', 'gallery', 'contact'
    - app/[lang]/page.tsx does NOT import ScrollFadeSection
    - app/[lang]/page.tsx does NOT contain the design system demo swatch arrays or typography specimens
    - app/[lang]/layout.tsx metadata title equals 'Dmytro Kovalenko — Football Player'
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>page.tsx is the 8-section scaffold (Hero + 3 stubs wired; About/Trophies/Club/Team to be added in Plan 03). layout.tsx shows the player's name in the page title. Build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hero CTA anchor → #contact scroll | User-initiated navigation via Lenis smooth scroll |
| GSAP scroll → bgRef DOM element | Scroll position drives yPercent on background layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | player.fullName in aria-label | accept | Value is a TypeScript string constant from content/player.ts — not user-generated, no XSS vector; rendered as aria-label attribute not innerHTML |
| T-02-04 | Information Disclosure | HeroSection background image slot | accept | .heroBg background-image is currently null/empty — the gradient placeholder exposes no PII; real photo is a future content update per D-01 |
| T-02-05 | Denial of Service | GSAP ScrollTrigger on hero scroll | accept | GSAP ScrollTrigger is a well-tested library; scrub:1 is a standard pattern; no custom code path that could loop or hang |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — lucide-react installed in Plan 01. slopcheck verified for all packages in RESEARCH.md |
</threat_model>

<verification>
After all three tasks complete:
- `npx next build` exits 0 (critical cross-cutting constraint)
- HeroSection.tsx: grep confirms `gsap.registerPlugin` at module scope, `bgRef` and `heroRef` are separate refs, `overflow: hidden` in SCSS
- SectionStub.tsx: grep confirms no 'use client', no animation imports
- page.tsx: grep confirms `import { player } from '@/content/player'` present, no ScrollFadeSection import, no swatch arrays
- layout.tsx: grep confirms `title: 'Dmytro Kovalenko — Football Player'`
</verification>

<success_criteria>
- HeroSection renders a full-screen cinematic hero with GSAP parallax background and Framer Motion staggered text entrance — dual-system animation on separate DOM nodes (CLAUDE.md rule satisfied)
- SectionStub renders dark centered sections with title and crimson accent rule, no animation, no 'use client'
- page.tsx assembles Nav + HeroSection + 3 SectionStubs, imports player from content/, is a Server Component
- layout.tsx metadata updated with player name
- `npx next build` exits 0
</success_criteria>

<output>
Create `.planning/phases/02-core-sections-animations/02-02-SUMMARY.md` when done
</output>
