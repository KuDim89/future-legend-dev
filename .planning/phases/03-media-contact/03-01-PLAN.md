---
phase: 03-media-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - content/videos.ts
  - content/gallery.ts
  - public/images/gallery/photo-01.webp
  - public/images/gallery/photo-02.webp
  - public/images/gallery/photo-03.webp
  - public/images/gallery/photo-04.webp
  - public/images/gallery/photo-05.webp
  - public/images/gallery/photo-06.webp
autonomous: true
requirements: [MEDIA-01, MEDIA-02]
user_setup: []

must_haves:
  truths:
    - "content/videos.ts exports a VideoEntry interface and 3 video entries with real public YouTube IDs"
    - "content/gallery.ts exports a GalleryEntry interface and 6 gallery entries with basePath-prefixed src paths"
    - "react-youtube, react-masonry-css, and yet-another-react-lightbox are installed and resolvable"
    - "6 placeholder WebP images exist in public/images/gallery/"
  artifacts:
    - path: "content/videos.ts"
      provides: "VideoEntry interface + 3 placeholder video entries"
      exports: ["VideoEntry", "videos"]
      contains: "export interface VideoEntry"
    - path: "content/gallery.ts"
      provides: "GalleryEntry interface + 6 placeholder gallery entries"
      exports: ["GalleryEntry", "gallery"]
      contains: "export interface GalleryEntry"
    - path: "package.json"
      provides: "three new media dependencies"
      contains: "react-youtube"
  key_links:
    - from: "content/gallery.ts"
      to: "/future-legend-dev/images/gallery/"
      via: "src path prefix"
      pattern: "/future-legend-dev/images/gallery/photo-0"
---

<objective>
Establish the Phase 3 data and dependency foundation: install the three media npm packages, create the two content data files (`content/videos.ts`, `content/gallery.ts`), and commit 6 placeholder gallery WebP images. Every downstream section plan (Highlights, Gallery, Contact-assembly) depends on these typed exports and installed packages.

Purpose: All three Phase 3 sections consume `VideoEntry[]` / `GalleryEntry[]` props sourced from these files, and the section components import `react-youtube`, `react-masonry-css`, and `yet-another-react-lightbox`. Without this foundation, no section can compile.
Output: `content/videos.ts`, `content/gallery.ts`, 6 WebP files in `public/images/gallery/`, three packages added to `package.json`/`package-lock.json`.
</objective>

<execution_context>
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-media-contact/03-CONTEXT.md
@.planning/phases/03-media-contact/03-RESEARCH.md
@.planning/phases/03-media-contact/03-PATTERNS.md

<interfaces>
<!-- Established content data file convention. Extracted from content/player.ts via 03-PATTERNS.md. -->
<!-- Executor follows this shape exactly — named exports only, interface first then const, no default export, no runtime imports. -->

VideoEntry data schema (CONTEXT.md D-08):
- videoId: string   (YouTube video ID)
- title: string     (card label)
- category: 'match' | 'training' | 'skills'

GalleryEntry data schema (CONTEXT.md D-13):
- src: string       (full path INCLUDING basePath: /future-legend-dev/images/gallery/{filename})
- alt: string
- category: 'match' | 'training' | 'official'

content/player.ts convention (the analog): named exports only, `export interface X { ... }` then `export const data: X[] = [ ... ]`. Pure TypeScript data — zero runtime imports. Imported ONLY in app/[lang]/page.tsx, never inside section components (CLAUDE.md data flow rule).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the three Phase 3 media packages</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (confirm current dependencies; react, framer-motion, gsap, lucide-react, sass already present per 03-RESEARCH.md)
    - .planning/phases/03-media-contact/03-RESEARCH.md (Package Legitimacy Audit table — all three packages slopcheck [OK], approved; verified versions: react-youtube 10.1.0, react-masonry-css 1.0.16, yet-another-react-lightbox 3.32.0)
  </read_first>
  <action>
    Run `npm install react-youtube react-masonry-css yet-another-react-lightbox` from the project root. All three are pre-approved in the 03-RESEARCH.md Package Legitimacy Audit (slopcheck [OK], no postinstall scripts, source repos verified). Do NOT pin or alter versions beyond what npm resolves. After install, confirm `node_modules/react-youtube/dist/index.d.ts` exports `YouTubeProps` as a named type (research assumption A4) — if the export name differs, note it in the SUMMARY so downstream plan 03-02 adjusts the import. Note in the SUMMARY that `react-masonry-css` React 19 compatibility is unverified (research assumption A1, last published 2022) — downstream plan 03-03 carries the CSS-columns fallback if it errors at runtime.
  </action>
  <verify>
    <automated>node -e "require.resolve('react-youtube'); require.resolve('react-masonry-css'); require.resolve('yet-another-react-lightbox'); console.log('all three resolved')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies block contains `react-youtube`, `react-masonry-css`, and `yet-another-react-lightbox`
    - `node -e "require.resolve('react-youtube')"` exits 0
    - `node -e "require.resolve('react-masonry-css')"` exits 0
    - `node -e "require.resolve('yet-another-react-lightbox')"` exits 0
    - SUMMARY records the resolved version of each package and the `YouTubeProps` export confirmation
  </acceptance_criteria>
  <done>All three packages installed and resolvable; package.json + package-lock.json updated.</done>
</task>

<task type="auto">
  <name>Task 2: Create content/videos.ts with 3 placeholder video entries</name>
  <files>content/videos.ts</files>
  <read_first>
    - content/player.ts (the exact content-file convention to mirror: named exports only, interface first then const, no default export, no runtime imports)
    - .planning/phases/03-media-contact/03-CONTEXT.md (D-08 schema, D-09 real public IDs, D-10 flat grid)
    - .planning/phases/03-media-contact/03-RESEARCH.md (Pattern 5; Assumption A5 — IDs must be valid, public, embeddable YouTube football highlight videos)
  </read_first>
  <action>
    Create `content/videos.ts` exporting `interface VideoEntry { videoId: string; title: string; category: 'match' | 'training' | 'skills' }` and `const videos: VideoEntry[]` with exactly 3 entries (per D-09), one per category. Use 3 REAL, public, embeddable YouTube football highlight video IDs from major channels (e.g. official UEFA, FIFA, or a major club channel) — NOT placeholder/lorem IDs and NOT three copies of one ID. Each `videoId` must be the 11-character YouTube ID only (not a full URL). Titles per UI-SPEC copywriting and D-08: e.g. "Match Highlights — FC Dynamo" (category 'match'), "Training Session — Dribbling Drills" (category 'training'), "Skills Compilation" (category 'skills'). Named exports only, no default export, no runtime imports — identical convention to content/player.ts. The category field is stored for a future filter UI (D-10) but no filter is built in Phase 3.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `content/videos.ts` contains `export interface VideoEntry`
    - `content/videos.ts` contains `export const videos: VideoEntry[]`
    - File declares exactly 3 entries, each with a distinct 11-character `videoId`, a `title`, and a `category` of `match`, `training`, or `skills`
    - File has no `import` statements and no `export default`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>content/videos.ts compiles with 3 real-ID video entries following the player.ts convention.</done>
</task>

<task type="auto">
  <name>Task 3: Create content/gallery.ts and 6 placeholder WebP gallery images</name>
  <files>content/gallery.ts, public/images/gallery/photo-01.webp, public/images/gallery/photo-02.webp, public/images/gallery/photo-03.webp, public/images/gallery/photo-04.webp, public/images/gallery/photo-05.webp, public/images/gallery/photo-06.webp</files>
  <read_first>
    - content/player.ts (content-file convention to mirror)
    - .planning/phases/03-media-contact/03-CONTEXT.md (D-12 local WebP storage, D-13 schema with basePath prefix, D-14 6 placeholder images 3:2 landscape)
    - .planning/phases/03-media-contact/03-RESEARCH.md (Open Question 2 — placeholder WebP generation method is not architecturally significant)
  </read_first>
  <action>
    First create the 6 placeholder WebP files in `public/images/gallery/` named `photo-01.webp` through `photo-06.webp`. Generate them as solid-color or gradient WebP images at 3:2 landscape ratio (600x400px), small file size, 2 per category. Use whatever tool is available (ImageMagick `convert`/`magick`, an `sharp`-based Node one-liner since `sharp` ships with Next, or `cwebp`). Verify each file is a valid WebP (magic bytes `RIFF....WEBP`). Then create `content/gallery.ts` exporting `interface GalleryEntry { src: string; alt: string; category: 'match' | 'training' | 'official' }` and `const gallery: GalleryEntry[]` with exactly 6 entries. CRITICAL (D-13): each `src` MUST be the full path INCLUDING the basePath prefix `/future-legend-dev/images/gallery/photo-0N.webp` (the basePath is `/future-legend-dev` from next.config.ts). Each `alt` is required and non-empty (descriptive, e.g. "Match action — Dmytro driving forward"). Distribute 2 entries per category (match, training, official). Named exports only, no default export, no runtime imports.
  </action>
  <verify>
    <automated>node -e "const {gallery}=require('@swc/register')?{}:{};" 2>NUL & npx tsc --noEmit -p tsconfig.json && node -e "const fs=require('fs'); for(let i=1;i<=6;i++){const p='public/images/gallery/photo-0'+i+'.webp'; const b=fs.readFileSync(p); if(b.slice(0,4).toString()!=='RIFF'||b.slice(8,12).toString()!=='WEBP'){throw new Error('not webp: '+p)};} console.log('6 valid webp files')"</automated>
  </verify>
  <acceptance_criteria>
    - 6 files exist: `public/images/gallery/photo-01.webp` through `photo-06.webp`, each with valid WebP magic bytes (`RIFF` at offset 0, `WEBP` at offset 8)
    - `content/gallery.ts` contains `export interface GalleryEntry`
    - `content/gallery.ts` contains `export const gallery: GalleryEntry[]`
    - All 6 `src` values match the pattern `/future-legend-dev/images/gallery/photo-0N.webp`
    - Every entry has a non-empty `alt` string
    - File has no `import` statements and no `export default`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>content/gallery.ts compiles with 6 basePath-prefixed entries; 6 valid WebP placeholders committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Third-party package code enters the build/runtime supply chain |
| Static asset paths → GitHub Pages | Gallery `src` paths must resolve under basePath or images 404 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | npm install of react-youtube, react-masonry-css, yet-another-react-lightbox | mitigate | 03-RESEARCH.md Package Legitimacy Audit ran slopcheck 0.6.1 → [OK] for all three; source repos verified; no postinstall scripts. Packages are pre-approved — no [ASSUMED]/[SUS] entries, so no blocking human checkpoint required. |
| T-03-02 | Information Disclosure | content/videos.ts, content/gallery.ts | accept | Both files contain only public, non-sensitive placeholder data (public YouTube IDs, public asset paths). No secrets, no PII. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck [OK] for all three packages (03-RESEARCH.md). No [ASSUMED]/[SUS] packages present → legitimacy checkpoint not triggered. |
</threat_model>

<verification>
- `npm install` completes; all three packages resolvable via `require.resolve`.
- `npx tsc --noEmit` exits 0 with both new content files present.
- 6 WebP files present and valid in `public/images/gallery/`.
- No section component imports these content files (they are imported only in page.tsx — enforced in plan 03-04).
</verification>

<success_criteria>
- `content/videos.ts` exports `VideoEntry` + `videos` (3 real-ID entries).
- `content/gallery.ts` exports `GalleryEntry` + `gallery` (6 basePath-prefixed entries).
- `react-youtube`, `react-masonry-css`, `yet-another-react-lightbox` installed.
- 6 valid placeholder WebP gallery images committed.
- TypeScript compiles clean.
</success_criteria>

<output>
Create `.planning/phases/03-media-contact/03-01-SUMMARY.md` when done. Record resolved package versions, the `YouTubeProps` export confirmation, and the WebP generation method used.
</output>
