---
phase: 03-media-contact
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - components/sections/ContactSection.tsx
  - components/sections/ContactSection.module.scss
  - .github/workflows/contact.yml
  - .github/workflows/deploy.yml
  - app/[lang]/page.tsx
autonomous: false
requirements: [CONTACT-01, CONTACT-02, CONTACT-03, CONTACT-04]
user_setup:
  - service: github-pat
    why: "Client bundle needs a PAT to POST workflow_dispatch from the browser (D-02)"
    env_vars:
      - name: GH_PAT
        source: "GitHub -> Settings -> Developer settings -> Fine-grained PAT, scoped to Actions:write on KuDim89/future-legend-dev only"
  - service: telegram
    why: "contact.yml sends the inquiry to the player's Telegram (D-05)"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "Telegram BotFather -> create bot -> copy token; add to repo GitHub Secrets"
      - name: TELEGRAM_CHAT_ID
        source: "Player's Telegram chat ID (e.g. via @userinfobot); add to repo GitHub Secrets"
    dashboard_config:
      - task: "Add GH_PAT, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID as repository secrets"
        location: "github.com/KuDim89/future-legend-dev -> Settings -> Secrets and variables -> Actions"

must_haves:
  truths:
    - "Visitor can submit a contact form with name, email, phone (optional), and message"
    - "Successful submission triggers the contact.yml workflow which sends a Telegram message"
    - "Honeypot fill OR submit under 3 seconds silently drops the submission"
    - "TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID appear only in contact.yml as ${{ secrets.* }} - never in any client file"
    - "The home page renders Highlights, Gallery, and Contact sections (no SectionStub for these three)"
  artifacts:
    - path: "components/sections/ContactSection.tsx"
      provides: "Contact form state machine + spam guards + GitHub workflow_dispatch fetch"
      contains: "contact.yml/dispatches"
    - path: ".github/workflows/contact.yml"
      provides: "workflow_dispatch -> Telegram sendMessage"
      contains: "workflow_dispatch"
    - path: "app/[lang]/page.tsx"
      provides: "Final 8-section assembly with 3 real Phase 3 sections"
      contains: "HighlightsSection"
  key_links:
    - from: "components/sections/ContactSection.tsx"
      to: "api.github.com/repos/KuDim89/future-legend-dev/actions/workflows/contact.yml/dispatches"
      via: "fetch POST with Bearer NEXT_PUBLIC_GH_PAT"
      pattern: "workflows/contact.yml/dispatches"
    - from: ".github/workflows/contact.yml"
      to: "api.telegram.org sendMessage"
      via: "curl with secrets.TELEGRAM_BOT_TOKEN"
      pattern: "api.telegram.org"
    - from: ".github/workflows/deploy.yml"
      to: "NEXT_PUBLIC_GH_PAT build env"
      via: "env block on Build step"
      pattern: "NEXT_PUBLIC_GH_PAT"
    - from: "app/[lang]/page.tsx"
      to: "HighlightsSection / GallerySection / ContactSection"
      via: "imports + render"
      pattern: "ContactSection"
---

<objective>
Build the contact vertical slice and complete the phase: a contact form whose successful submission triggers a GitHub Actions workflow that sends a Telegram message, with honeypot + time-check spam protection and the Telegram secrets confined to GitHub Secrets. Then wire all three Phase 3 sections into page.tsx (replacing the last three SectionStubs) and verify the full build.

Purpose: Closes CONTACT-01..04 and finishes the Phase 3 goal - a scout can submit an inquiry that reaches the player via Telegram, with no bot token exposed client-side. Page assembly is consolidated here (depends on 03-02 + 03-03) so the section-building plans avoid page.tsx file contention.
Output: `components/sections/ContactSection.tsx` (+ `.module.scss`), `.github/workflows/contact.yml`, modified `.github/workflows/deploy.yml`, modified `app/[lang]/page.tsx`.
</objective>

<execution_context>
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-media-contact/03-CONTEXT.md
@.planning/phases/03-media-contact/03-RESEARCH.md
@.planning/phases/03-media-contact/03-PATTERNS.md
@.planning/phases/03-media-contact/03-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Section components come from 03-02 and 03-03. -->

GitHub workflow_dispatch endpoint (CONTEXT.md D-01/D-03):
  POST https://api.github.com/repos/KuDim89/future-legend-dev/actions/workflows/contact.yml/dispatches
  headers: Authorization: Bearer ${process.env.NEXT_PUBLIC_GH_PAT}, Accept: application/vnd.github+json,
           Content-Type: application/json, X-GitHub-Api-Version: 2022-11-28
  body: { ref: 'main', inputs: { name, email, phone, message } }
  RESPONSE: 204 No Content by default (NOT 202). Success check MUST be `response.ok`, never `=== 202`.

From components/sections/HighlightsSection.tsx (03-02): export function HighlightsSection({ videos }: { videos: VideoEntry[] })
From components/sections/GallerySection.tsx (03-03): export function GallerySection({ photos }: { photos: GalleryEntry[] })
ContactSection takes NO props (D-20; URL hardcoded per D-03).

Existing app/[lang]/page.tsx (Server Component - must stay a Server Component, no 'use client'):
  imports: player, Nav, HeroSection, AboutSection, TrophiesSection, ClubSection, TeamSection, SectionStub
  renders: HeroSection data={player} / AboutSection data={player} / TrophiesSection trophies={player.trophies}
           / ClubSection club={player.club} / TeamSection team={player.team}
           / SectionStub id=highlights / SectionStub id=gallery / SectionStub id=contact

Existing .github/workflows/deploy.yml Build step:
      - name: Build
        run: npx next build

ContactSection structure (03-PATTERNS.md lines 567-691): 'use client'; FormState = 'idle'|'loading'|'success'|'error';
mountTime = useRef(Date.now()); honeypot state; formData {name,email,phone,message}; AnimatePresence form<->success swap.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build ContactSection form, state machine, and spam guards</name>
  <files>components/sections/ContactSection.tsx, components/sections/ContactSection.module.scss</files>
  <read_first>
    - components/sections/AboutSection.tsx (dual-animation analog: useScrollReveal + component-level animation; gsap.registerPlugin at module scope is NOT needed here since useScrollReveal handles it)
    - components/sections/AboutSection.module.scss (label typography pattern - bioField: var(--text-sm), uppercase, letter-spacing 0.08em, var(--color-text-muted))
    - .planning/phases/03-media-contact/03-PATTERNS.md (full ContactSection.tsx structure lines 567-691; ContactSection.module.scss lines 713-842)
    - .planning/phases/03-media-contact/03-RESEARCH.md (Pattern 3 fetch call + spam guards; Pitfall 2 - response.ok NOT === 202; Open Question 3 - textarea maxLength 1000)
    - .planning/phases/03-media-contact/03-UI-SPEC.md (ContactSection structure, input/honeypot/submit-button CSS, form states table, copywriting contract: all labels/placeholders/messages, fallback email dimakyh@ukr.net)
    - .planning/phases/03-media-contact/03-CONTEXT.md (D-06 spam, D-16 fields, D-17 success, D-18 error, D-19 loading, D-20 layout)
    - styles/_tokens.scss (confirm tokens: --color-bg-elevated, --color-border, --color-accent, --color-accent-hover, --color-text, --color-text-muted, --font-heading, --font-body, --text-base, --text-sm, --space-2, --space-4, --space-6, --space-8, --space-12)
  </read_first>
  <action>
    Create `components/sections/ContactSection.tsx` as `'use client'`. Imports: `useState, useRef` from react; `motion, AnimatePresence` from framer-motion; `Send, Loader2` from lucide-react; `useScrollReveal` from `@/lib/animations/useScrollReveal`; `styles` from `./ContactSection.module.scss`. Define `type FormState = 'idle' | 'loading' | 'success' | 'error'`. `export function ContactSection()` - NO props.

    State: `const containerRef = useRef<HTMLElement>(null); const mountTime = useRef<number>(Date.now()); useScrollReveal(containerRef);` then `const [formState, setFormState] = useState<FormState>('idle');`, `const [honeypot, setHoneypot] = useState('');`, `const [formData, setFormData] = useState({ name: '', email: '', phone: '', message: '' });`.

    `async function handleSubmit(e: React.FormEvent<HTMLFormElement>)`: `e.preventDefault();` then spam guard 1 `if (honeypot !== '') return;` (D-06 honeypot - silently drop) then spam guard 2 `if (Date.now() - mountTime.current < 3000) return;` (D-06 3-second time check - silently drop). Then `setFormState('loading');` and a try/catch that fetches POST to `https://api.github.com/repos/KuDim89/future-legend-dev/actions/workflows/contact.yml/dispatches` with headers Authorization `Bearer ${process.env.NEXT_PUBLIC_GH_PAT}`, Accept `application/vnd.github+json`, Content-Type `application/json`, X-GitHub-Api-Version `2022-11-28`, and body `JSON.stringify({ ref: 'main', inputs: { name: formData.name, email: formData.email, phone: formData.phone || '', message: formData.message } })`. CRITICAL (Pitfall 2): success check is `setFormState(response.ok ? 'success' : 'error');` - NEVER `response.status === 202`. The catch sets `'error'`.

    Render `<section id="contact" ref={containerRef} className={styles.section}>` with an h2 `Contact` (reveal-item) and an intro p `Get in touch - whether you are a scout, coach, or club representative.` (reveal-item), then `<AnimatePresence mode="wait">` branching:
    - `formState === 'success'`: `motion.div key="success" role="status"` (initial opacity 0 y 10, animate opacity 1 y 0, transition duration 0.4 easeOut) with `<p className={styles.successHeading}>Message Sent</p>` and a paragraph `Your message has been sent. We'll be in touch soon.`
    - else: `motion.form key="form" className={styles.form + ' reveal-item'} onSubmit={handleSubmit} aria-busy={formState === 'loading'}` (exit opacity 0 y -10, transition duration 0.3 easeOut). Contents:
      - Honeypot: `<div className={styles.honeypot} aria-hidden="true">` containing `<label htmlFor="website">Website</label>` and `<input id="website" name="website" type="text" tabIndex={-1} autoComplete="off" value={honeypot} onChange={(e) => setHoneypot(e.target.value)} />` (hidden via CSS, NOT display:none / NOT type=hidden - D-06).
      - Visible fields, each in a `<div className={styles.field}>` with a `<label className={styles.label}>` + a controlled input bound to formData/setFormData and `disabled={formState === 'loading'}`:
        - name: label `Name`, input type text name="name" required placeholder `Your full name`, className styles.input
        - email: label `Email`, input type email name="email" required placeholder `your@email.com`, className styles.input
        - phone: label `Phone (optional)`, input type tel name="phone" placeholder `+380 XX XXX XXXX`, className styles.input (NOT required - D-16)
        - message: label `Message`, textarea name="message" required maxLength={1000} placeholder `Tell us what you have in mind...`, className styles.textarea (maxLength 1000 keeps within Telegram 4096 limit - Open Question 3)
      - Submit: `<button type="submit" className={styles.submitBtn} disabled={formState === 'loading'}>` rendering `formState === 'loading'` -> `<Loader2 size={18} className={styles.spinner} aria-label="Sending..." /> Sending...` else `<Send size={18} /> Send Message`.
      - Error (only when `formState === 'error'`): `<motion.p role="alert" className={styles.errorMsg} initial={{ opacity: 0, y: 5 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.25 }}>` with text `Something went wrong. Please try again or email us directly at ` + `<a href="mailto:dimakyh@ukr.net">dimakyh@ukr.net</a>.`

    Create `components/sections/ContactSection.module.scss` with the standard header comment (`// ContactSection.module.scss` / `// Mixins auto-injected via sassOptions.additionalData - no @use needed` / `// Tokens: use var(--token) - NOT $scss-variable`). Copy `.section` + `.sectionTitle` (with `::after` accent underbar) from the established TrophiesSection/AboutSection pattern. Add per 03-PATTERNS.md / UI-SPEC: `.intro` (var(--text-base), color var(--color-text-muted), line-height 1.5, margin-bottom var(--space-12)); `.form` (max-width 640px, margin 0 auto, `position: relative` - required for honeypot absolute positioning); `.field` (flex column, gap var(--space-2), margin-bottom var(--space-6)); `.label` (var(--font-body), var(--text-sm), color var(--color-text-muted), text-transform uppercase, letter-spacing 0.08em); `.input` and `.textarea` (var(--font-body), var(--text-base), color var(--color-text), background var(--color-bg-elevated), border 1px solid var(--color-border), border-radius 4px, padding var(--space-4), width 100%, `transition: border-color 0.15s ease` - CSS owns this, `&:focus` outline 2px solid var(--color-accent) + outline-offset 2px + border-color var(--color-accent), placeholder color var(--color-text-muted)); `.textarea` (min-height 140px, resize vertical); `.honeypot` (`position: absolute; left: -9999px; top: auto; width: 1px; height: 1px; overflow: hidden;` - NOT display:none, D-06); `.submitBtn` (flex, align-items center, gap var(--space-2), var(--font-heading), var(--text-base), weight 700, text-transform uppercase, color #ffffff, background var(--color-accent), no border, border-radius 4px, padding var(--space-4) var(--space-8), width 100%, cursor pointer, `transition: background 0.15s ease`, `&:hover:not(:disabled) { background: var(--color-accent-hover) }`, `&:focus-visible` outline, `&:disabled { opacity: 0.7; cursor: not-allowed }`); `.spinner` (`animation: spin 1s linear infinite`) + a `@keyframes spin` from 0deg to 360deg (CSS owns the spinner, NOT Framer Motion); `.errorMsg` (var(--text-sm), color var(--color-text-muted), margin-top var(--space-4), nested `a { color: var(--color-accent); text-decoration: underline }`); `.successMessage` (max-width 640px, margin 0 auto, text-align center, padding var(--space-12) 0); `.successHeading` (var(--font-heading), var(--text-base), weight 700, uppercase, color var(--color-text), margin-bottom var(--space-4)).

    Animation ownership: GSAP owns scroll reveal (section title, intro, form via `.reveal-item`); Framer Motion owns the form<->success AnimatePresence swap + error mount; CSS owns the spinner loop and focus/hover transitions. No element animated by both GSAP and Framer Motion.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/ContactSection.tsx` first line is `'use client';`
    - Contains the endpoint substring `workflows/contact.yml/dispatches`
    - Success branch uses `response.ok` and the file does NOT contain `=== 202` or `status === 202`
    - Contains spam guard `if (honeypot !== '') return` and `Date.now() - mountTime.current < 3000`
    - Contains an Authorization header with `Bearer ${process.env.NEXT_PUBLIC_GH_PAT}` and `X-GitHub-Api-Version` value `2022-11-28`
    - Phone input is `type="tel"` and does NOT carry `required`; name/email inputs and the message textarea DO carry `required`
    - textarea contains `maxLength={1000}`
    - Honeypot input has `tabIndex={-1}` and `name="website"`; `.honeypot` SCSS uses `position: absolute` with `left: -9999px` and does NOT use `display: none`
    - `ContactSection.module.scss` `.spinner` uses `animation: spin` and `.submitBtn`/`.input` use `transition:` (CSS-owned, not Framer Motion)
    - File contains the error fallback `mailto:dimakyh@ukr.net`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ContactSection compiles with form state machine, both spam guards, response.ok success check, and full UI-SPEC styling.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create contact.yml workflow and inject NEXT_PUBLIC_GH_PAT into deploy.yml</name>
  <files>.github/workflows/contact.yml, .github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (existing structure - the Build step `run: npx next build` is where the env block is added; secrets accessed via ${{ secrets.* }})
    - .planning/phases/03-media-contact/03-RESEARCH.md (Pattern 4 contact.yml full file; deploy.yml env modification; Telegram sendMessage URL format; Assumption A3 - Markdown parse_mode escaping risk)
    - .planning/phases/03-media-contact/03-PATTERNS.md (contact.yml structure lines 989-1032; deploy.yml modification lines 1036-1042)
    - .planning/phases/03-media-contact/03-CONTEXT.md (D-02 NEXT_PUBLIC_GH_PAT, D-04 four inputs name/email/message/phone with phone optional, D-05 Telegram secrets manual, CONTACT-04 secrets only in workflow)
  </read_first>
  <action>
    Create `.github/workflows/contact.yml` (D-04). `name: Contact Form Notification`. Trigger `on: workflow_dispatch:` with `inputs:` for `name` (description 'Contact name', required: true, type: string), `email` (required: true, type: string), `phone` (description 'Contact phone (optional)', required: false, type: string, default: ''), `message` (required: true, type: string). Single job `notify` on `runs-on: ubuntu-latest` with one step named "Send Telegram notification" whose `run:` builds a `MESSAGE` shell variable from `${{ github.event.inputs.* }}` (name, email, phone with a `|| 'not provided'` fallback, a `----` separator line, then message - newlines encoded as `%0A`) and then runs `curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" -d "text=${MESSAGE}" -d "parse_mode=Markdown"`. Add an `env:` block on the step exposing `TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}` and `TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}`.

    CONTACT-04 CRITICAL: `TELEGRAM_BOT_TOKEN` and `TELEGRAM_CHAT_ID` appear ONLY as `${{ secrets.* }}` references in this workflow file - never as literal values, never in any client file. Record in the SUMMARY the Assumption A3 risk: if a user message contains Markdown special chars (* _ backtick), `parse_mode=Markdown` could break formatting or be rejected; the documented safe fallback is to omit `parse_mode` (plain text). Keep Markdown per Claude's-discretion but note the tradeoff.

    Modify `.github/workflows/deploy.yml` (D-02): add an `env:` block to the existing Build step so it becomes `- name: Build` / `run: npx next build` / `env:` / `  NEXT_PUBLIC_GH_PAT: ${{ secrets.GH_PAT }}`. This bakes the PAT into the static JS bundle at deploy time (NEXT_PUBLIC_* is inlined at build time). Do NOT alter any other step in deploy.yml.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.github/workflows/contact.yml','utf8'); const d=fs.readFileSync('.github/workflows/deploy.yml','utf8'); if(!/workflow_dispatch/.test(c)) throw new Error('no workflow_dispatch'); if(!/secrets\.TELEGRAM_BOT_TOKEN/.test(c)) throw new Error('no telegram token ref'); if(!/api\.telegram\.org/.test(c)) throw new Error('no telegram api'); if(!/NEXT_PUBLIC_GH_PAT:\s*\$\{\{\s*secrets\.GH_PAT\s*\}\}/.test(d)) throw new Error('deploy.yml missing NEXT_PUBLIC_GH_PAT env'); console.log('workflows ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/contact.yml` contains `workflow_dispatch:` with four inputs: `name`, `email`, `phone` (required: false), `message`
    - contact.yml references `${{ secrets.TELEGRAM_BOT_TOKEN }}` and `${{ secrets.TELEGRAM_CHAT_ID }}` and contains the literal URL host `api.telegram.org`
    - contact.yml contains NO literal token or chat-id value (only `${{ secrets.* }}` references) - CONTACT-04
    - `.github/workflows/deploy.yml` Build step has an `env:` block with `NEXT_PUBLIC_GH_PAT: ${{ secrets.GH_PAT }}`
    - No other deploy.yml step is modified (Checkout, Setup Node, Cache, Install, Build run command, Upload artifact, deploy job unchanged)
    - The verify node script exits 0
  </acceptance_criteria>
  <done>contact.yml triggers a Telegram notification on workflow_dispatch with secrets confined to GitHub Secrets; deploy.yml bakes NEXT_PUBLIC_GH_PAT at build time.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire all 3 Phase 3 sections into page.tsx and run the final build</name>
  <files>app/[lang]/page.tsx</files>
  <read_first>
    - app/[lang]/page.tsx (current state - imports + 3 SectionStub calls to replace; must remain a Server Component, no 'use client')
    - content/videos.ts (the `videos` named export from 03-01)
    - content/gallery.ts (the `gallery` named export from 03-01)
    - components/sections/HighlightsSection.tsx (the `HighlightsSection` named export + `videos` prop name, from 03-02)
    - components/sections/GallerySection.tsx (the `GallerySection` named export + `photos` prop name, from 03-03)
    - components/sections/ContactSection.tsx (the `ContactSection` named export, no props, from Task 1)
    - .planning/phases/03-media-contact/03-PATTERNS.md (page.tsx target state lines 940-954)
  </read_first>
  <action>
    Modify `app/[lang]/page.tsx`. Remove the `SectionStub` import (it is no longer used after this change - confirm no other SectionStub usage remains in the file). Add imports: `import { videos } from '@/content/videos';`, `import { gallery } from '@/content/gallery';`, `import { HighlightsSection } from '@/components/sections/HighlightsSection';`, `import { GallerySection } from '@/components/sections/GallerySection';`, `import { ContactSection } from '@/components/sections/ContactSection';`. In the JSX, replace the three `<SectionStub id="highlights" .../>`, `<SectionStub id="gallery" .../>`, `<SectionStub id="contact" .../>` calls with `<HighlightsSection videos={videos} />`, `<GallerySection photos={gallery} />`, `<ContactSection />` in that order (keeps the D-21 nav anchor order: ...team, highlights, gallery, contact). The file MUST remain a Server Component (no `'use client'`) - it is the sole importer of content/* per the CLAUDE.md data flow rule; the section components themselves are `'use client'`. Do not change the Hero/About/Trophies/Club/Team rendering.

    After the edit, run `npx next build` and confirm it exits 0 with `/ua` and `/en` pre-rendered. Then inspect the build output to confirm no YouTube iframe ships in the initial HTML: grep the generated `out/` HTML for `youtube.com/embed` / `<iframe` - there should be zero YouTube iframes in the static HTML (lite-embed / MEDIA-03). Record the grep result in the SUMMARY.
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `app/[lang]/page.tsx` imports `videos` from `@/content/videos` and `gallery` from `@/content/gallery`
    - Renders `<HighlightsSection videos={videos} />`, `<GallerySection photos={gallery} />`, and `<ContactSection />`
    - Does NOT import or render `SectionStub` for highlights/gallery/contact (no `SectionStub` import remains if it is otherwise unused)
    - File does NOT contain `'use client'`
    - `npx next build` exits 0 and pre-renders `/ua` and `/en`
    - The generated static HTML in `out/` contains zero `youtube.com/embed` iframes (SUMMARY records the grep count)
  </acceptance_criteria>
  <done>page.tsx renders the 3 real Phase 3 sections (no stubs); `npx next build` exits 0; no YouTube iframe in initial static HTML.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of the full Phase 3 media + contact experience</name>
  <files>app/[lang]/page.tsx, components/sections/ContactSection.tsx, components/sections/HighlightsSection.tsx, components/sections/GallerySection.tsx</files>
  <action>
    Pause for the user (blocking checkpoint, not auto-approvable - includes a CONTACT-04 secret-leak check). Present the what-built summary and the numbered how-to-verify steps below, then wait for the resume signal before marking the phase complete. All implementation (Tasks 1-3) must be done and the build green before this checkpoint is shown.
  </action>
  <what-built>
    All three Phase 3 sections are live on the home page: HighlightsSection (lite-embed YouTube grid), GallerySection (masonry + lightbox), and ContactSection (form -> GitHub workflow_dispatch -> Telegram). The build passes and ships no YouTube iframe in the initial HTML.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` (or `npx next dev`) and open the local site, then scroll to the Highlights section.
    2. Confirm each video card shows a thumbnail (no video playing) on load. Open DevTools -> Network, reload, and confirm NO request to `youtube.com/iframe_api` or any youtube iframe before you click. Click one thumbnail -> the clip should load and autoplay in place.
    3. Scroll to the Gallery section. Confirm photos lay out in masonry columns (resize the window: 3 cols wide, 2 at ~768px, 1 at ~480px). Click a photo -> a styled fullscreen lightbox opens. Test arrow keys (prev/next), the zoom control, and Escape to close.
    4. Scroll to the Contact section. Fill name/email/message, wait at least 3 seconds after page load, and submit. With GitHub Secrets configured (GH_PAT, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID), confirm a Telegram message arrives and the form swaps to the "Message Sent" success state. If secrets are not yet added, confirm the form shows the inline error with the dimakyh@ukr.net fallback (expected until secrets exist).
    5. Open DevTools -> Sources/Network and search the client bundle for `TELEGRAM` - confirm neither the bot token nor chat ID appears anywhere client-side (CONTACT-04). The PAT (NEXT_PUBLIC_GH_PAT) being present is the accepted D-02 tradeoff.
    6. Toggle light/dark theme and confirm all three sections render correctly in both themes.
  </how-to-verify>
  <verify>
    <human-check>User confirms all 6 verification steps pass (or reports issues).</human-check>
  </verify>
  <resume-signal>Type "approved" or describe any issues (e.g. iframe loading early, lightbox unstyled, masonry single-column, secret leaked).</resume-signal>
  <done>User has run all 6 verification steps and typed "approved"; no client-side Telegram secret leak observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form -> GitHub API | Untrusted user input crosses into a workflow_dispatch call authenticated by a bundle-exposed PAT |
| GitHub Actions runner -> Telegram API | Server-side curl carries the bot token; never touches the browser |
| Client bundle -> public internet | NEXT_PUBLIC_GH_PAT is inlined into shipped JS (intentional, D-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Spoofing / DoS | workflow_dispatch endpoint via exposed NEXT_PUBLIC_GH_PAT | accept | D-02 accepted tradeoff for a backend-less static site. PAT scoped to Actions:write only (cannot read repo, push code, or read secrets). Honeypot + 3s time check reduce automated spam. Worst case: spam workflow runs; monitor Actions usage. |
| T-03-09 | Information Disclosure | TELEGRAM_BOT_TOKEN / TELEGRAM_CHAT_ID | mitigate | Secrets live only in GitHub Secrets; contact.yml reads them via `${{ secrets.* }}`; they never appear in content/, components/, app/, or any client bundle. Acceptance criterion + human checkpoint step 5 verify no `TELEGRAM` string in the client bundle (CONTACT-04). |
| T-03-10 | Tampering (XSS via form input) | ContactSection rendering of name/email/phone/message | mitigate | React renders all form state as text nodes; no `dangerouslySetInnerHTML` anywhere in ContactSection. User input only travels into a JSON fetch body, never into innerHTML. |
| T-03-11 | Tampering (Telegram Markdown injection) | contact.yml MESSAGE interpolation with parse_mode=Markdown | accept | Assumption A3: special Markdown chars in user input may break message formatting. Low impact (player still receives the message text). Documented safe fallback: drop parse_mode for plain text. Recorded in SUMMARY. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (all installed in 03-01, slopcheck [OK]). No [ASSUMED]/[SUS] packages -> legitimacy checkpoint not triggered. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (Task 1).
- contact.yml + deploy.yml verify script exits 0 (Task 2).
- `npx next build` exits 0 with `/ua` + `/en` pre-rendered and zero YouTube iframes in `out/` HTML (Task 3).
- Human verification of all three sections + secret-leak check passes (Task 4).
</verification>

<success_criteria>
- Contact form submits, runs spam guards, POSTs workflow_dispatch with `response.ok` success handling, and shows success/error/loading states per UI-SPEC.
- contact.yml sends a Telegram message; Telegram secrets exist only as `${{ secrets.* }}` in the workflow (CONTACT-04).
- deploy.yml bakes NEXT_PUBLIC_GH_PAT into the build (D-02).
- page.tsx renders all 3 real Phase 3 sections (no SectionStub for highlights/gallery/contact); build exits 0.
- Human verification confirms the full media + contact experience and no client-side Telegram secret leak.
</success_criteria>

<output>
Create `.planning/phases/03-media-contact/03-04-SUMMARY.md` when done. Record: the build result, the `out/` HTML iframe grep count, the parse_mode tradeoff note, and the user_setup secrets still required for end-to-end Telegram delivery.
</output>
