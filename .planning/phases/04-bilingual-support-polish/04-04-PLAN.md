---
phase: 04-bilingual-support-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - app/[lang]/page.tsx
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - I18N-03
  - I18N-04

must_haves:
  truths:
    - "app/[lang]/page.tsx calls getDictionary(lang) and passes dict slices to all 9 components (Nav + 8 sections)"
    - "npx next build exits 0 with no TypeScript errors and no missing module errors"
    - "Build output contains out/ua/index.html and out/en/index.html as pre-rendered static HTML files"
    - "Each pre-rendered HTML file contains content from the corresponding locale dictionary (e.g., out/ua/index.html contains Ukrainian text)"
    - "No section component or Nav imports getDictionary directly — only page.tsx does (CLAUDE.md content data flow rule)"
  artifacts:
    - path: "app/[lang]/page.tsx"
      provides: "Async Server Component that loads dict and threads slices to all components"
      exports: ["HomePage"]
    - path: "out/ua/index.html"
      provides: "Pre-rendered Ukrainian locale HTML"
    - path: "out/en/index.html"
      provides: "Pre-rendered English locale HTML"
  key_links:
    - from: "app/[lang]/page.tsx"
      to: "lib/getDictionary.ts"
      via: "getDictionary(lang) call"
      pattern: "getDictionary"
    - from: "app/[lang]/page.tsx"
      to: "components/layout/Nav"
      via: "dict={dict.nav} prop"
      pattern: "dict\\.nav"
    - from: "app/[lang]/page.tsx"
      to: "components/sections/*"
      via: "dict={dict.sectionName} props"
      pattern: "dict=\\{dict\\."
---

<objective>
Wire everything together: update app/[lang]/page.tsx to load the dictionary for the current locale and pass the correct dict slice to Nav and all 8 section components. Verify the complete build produces both locale pre-rendered HTML files with zero errors.

Purpose: This is the final integration plan — all prior plans produce the parts; this plan connects them into a working bilingual site. Depends on both 04-02 (Nav + LanguageSwitcher) and 04-03 (section components) completing first.

Output: Updated app/[lang]/page.tsx, verified build output at out/ua/index.html and out/en/index.html.
</objective>

<execution_context>
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-bilingual-support-polish/04-CONTEXT.md
@.planning/phases/04-bilingual-support-polish/04-RESEARCH.md
@app/[lang]/layout.tsx
@app/[lang]/page.tsx

<interfaces>
<!-- Established pattern from layout.tsx — page.tsx follows the same async params pattern -->
From app/[lang]/layout.tsx (already established pattern):
```typescript
export default async function RootLayout({
  params,
}: {
  params: Promise<{ lang: string }>;
}) {
  const { lang } = await params;  // ← async params — Next.js 15 pattern
  ...
}
```

From lib/getDictionary.ts (created in 04-01):
```typescript
export type Dictionary = typeof enDict;
export async function getDictionary(lang: string): Promise<Dictionary>;
```

Component props after 04-02 and 04-03 (all components now require dict):
```typescript
// Nav requires: dict: Dictionary['nav']
// HeroSection requires: data: Player, dict: Dictionary['hero']
// AboutSection requires: data: Player, dict: Dictionary['about']
// TrophiesSection requires: trophies: Trophy[], dict: Dictionary['trophies']
// ClubSection requires: club: Club, dict: Dictionary['club']
// TeamSection requires: team: Team, dict: Dictionary['team']
// HighlightsSection requires: videos: VideoEntry[], dict: Dictionary['highlights']
// GallerySection requires: photos: GalleryEntry[], dict: Dictionary['gallery']
// ContactSection requires: dict: Dictionary['contact']
```

Player (after 04-01 migration — factual fields only):
```typescript
// player.fullName, player.workingFoot, player.dateOfBirth, player.nationality
// player.stats: PlayerStats
// player.trophies: Trophy[] (Trophy has only year: number)
// player.club: Club (Club has only logo: string | null)
// player.team: Team (Team has only logo: string | null)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire getDictionary into app/[lang]/page.tsx and pass dict props to all components</name>
  <files>app/[lang]/page.tsx</files>
  <read_first>
    - app/[lang]/page.tsx (full current state — currently a sync function with no dict loading, all components called without dict prop)
    - app/[lang]/layout.tsx (established async params pattern to replicate — const { lang } = await params)
    - lib/getDictionary.ts (confirm getDictionary export signature and Dictionary type name)
    - content/player.ts (confirm player export structure after 04-01 migration — club and team objects have only logo now)
  </read_first>
  <action>
Update app/[lang]/page.tsx with the following changes (read the file first to understand the current import list and component assembly):

1. ADD import: `import { getDictionary } from '@/lib/getDictionary'`
2. ADD import: `import type { Dictionary } from '@/lib/getDictionary'` — needed if TypeScript requires explicit type annotation (optional if getDictionary return type is inferred)
3. UPDATE the HomePage function to be async and accept params: change `export default function HomePage()` to `export default async function HomePage({ params }: { params: Promise<{ lang: string }> })`
4. At the top of the function body, add: `const { lang } = await params`
5. After the params destructure, add: `const dict = await getDictionary(lang)`
6. UPDATE the Nav render call: change `<Nav />` to `<Nav dict={dict.nav} />`
7. UPDATE HeroSection: change `<HeroSection data={player} />` to `<HeroSection data={player} dict={dict.hero} />`
8. UPDATE AboutSection: change `<AboutSection data={player} />` to `<AboutSection data={player} dict={dict.about} />`
9. UPDATE TrophiesSection: change `<TrophiesSection trophies={player.trophies} />` to `<TrophiesSection trophies={player.trophies} dict={dict.trophies} />`
10. UPDATE ClubSection: change `<ClubSection club={player.club} />` to `<ClubSection club={player.club} dict={dict.club} />`
11. UPDATE TeamSection: change `<TeamSection team={player.team} />` to `<TeamSection team={player.team} dict={dict.team} />`
12. UPDATE HighlightsSection: change `<HighlightsSection videos={videos} />` to `<HighlightsSection videos={videos} dict={dict.highlights} />`
13. UPDATE GallerySection: change `<GallerySection photos={gallery} />` to `<GallerySection photos={gallery} dict={dict.gallery} />`
14. UPDATE ContactSection: change `<ContactSection />` to `<ContactSection dict={dict.contact} />`

After editing, run `npx tsc --noEmit` to confirm zero TypeScript errors across the entire project before proceeding to the build step.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS" | xargs -I{} sh -c 'test {} -eq 0 && echo "TypeScript: PASS (0 errors)" || echo "TypeScript: FAIL ({} errors)"'</automated>
  </verify>
  <done>
    - app/[lang]/page.tsx function is async and accepts params: Promise&lt;{ lang: string }&gt;
    - page.tsx contains `const { lang } = await params`
    - page.tsx contains `const dict = await getDictionary(lang)`
    - All 9 component calls include their dict prop (Nav + 8 sections)
    - npx tsc --noEmit exits 0 with zero TypeScript errors
    - No section component imports getDictionary directly (only page.tsx does)
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full build verification and confirm both locale HTML files exist</name>
  <files>out/ua/index.html, out/en/index.html, out/index.html</files>
  <read_first>
    - next.config.ts (confirm output: 'export' and basePath: '/future-legend-dev' are set — required for static HTML output)
  </read_first>
  <action>
Run the full Next.js static export build and verify both locale routes are pre-rendered:

1. Run: `npx next build`
2. The build must exit 0 — any non-zero exit code means a build error that must be resolved before marking this plan done
3. After a successful build, verify the output directory structure contains both locale HTML files: `out/ua/index.html` and `out/en/index.html`
4. Spot-check locale content in the output HTML:
   - Verify `out/ua/index.html` contains Ukrainian text — check for at least one Ukrainian string from the dictionary (e.g., "Про мене" from nav, or "Трофеї", or "Галерея")
   - Verify `out/en/index.html` contains English text — check for at least one English string (e.g., "About", "Trophies", "Gallery")
5. Verify `out/index.html` exists (root page — the client-side redirect)
6. If the build fails due to TypeScript errors, read the error output carefully, fix the relevant file, and re-run the build — do not mark this task done until `npx next build` exits 0

Common failure modes to watch for and fix:
- Missing dict prop on a component call in page.tsx — add the missing prop
- TypeScript error in a section component from an improperly typed prop — check the Props interface matches the Dictionary slice shape
- Import error in getDictionary.ts — verify the import path `'../dictionaries/en.json'` resolves correctly relative to lib/
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5 && ls out/ua/index.html out/en/index.html out/index.html && grep -l "Про мене\|Трофеї\|Галерея" out/ua/index.html && grep -l "Trophies\|Gallery\|About" out/en/index.html && echo "BUILD VERIFICATION: PASS"</automated>
  </verify>
  <done>
    - npx next build exits 0
    - out/ua/index.html exists and contains at least one Ukrainian string
    - out/en/index.html exists and contains at least one English string
    - out/index.html exists (root client-redirect page)
    - No TypeScript errors, no missing module errors, no build warnings about unresolved imports
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lang URL segment → getDictionary call | The lang value from the URL segment is passed to getDictionary — must only load the whitelisted locales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | getDictionary(lang) call with URL-derived lang | mitigate | getDictionary falls back to 'en' loader for any unrecognised lang value — only 'ua' and 'en' loaders exist; dynamicParams = false in layout.tsx means only /ua/ and /en/ are pre-rendered — no arbitrary lang values reach getDictionary at build time |
| T-04-07 | Denial of Service | next build with broken dictionary | accept | Build-time failure is caught immediately by CI/CD — no runtime impact on deployed site |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
Phase 4 complete when ALL of these pass:

1. `npx tsc --noEmit` exits 0 — zero TypeScript errors across the entire project
2. `npx next build` exits 0
3. `ls out/ua/index.html out/en/index.html out/index.html` — all three files exist
4. `grep -c "Про мене" out/ua/index.html` returns 1 or more
5. `grep -c "Trophies" out/en/index.html` returns 1 or more
6. `grep -rn "getDictionary" components/` returns no output (sections never import getDictionary)
7. `grep -n "dict=" app/\[lang\]/page.tsx | wc -l` returns 9 or more (one per component call)
8. `grep -n "window.location.replace" app/page.tsx` returns a match with '/future-legend-dev'
9. `grep -rn "Contact Me\|'Trophies'\|'About'\|'Gallery'\|'Highlights'\|'Club'\|'Team'\|Send Message" components/sections/` returns no output (no hardcoded strings remain)
10. `grep -n "LanguageSwitcher" components/layout/Nav.tsx | wc -l` returns 3 or more
</verification>

<success_criteria>
- app/[lang]/page.tsx: async, awaits params and getDictionary, passes dict slices to all 9 components
- npx tsc --noEmit: exits 0 — zero TypeScript errors in the entire project
- npx next build: exits 0 — both /ua/ and /en/ pre-rendered as static HTML
- out/ua/index.html: contains Ukrainian dictionary strings
- out/en/index.html: contains English dictionary strings
- Phase 4 success criteria from ROADMAP.md all TRUE:
  1. /ua/ and /en/ exist as static HTML in build output
  2. Language switcher visible and functional with localStorage persistence
  3. Zero hardcoded strings in component files
  4. Both locales pre-rendered with zero runtime i18n overhead
</success_criteria>

<output>
Create `.planning/phases/04-bilingual-support-polish/04-04-SUMMARY.md` when done
</output>
