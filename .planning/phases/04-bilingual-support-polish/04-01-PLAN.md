---
phase: 04-bilingual-support-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dictionaries/en.json
  - dictionaries/ua.json
  - lib/getDictionary.ts
  - app/page.tsx
  - content/player.ts
autonomous: true
requirements:
  - I18N-01
  - I18N-03
  - I18N-04

must_haves:
  truths:
    - "Both dictionaries contain a complete nested key structure with all section namespaces"
    - "ua.json is type-compatible with en.json — npx tsc --noEmit emits no Dictionary-shape errors"
    - "lib/getDictionary.ts exports type Dictionary and async function getDictionary(lang)"
    - "getDictionary uses dynamic import() — no fs.readFile — so it runs at build time in static export"
    - "app/page.tsx is a 'use client' component that redirects via useEffect + window.location.replace with the basePath prefix"
    - "Root redirect reads localStorage 'locale' key, whitelists to 'ua'|'en', defaults to 'ua' for first-time visitors"
    - "player.ts interfaces (Player, Club, Team, Trophy) no longer declare bio, position, name (Club/Team), description (Club/Team) fields"
    - "npx tsc --noEmit after player.ts migration emits TypeScript errors in section components (expected — downstream plans fix them)"
  artifacts:
    - path: "dictionaries/en.json"
      provides: "Complete English dictionary with nav, hero, about, trophies, club, team, highlights, gallery, contact namespaces"
      contains: "nav.brand"
    - path: "dictionaries/ua.json"
      provides: "Complete Ukrainian dictionary with identical key structure to en.json"
      contains: "nav.brand"
    - path: "lib/getDictionary.ts"
      provides: "Dictionary type + async loader"
      exports: ["Dictionary", "getDictionary"]
    - path: "app/page.tsx"
      provides: "Client-side localStorage-aware root redirect"
      contains: "'use client'"
    - path: "content/player.ts"
      provides: "Factual player data only — translatable text removed"
  key_links:
    - from: "lib/getDictionary.ts"
      to: "dictionaries/en.json"
      via: "import type enDict"
      pattern: "typeof enDict"
    - from: "lib/getDictionary.ts"
      to: "dictionaries/ua.json"
      via: "dynamic import"
      pattern: "import.*ua.json"
    - from: "app/page.tsx"
      to: "/future-legend-dev/ua/ or /future-legend-dev/en/"
      via: "window.location.replace"
      pattern: "window\\.location\\.replace"
---

<objective>
Lay the dictionary foundation for the bilingual system: fill both locale JSON files with the complete nested key structure, create the getDictionary loader, fix the broken root redirect in app/page.tsx, and strip translatable fields from player.ts interfaces (D-12 Option A).

Purpose: Every downstream plan (04-02, 04-03, 04-04) depends on the Dictionary type, the filled JSON files, and the cleaned player.ts interfaces existing first. This plan has zero upstream dependencies and can run immediately.

Output: dictionaries/en.json (filled), dictionaries/ua.json (filled), lib/getDictionary.ts (new), app/page.tsx (rewritten), content/player.ts (interfaces + data objects pruned).
</objective>

<execution_context>
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-bilingual-support-polish/04-CONTEXT.md
@.planning/phases/04-bilingual-support-polish/04-RESEARCH.md
@content/player.ts
@app/page.tsx
@tsconfig.json

<interfaces>
<!-- Current player.ts interfaces — executor needs these to understand what to remove -->

From content/player.ts (current state — fields to REMOVE marked):
```typescript
export interface Trophy {
  name: string;       // REMOVE — moves to dict.trophies.items[i].name
  year: number;       // KEEP
  competition: string; // REMOVE — moves to dict.trophies.items[i].competition
}

export interface Club {
  name: string;       // REMOVE — moves to dict.club.name
  logo: string | null; // KEEP
  description: string; // REMOVE — moves to dict.club.description
}

export interface Team {
  name: string;       // REMOVE — moves to dict.team.name
  logo: string | null; // KEEP
  description: string; // REMOVE — moves to dict.team.description
}

export interface Player {
  fullName: string;   // KEEP (D-13)
  position: string;   // REMOVE — moves to dict.hero.position / dict.about.labelPosition
  workingFoot: 'Right' | 'Left' | 'Both'; // KEEP
  dateOfBirth: string; // KEEP
  nationality: string; // KEEP
  bio: string;        // REMOVE — moves to dict.about.bio
  stats: PlayerStats; // KEEP
  trophies: Trophy[]; // KEEP (but Trophy interface changes)
  club: Club;         // KEEP (but Club interface changes)
  team: Team;         // KEEP (but Team interface changes)
}
```

After migration (D-12 Option A), Trophy interface keeps only `year: number`.
Club interface keeps only `logo: string | null`.
Team interface keeps only `logo: string | null`.
Player interface drops `position` and `bio`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill dictionaries/en.json with complete nested structure</name>
  <files>dictionaries/en.json</files>
  <read_first>
    - dictionaries/en.json (current state — currently `{}`, will be fully replaced)
    - .planning/phases/04-bilingual-support-polish/04-RESEARCH.md (§ "en.json — complete nested structure" for exact keys)
    - components/sections/ContactSection.tsx (source of truth for all 15 contact strings including the errorSubmit email literal)
    - components/layout/Nav.tsx (source for 10 nav strings including NAV_LINKS labels and brand text)
  </read_first>
  <action>
Replace the empty `{}` in dictionaries/en.json with the complete nested JSON object. The structure must contain exactly these top-level namespaces with these keys:

nav namespace (10 keys): brand = "Future Legend", home = "Home", about = "About", highlights = "Highlights", gallery = "Gallery", trophies = "Trophies", club = "Club", team = "Team", contact = "Contact", menuAriaLabel = "Toggle navigation menu"

hero namespace (3 keys): position = "Central Midfielder", cta = "Contact Me", ctaAriaLabel = "Contact Dmytro Kovalenko"

about namespace (14 keys): title = "About", labelName = "Name", labelPosition = "Position", labelWorkingFoot = "Working Foot", labelDob = "Date of Birth", workingFootRight = "Right", workingFootLeft = "Left", workingFootBoth = "Both", bio = (the full 3-sentence bio paragraph currently in player.ts — copy it exactly), statPace = "Pace", statDribbling = "Dribbling", statShooting = "Shooting", statPassing = "Passing", statPhysical = "Physical", statDefending = "Defending"

trophies namespace: title = "Trophies", items = array of exactly 3 objects matching player.ts order: [{ name: "Youth League Champion", competition: "UPL Youth League U18" }, { name: "Cup Winner", competition: "Kyiv Regional Youth Cup" }, { name: "Tournament MVP", competition: "Dana Cup Denmark" }] — D-14 enforcement: 3 items required to match player.ts trophies array length

club namespace (4 keys): title = "Club", name = "Dynamo Kyiv U21", description = (full description from player.ts club.description — copy exactly), crestPlaceholder = "Club crest"

team namespace (4 keys): title = "Team", name = "Ukraine U18 National Team", description = (full description from player.ts team.description — copy exactly), crestPlaceholder = "Team crest"

highlights namespace (2 keys): title = "Highlights", intro = "Watch training sessions and match clips from the pitch."

gallery namespace (4 keys): title = "Gallery", intro = "Behind the scenes on the pitch and in training.", empty = "No photos yet.", openPhotoAriaLabel = "Open photo in fullscreen"

contact namespace (15 keys): title = "Contact", intro = "Get in touch — whether you are a scout, coach, or club representative.", successHeading = "Message Sent", successBody = "Your message has been sent. We'll be in touch soon.", labelName = "Name", placeholderName = "Your full name", errorNameMinLength = "Name must be at least 3 characters", labelPhone = "Phone (optional)", placeholderPhone = "+380 XX XXX XXXX", labelMessage = "Message", placeholderMessage = "Tell us what you have in mind...", btnSend = "Send Message", btnSending = "Sending...", spinnerAriaLabel = "Sending...", errorSubmit = "Something went wrong. Please try again or email us directly at dimakyh@ukr.net"

The honeypot "Website" label stays hardcoded in ContactSection — do NOT add it to the dictionary.
  </action>
  <verify>
    <automated>node -e "const d = require('./dictionaries/en.json'); const keys = ['nav','hero','about','trophies','club','team','highlights','gallery','contact']; keys.forEach(k => { if (!d[k]) throw new Error('Missing namespace: ' + k); }); if (d.trophies.items.length !== 3) throw new Error('trophies.items must have 3 entries'); if (!d.contact.errorSubmit.includes('dimakyh@ukr.net')) throw new Error('errorSubmit missing email'); console.log('en.json OK');"</automated>
  </verify>
  <done>
    - dictionaries/en.json is valid JSON with 9 top-level namespace keys
    - trophies.items has exactly 3 entries (D-14)
    - contact.errorSubmit contains "dimakyh@ukr.net"
    - about.bio contains the full player biography prose (not a placeholder)
    - club.description and team.description contain the full prose from player.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Fill dictionaries/ua.json with Ukrainian translations</name>
  <files>dictionaries/ua.json</files>
  <read_first>
    - dictionaries/ua.json (current state — currently `{}`, will be fully replaced)
    - dictionaries/en.json (just filled in Task 1 — use as structural template; every key must be present)
  </read_first>
  <action>
Replace the empty `{}` in dictionaries/ua.json with a complete Ukrainian translation using the identical key structure as en.json. Every key present in en.json must be present in ua.json — TypeScript will catch gaps via the Dictionary type inference. Use these Ukrainian values:

nav: brand = "Future Legend" (unchanged — proper noun), home = "Головна", about = "Про мене", highlights = "Відео", gallery = "Галерея", trophies = "Трофеї", club = "Клуб", team = "Збірна", contact = "Контакт", menuAriaLabel = "Відкрити навігаційне меню"

hero: position = "Центральний півзахисник", cta = "Зв'язатися", ctaAriaLabel = "Зв'язатися з Дмитром Коваленком"

about: title = "Про мене", labelName = "Ім'я", labelPosition = "Позиція", labelWorkingFoot = "Робоча нога", labelDob = "Дата народження", workingFootRight = "Права", workingFootLeft = "Ліва", workingFootBoth = "Обидві", bio = "Технічно обдарований півзахисник, який читає гру на два кроки наперед, Дмитро здобув репутацію завдяки точним передачам і невпинному пресингу, що руйнує суперника до того, як той встигає зорієнтуватися. Вихований у молодіжній академії Динамо Київ, він поєднує тактичну дисципліну зі здатністю розкривати компактні оборонні побудови. У вісімнадцять він уже серце своєї команди — той, хто торкається м'яча найбільше і втрачає найменше.", statPace = "Швидкість", statDribbling = "Дриблінг", statShooting = "Удар", statPassing = "Передача", statPhysical = "Фізика", statDefending = "Захист"

trophies: title = "Трофеї", items = [{ name: "Чемпіон юнацької ліги", competition: "Юнацька ліга УПЛ U18" }, { name: "Фіналіст кубку", competition: "Кубок юнаків Київської області" }, { name: "MVP турніру", competition: "Dana Cup Данія" }] — exactly 3 items (D-14)

club: title = "Клуб", name = "Динамо Київ U21", description = "Один із найтитулованіших клубів України, Динамо Київ виховало покоління міжнародних футболістів. Структура U21 тренується поруч із першою командою, занурюючи молодих гравців у професійні стандарти з першого дня.", crestPlaceholder = "Герб клубу"

team: title = "Збірна", name = "Збірна України U18", description = "Захищаючи кольори національної збірної на юнацькому рівні, Дмитро переносить клубну форму на міжнародні змагання в рамках турнірів розвитку УЄФА.", crestPlaceholder = "Герб збірної"

highlights: title = "Відео", intro = "Дивіться тренувальні сесії та матчові нарізки з поля."

gallery: title = "Галерея", intro = "Закулісне — на полі та на тренуваннях.", empty = "Фото ще немає.", openPhotoAriaLabel = "Відкрити фото в повноекранному режимі"

contact: title = "Контакт", intro = "Зв'яжіться — незалежно від того, чи ви скаут, тренер або представник клубу.", successHeading = "Повідомлення надіслано", successBody = "Ваше повідомлення надіслано. Ми зв'яжемося з вами найближчим часом.", labelName = "Ім'я", placeholderName = "Ваше повне ім'я", errorNameMinLength = "Ім'я має містити не менше 3 символів", labelPhone = "Телефон (необов'язково)", placeholderPhone = "+380 XX XXX XXXX", labelMessage = "Повідомлення", placeholderMessage = "Розкажіть, що вас цікавить...", btnSend = "Надіслати", btnSending = "Надсилання...", spinnerAriaLabel = "Надсилання...", errorSubmit = "Щось пішло не так. Спробуйте ще раз або напишіть нам на dimakyh@ukr.net"
  </action>
  <verify>
    <automated>node -e "const en = require('./dictionaries/en.json'); const ua = require('./dictionaries/ua.json'); function checkKeys(obj, ref, path) { for (const k of Object.keys(ref)) { if (!(k in obj)) throw new Error('ua.json missing key: ' + path + '.' + k); if (typeof ref[k] === 'object' && !Array.isArray(ref[k])) checkKeys(obj[k], ref[k], path + '.' + k); } } checkKeys(ua, en, 'root'); if (ua.trophies.items.length !== 3) throw new Error('ua trophies.items must have 3 entries'); console.log('ua.json structure OK');"</automated>
  </verify>
  <done>
    - ua.json is valid JSON with exactly the same key structure as en.json
    - ua.trophies.items has exactly 3 entries (D-14)
    - All string values are Ukrainian (not English copies)
    - contact.errorSubmit contains "dimakyh@ukr.net" (email stays identical in both languages)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create lib/getDictionary.ts and rewrite app/page.tsx</name>
  <files>lib/getDictionary.ts, app/page.tsx</files>
  <read_first>
    - lib/getDictionary.ts (check if file exists; create if absent)
    - app/page.tsx (current state — contains broken redirect('/ua') server call)
    - .planning/phases/04-bilingual-support-polish/04-RESEARCH.md (§ "Pattern 1: getDictionary async loader" and § "Pattern 4: Root redirect")
    - tsconfig.json (confirm resolveJsonModule: true is set — required for typeof import() type inference)
    - next.config.ts (confirm basePath value — must match BASE_PATH constant)
  </read_first>
  <action>
CREATE lib/getDictionary.ts as a new file with these exact elements:

1. Import the en.json type using `import type enDict from '../dictionaries/en.json'` — this uses TypeScript's resolveJsonModule (already enabled in tsconfig.json) to infer the Dictionary type automatically (D-04).

2. Export the inferred type: `export type Dictionary = typeof enDict`

3. Define a dictionaries lookup object with two loaders:
   - ua: arrow function returning `import('../dictionaries/ua.json').then((m) => m.default as Dictionary)`
   - en: arrow function returning `import('../dictionaries/en.json').then((m) => m.default as Dictionary)`

4. Export `async function getDictionary(lang: string): Promise<Dictionary>` that looks up the loader by lang key, falls back to the 'en' loader if no match, and returns the result.

REWRITE app/page.tsx as a client component with these exact elements:

1. First line: `'use client'`
2. Import only `useEffect` from 'react' — remove the existing `redirect` import from 'next/navigation'
3. Declare `const BASE_PATH = '/future-legend-dev'` as a module-level constant (per RESEARCH.md Pitfall 1 — missing basePath in redirect is the live bug)
4. Export default function `RootPage()` that:
   - Contains a `useEffect` with empty dependency array `[]`
   - Inside useEffect: `const stored = localStorage.getItem('locale')` then `const target = stored === 'en' ? 'en' : 'ua'` (whitelist — per security domain: never use raw stored value) then `window.location.replace(\`${BASE_PATH}/${target}/\`)`
   - Returns `null` (no JSX — renders nothing while redirecting, per D-09)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "player\.ts\|HeroSection\|AboutSection\|TrophiesSection\|ClubSection\|TeamSection" | grep -c "error TS" || echo "0 unexpected errors"</automated>
  </verify>
  <done>
    - lib/getDictionary.ts exists and exports Dictionary type and getDictionary function
    - getDictionary uses dynamic import() — no fs import present in the file
    - app/page.tsx starts with 'use client' on line 1
    - app/page.tsx contains `const BASE_PATH = '/future-legend-dev'`
    - app/page.tsx contains `window.location.replace` (not redirect())
    - app/page.tsx contains the whitelist: `stored === 'en' ? 'en' : 'ua'`
    - app/page.tsx returns null (no JSX elements)
    - No TypeScript errors in getDictionary.ts or app/page.tsx (errors in section components from player.ts migration are expected and are caught in the next task)
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate content/player.ts — remove translatable fields (D-12 Option A)</name>
  <files>content/player.ts</files>
  <read_first>
    - content/player.ts (current state — must read every interface and the data object before editing)
  </read_first>
  <action>
Apply D-12 Option A to content/player.ts — remove all translatable/narrative fields from both interfaces and the data object. This intentionally creates TypeScript compile errors in section components; those errors guide the migration in plan 04-03.

INTERFACE CHANGES — remove these fields:

From Trophy interface: remove `name: string` and `competition: string` — keep only `year: number`

From Club interface: remove `name: string` and `description: string` — keep only `logo: string | null`

From Team interface: remove `name: string` and `description: string` — keep only `logo: string | null`

From Player interface: remove `position: string` and `bio: string` — keep fullName, workingFoot, dateOfBirth, nationality, stats, trophies, club, team

DATA OBJECT CHANGES — remove these values from the `player` const:

From the player object: remove `position: 'Central Midfielder'` line and `bio: '...'` line (the long paragraph)

From each trophy object in player.trophies: remove `name:` and `competition:` fields — keep only `year:` for each entry. The three entries become:
  `{ year: 2024 }`, `{ year: 2023 }` (cup), `{ year: 2023 }` (mvp)

From player.club object: remove `name: 'Dynamo Kyiv U21'` and `description: '...'` — keep only `logo: null`

From player.team object: remove `name: 'Ukraine U18 National Team'` and `description: '...'` — keep only `logo: null`

After editing, run `npx tsc --noEmit` and confirm it produces errors in HeroSection.tsx, AboutSection.tsx, TrophiesSection.tsx, ClubSection.tsx, TeamSection.tsx (reading removed fields). These errors are expected and confirm the migration is correctly forcing section components to stop reading these fields from player data.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS" | xargs -I{} sh -c 'test {} -gt 0 && echo "TypeScript errors present (expected — section components read removed fields)" || echo "WARNING: no errors found — migration may not have taken effect"'</automated>
  </verify>
  <done>
    - content/player.ts Trophy interface has only `year: number` (name and competition removed)
    - content/player.ts Club interface has only `logo: string | null` (name and description removed)
    - content/player.ts Team interface has only `logo: string | null` (name and description removed)
    - content/player.ts Player interface has no `position` or `bio` fields
    - player data object trophies array entries have no `name` or `competition` keys
    - player data object club has no `name` or `description` keys
    - player data object team has no `name` or `description` keys
    - npx tsc --noEmit produces TypeScript errors in section components (confirms sections were reading removed fields — expected)
    - No TypeScript errors originate from player.ts itself (interfaces and data are internally consistent)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → redirect target | Stored locale value crosses from persistent storage into URL navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | app/page.tsx localStorage read | mitigate | Whitelist: `const target = stored === 'en' ? 'en' : 'ua'` — raw stored value never used directly in navigation |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase — all dependencies are existing project dependencies |
</threat_model>

<verification>
After all four tasks complete:
1. `node -e "require('./dictionaries/en.json')"` exits 0 (valid JSON)
2. `node -e "require('./dictionaries/ua.json')"` exits 0 (valid JSON)
3. `ls lib/getDictionary.ts` confirms file exists
4. `grep -n "'use client'" app/page.tsx` returns line 1
5. `grep -n "window.location.replace" app/page.tsx` returns a match
6. `grep -n "redirect" app/page.tsx` returns no match (old import removed)
7. `grep -n "position\|bio" content/player.ts | grep -v "//"` returns no interface or data field matches
8. `npx tsc --noEmit 2>&1 | grep "error TS"` shows errors only in section component files (expected — plan 04-03 fixes them)
</verification>

<success_criteria>
- dictionaries/en.json: valid JSON, 9 namespaces, trophies.items has 3 entries, contact.errorSubmit contains dimakyh@ukr.net
- dictionaries/ua.json: valid JSON, identical key structure to en.json, trophies.items has 3 entries
- lib/getDictionary.ts: exports Dictionary type (inferred from en.json) and getDictionary async function using dynamic import()
- app/page.tsx: 'use client', useEffect redirect to BASE_PATH + whitelisted locale, returns null
- content/player.ts: Player/Club/Team/Trophy interfaces contain only factual fields; data object matches interfaces
</success_criteria>

<output>
Create `.planning/phases/04-bilingual-support-polish/04-01-SUMMARY.md` when done
</output>
