---
phase: 04-bilingual-support-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - components/layout/LanguageSwitcher.tsx
  - components/layout/LanguageSwitcher.module.scss
  - components/layout/Nav.tsx
  - components/layout/Nav.module.scss
autonomous: true
requirements:
  - I18N-02

must_haves:
  truths:
    - "LanguageSwitcher renders two buttons — one per locale — with flag emoji + locale code (D-06)"
    - "Active locale button has aria-pressed=true and the active CSS class with bottom underline in --color-accent (D-08)"
    - "Clicking a locale button writes 'locale' to localStorage (D-10, D-11) then navigates via window.location.href with BASE_PATH prefix (D-07)"
    - "LanguageSwitcher has mounted guard — returns null before hydration — matching ThemeToggle.tsx pattern"
    - "Nav.tsx accepts dict prop of type Dictionary['nav'] and renders nav link labels from dict values"
    - "NAV_LINKS is no longer a module-scope constant — labels come from dict.nav.* inside the render"
    - "LanguageSwitcher is rendered in both the desktop controls div (after ThemeToggle) and the mobile menu overlay"
  artifacts:
    - path: "components/layout/LanguageSwitcher.tsx"
      provides: "'use client' locale switcher with localStorage write + window.location.href navigation"
      contains: "'use client'"
    - path: "components/layout/LanguageSwitcher.module.scss"
      provides: "Switcher, btn, and active CSS classes using --color-accent token"
      contains: ".active"
    - path: "components/layout/Nav.tsx"
      provides: "Nav accepting dict prop; hardcoded strings replaced with dict lookups; LanguageSwitcher rendered"
      contains: "LanguageSwitcher"
  key_links:
    - from: "components/layout/Nav.tsx"
      to: "components/layout/LanguageSwitcher.tsx"
      via: "import + render in controls div and mobileMenu"
      pattern: "LanguageSwitcher"
    - from: "components/layout/LanguageSwitcher.tsx"
      to: "localStorage key 'locale'"
      via: "localStorage.setItem"
      pattern: "localStorage\\.setItem"
    - from: "components/layout/LanguageSwitcher.tsx"
      to: "window.location.href"
      via: "switchLocale function"
      pattern: "window\\.location\\.href"
---

<objective>
Create the LanguageSwitcher component and integrate it into Nav. Update Nav to accept a dict prop (replacing the hardcoded NAV_LINKS module constant) and render LanguageSwitcher in both desktop controls and mobile menu.

Purpose: Delivers the user-facing locale toggle (I18N-02). Runs in parallel with plan 04-03 (both depend only on 04-01).

Output: LanguageSwitcher.tsx (new), LanguageSwitcher.module.scss (new), Nav.tsx (dict prop + LanguageSwitcher renders).
</objective>

<execution_context>
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-bilingual-support-polish/04-CONTEXT.md
@.planning/phases/04-bilingual-support-polish/04-RESEARCH.md
@components/layout/Nav.tsx
@components/layout/ThemeToggle.tsx
@components/layout/Nav.module.scss

<interfaces>
<!-- Dictionary type from 04-01 output — import from lib/getDictionary -->
From lib/getDictionary.ts (created in 04-01):
```typescript
export type Dictionary = typeof enDict; // inferred from en.json

// nav slice shape:
// Dictionary['nav'] = {
//   brand: string;
//   home: string;
//   about: string;
//   highlights: string;
//   gallery: string;
//   trophies: string;
//   club: string;
//   team: string;
//   contact: string;
//   menuAriaLabel: string;
// }
```

Nav's current Props interface (no dict yet — must add):
```typescript
// Current:
export function Nav() { ... }

// Target:
interface Props {
  dict: Dictionary['nav'];
}
export function Nav({ dict }: Props) { ... }
```

Current NAV_LINKS (module-scope constant — must be replaced with inline array using dict values):
```typescript
const NAV_LINKS = [
  { label: 'Home',       href: '#home' },
  { label: 'About',      href: '#about' },
  { label: 'Highlights', href: '#highlights' },
  { label: 'Gallery',    href: '#gallery' },
  { label: 'Trophies',   href: '#trophies' },
  { label: 'Club',       href: '#club' },
  { label: 'Team',       href: '#team' },
  { label: 'Contact',    href: '#contact' },
];
```

ThemeToggle.tsx mounted guard pattern (replicate in LanguageSwitcher):
```typescript
const [mounted, setMounted] = useState(false);
useEffect(() => setMounted(true), []);
if (!mounted) return null;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LanguageSwitcher.tsx and LanguageSwitcher.module.scss</name>
  <files>components/layout/LanguageSwitcher.tsx, components/layout/LanguageSwitcher.module.scss</files>
  <read_first>
    - components/layout/ThemeToggle.tsx (mounted guard pattern to replicate exactly — useState false, useEffect sets true, return null if !mounted)
    - components/layout/ThemeToggle.module.scss (token usage pattern to follow for LanguageSwitcher styles)
    - .planning/phases/04-bilingual-support-polish/04-RESEARCH.md (§ "Pattern 3: LanguageSwitcher component" for exact implementation)
    - next.config.ts (confirm basePath = '/future-legend-dev' — must match BASE_PATH constant in this file)
  </read_first>
  <action>
CREATE components/layout/LanguageSwitcher.tsx as a new file with these exact elements:

1. `'use client'` directive on line 1
2. Imports: `usePathname` from 'next/navigation'; `useState` and `useEffect` from 'react'; `styles` from './LanguageSwitcher.module.scss'
3. Constants at module scope: `const LOCALES = ['ua', 'en'] as const` (as const for type narrowing); `const FLAGS = { ua: '🇺🇦', en: '🇬🇧' } as const`; `const BASE_PATH = '/future-legend-dev'`
4. Export named function `LanguageSwitcher()` (no props)
5. Inside function: `const pathname = usePathname()` — pathname will be e.g. "/future-legend-dev/ua" or "/future-legend-dev/en"
6. Mounted guard: `const [mounted, setMounted] = useState(false)` then `useEffect(() => setMounted(true), [])` then `if (!mounted) return null` — this follows the ThemeToggle pattern exactly and prevents hydration mismatch (RESEARCH.md Pitfall 5)
7. Locale detection: `const currentLang = LOCALES.find((l) => pathname.includes(\`/\${l}\`)) ?? 'ua'` — falls back to 'ua' if no locale segment found
8. switchLocale function: `function switchLocale(newLang: string)` that calls `localStorage.setItem('locale', newLang)` (D-10, D-11), then reads `const hash = window.location.hash`, then sets `window.location.href = \`\${BASE_PATH}/\${newLang}/\${hash}\`` (D-07 — preserves section anchor)
9. Return JSX: a `div` with `className={styles.switcher}`, `role="group"`, `aria-label="Language selector"`, containing `{LOCALES.map((lang) => ( <button key={lang} onClick={() => switchLocale(lang)} className={\`\${styles.btn} \${lang === currentLang ? styles.active : ''}\`} aria-pressed={lang === currentLang} aria-label={\`Switch to \${lang === 'ua' ? 'Ukrainian' : 'English'}\`}>{FLAGS[lang]} {lang.toUpperCase()}</button> ))}`

CREATE components/layout/LanguageSwitcher.module.scss with these classes:

.switcher: `display: flex; align-items: center; gap: 0.25rem;`

.btn: `background: none; border: none; cursor: pointer; font-family: var(--font-roboto); color: var(--color-text-secondary); text-transform: uppercase; font-size: 0.85rem; padding: 0.25rem 0.5rem; transition: color 0.2s ease, opacity 0.2s ease; line-height: 1; text-decoration: none;`

.active: `color: var(--color-accent); text-decoration-line: underline; text-decoration-color: var(--color-accent); text-underline-offset: 3px;` (D-08 — bottom underline in --color-accent)

.btn:hover (non-active): `opacity: 0.8;` — do NOT animate with GSAP (Framer Motion / CSS hover only per CLAUDE.md animation rule)
  </action>
  <verify>
    <automated>grep -n "'use client'" components/layout/LanguageSwitcher.tsx && grep -n "mounted" components/layout/LanguageSwitcher.tsx && grep -n "localStorage.setItem" components/layout/LanguageSwitcher.tsx && grep -n "BASE_PATH" components/layout/LanguageSwitcher.tsx && echo "LanguageSwitcher.tsx structure OK"</automated>
  </verify>
  <done>
    - LanguageSwitcher.tsx starts with 'use client'
    - LanguageSwitcher.tsx contains mounted guard (useState false + useEffect sets true + return null if !mounted)
    - LanguageSwitcher.tsx contains `const BASE_PATH = '/future-legend-dev'`
    - LanguageSwitcher.tsx contains `localStorage.setItem('locale', newLang)` in switchLocale
    - LanguageSwitcher.tsx contains `window.location.href` navigation (not next/link, not useRouter)
    - LanguageSwitcher.module.scss contains .switcher, .btn, .active classes
    - .active class references var(--color-accent) for color and text-decoration
    - No GSAP import or useGSAP in LanguageSwitcher.tsx (Framer Motion owns hover per CLAUDE.md)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Nav.tsx to accept dict prop and render LanguageSwitcher</name>
  <files>components/layout/Nav.tsx</files>
  <read_first>
    - components/layout/Nav.tsx (full current state — must read before any edit to understand the exact DOM structure: controls div, mobileMenu div, hamburger button, NAV_LINKS map)
    - lib/getDictionary.ts (confirm Dictionary type is exported — needed for the Props interface import)
  </read_first>
  <action>
Update components/layout/Nav.tsx with these changes (read the current file first to locate exact insertion points):

1. ADD import at top: `import type { Dictionary } from '@/lib/getDictionary'`
2. ADD import: `import { LanguageSwitcher } from './LanguageSwitcher'`
3. ADD Props interface before the Nav function: `interface Props { dict: Dictionary['nav']; }` 
4. UPDATE function signature from `export function Nav()` to `export function Nav({ dict }: Props)`
5. REMOVE the module-scope `const NAV_LINKS = [...]` constant entirely
6. Inside the Nav render, replace the NAV_LINKS reference with an inline array. Define the nav links array inline at the start of the render using dict values. The inline array has the same 8 entries, but labels come from dict.nav:
   - `{ label: dict.home, href: '#home' }` — repeat for about, highlights, gallery, trophies, club, team, contact using the matching dict.nav key (dict.about, dict.highlights, dict.gallery, dict.trophies, dict.club, dict.team, dict.contact)
7. REPLACE the hardcoded `'Future Legend'` brand text inside the `<a href="#home">` element with `{dict.brand}`
8. REPLACE the hardcoded `aria-label="Toggle navigation menu"` on the hamburger button with `aria-label={dict.menuAriaLabel}`
9. In the DESKTOP controls div (the div with className={styles.controls}): add `<LanguageSwitcher />` as a sibling element after `<ThemeToggle />` and before the hamburger button
10. In the MOBILE menu overlay (the AnimatePresence motion.div containing the mobile nav links): add `<LanguageSwitcher />` as the LAST child inside the motion.div, after all the nav link `<a>` elements (D-05 — at bottom of link list)

The `href` values (#home, #about, etc.) remain hardcoded — they are anchor IDs, not translatable content.
  </action>
  <verify>
    <automated>grep -n "Dictionary" components/layout/Nav.tsx && grep -n "LanguageSwitcher" components/layout/Nav.tsx && grep -c "LanguageSwitcher" components/layout/Nav.tsx | xargs -I{} sh -c 'test {} -ge 2 && echo "LanguageSwitcher present in 2+ locations (desktop + mobile)" || echo "ERROR: LanguageSwitcher must appear at least twice"' && grep -n "NAV_LINKS" components/layout/Nav.tsx | wc -l | xargs -I{} sh -c 'test {} -eq 0 && echo "NAV_LINKS removed" || echo "ERROR: NAV_LINKS still present"'</automated>
  </verify>
  <done>
    - Nav.tsx imports Dictionary from '@/lib/getDictionary'
    - Nav.tsx imports LanguageSwitcher from './LanguageSwitcher'
    - Nav.tsx function signature includes `{ dict }: Props` parameter
    - Nav.tsx Props interface declares `dict: Dictionary['nav']`
    - The module-scope `const NAV_LINKS` constant is completely removed
    - Nav link labels reference dict.home, dict.about, dict.highlights, dict.gallery, dict.trophies, dict.club, dict.team, dict.contact
    - Brand text `<a href="#home">` renders `{dict.brand}`
    - Hamburger button aria-label is `{dict.menuAriaLabel}`
    - `<LanguageSwitcher />` appears in desktop controls div (after ThemeToggle)
    - `<LanguageSwitcher />` appears in mobile menu overlay (after last nav link)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → navigation target | Locale value read from localStorage influences window.location.href target |
| usePathname() → active state display | Pathname string from Next.js router used to derive active locale indicator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | LanguageSwitcher localStorage read (in switchLocale, written — not read) | accept | Write-only in LanguageSwitcher; read and whitelisted in app/page.tsx (T-04-01 already mitigated) |
| T-04-03 | Information Disclosure | LanguageSwitcher renders locale info | accept | Locale preference is non-sensitive UI state; no PII involved |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
After both tasks complete:
1. `grep -n "'use client'" components/layout/LanguageSwitcher.tsx` returns line 1
2. `grep -c "LanguageSwitcher" components/layout/Nav.tsx` returns 3 or more (import + 2 render sites)
3. `grep -n "NAV_LINKS" components/layout/Nav.tsx` returns no output (constant removed)
4. `grep -n "dict\." components/layout/Nav.tsx` returns multiple matches (dict.home, dict.brand, etc.)
5. `grep -n "localStorage.setItem" components/layout/LanguageSwitcher.tsx` returns a match
6. `grep -n "BASE_PATH" components/layout/LanguageSwitcher.tsx` returns a match with '/future-legend-dev'
7. `npx tsc --noEmit 2>&1 | grep "LanguageSwitcher\|Nav.tsx" | grep "error TS"` returns no errors in these two files (errors only expected in section components from plan 04-01)
</verification>

<success_criteria>
- LanguageSwitcher.tsx: 'use client', mounted guard, LOCALES + FLAGS constants, BASE_PATH = '/future-legend-dev', switchLocale writes localStorage then navigates with hash preservation
- LanguageSwitcher.module.scss: .switcher (flex), .btn (no border/bg, uppercase, var(--font-roboto)), .active (var(--color-accent) underline)
- Nav.tsx: accepts dict: Dictionary['nav'] prop, NAV_LINKS removed, labels from dict.nav.*, brand from dict.brand, menuAriaLabel from dict.menuAriaLabel, LanguageSwitcher rendered in desktop controls and mobile menu
</success_criteria>

<output>
Create `.planning/phases/04-bilingual-support-polish/04-02-SUMMARY.md` when done
</output>
