---
phase: 04-bilingual-support-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - components/sections/HeroSection.tsx
  - components/sections/AboutSection.tsx
  - components/sections/TrophiesSection.tsx
  - components/sections/ClubSection.tsx
  - components/sections/TeamSection.tsx
  - components/sections/HighlightsSection.tsx
  - components/sections/GallerySection.tsx
  - components/sections/ContactSection.tsx
autonomous: true
requirements:
  - I18N-03

must_haves:
  truths:
    - "All 8 section components accept a dict prop typed as Dictionary['sectionName']"
    - "No hardcoded English string remains in any section component — every visible string references a dict key"
    - "HeroSection no longer reads data.position (field removed in 04-01) — reads dict.hero.position instead"
    - "AboutSection no longer reads data.bio or data.position — reads dict.about.bio and dict.about.labelPosition respectively"
    - "TrophiesSection zips player.trophies by index with dict.trophies.items[i] for name and competition; only year comes from player data"
    - "ClubSection and TeamSection no longer read club.name, club.description, team.name, team.description (removed in 04-01)"
    - "ContactSection setNameError() calls in handleNameBlur and handleSubmit use dict.contact.errorNameMinLength"
    - "npx tsc --noEmit emits zero errors in any section component file after this plan"
  artifacts:
    - path: "components/sections/HeroSection.tsx"
      provides: "Hero section with dict.hero prop replacing hardcoded position, cta, ctaAriaLabel"
      contains: "dict.hero"
    - path: "components/sections/AboutSection.tsx"
      provides: "About section with dict.about prop replacing all bio grid labels, bio text, stat labels"
      contains: "dict.about"
    - path: "components/sections/TrophiesSection.tsx"
      provides: "Trophies section with dict.trophies prop; index-zipping of trophies array with dict.trophies.items"
      contains: "dict.trophies.items"
    - path: "components/sections/ClubSection.tsx"
      provides: "Club section with dict.club prop replacing club.name, club.description, crestPlaceholder"
      contains: "dict.club"
    - path: "components/sections/TeamSection.tsx"
      provides: "Team section with dict.team prop replacing team.name, team.description, crestPlaceholder"
      contains: "dict.team"
    - path: "components/sections/HighlightsSection.tsx"
      provides: "Highlights section with dict.highlights prop replacing title and intro"
      contains: "dict.highlights"
    - path: "components/sections/GallerySection.tsx"
      provides: "Gallery section with dict.gallery prop replacing title, intro, empty, openPhotoAriaLabel"
      contains: "dict.gallery"
    - path: "components/sections/ContactSection.tsx"
      provides: "Contact section with dict.contact prop; all 15 strings replaced; setNameError uses dict key"
      contains: "dict.contact.errorNameMinLength"
  key_links:
    - from: "components/sections/TrophiesSection.tsx"
      to: "dict.trophies.items[i]"
      via: "index-based zip in trophy map"
      pattern: "trophies\\.items"
    - from: "components/sections/ContactSection.tsx"
      to: "dict.contact.errorNameMinLength"
      via: "setNameError calls in handleNameBlur and handleSubmit"
      pattern: "setNameError.*dict"
---

<objective>
Migrate all 8 section components from hardcoded strings to dictionary prop lookups. Each component receives a new `dict` prop typed as the corresponding Dictionary slice and replaces every hardcoded visible string with the dict key. This plan can run in parallel with 04-02 since both depend only on 04-01.

Purpose: Delivers I18N-03 — zero hardcoded strings in component files. Also resolves all TypeScript errors introduced by the player.ts interface migration in 04-01.

Output: All 8 section components updated with dict props and dict key references instead of hardcoded strings.
</objective>

<execution_context>
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-bilingual-support-polish/04-CONTEXT.md
@.planning/phases/04-bilingual-support-polish/04-RESEARCH.md
@content/player.ts

<interfaces>
<!-- Dictionary type slices from 04-01 output — executor uses these to write Props interfaces -->

Dictionary['hero'] = { position: string; cta: string; ctaAriaLabel: string; }
Dictionary['about'] = { title: string; labelName: string; labelPosition: string; labelWorkingFoot: string; labelDob: string; workingFootRight: string; workingFootLeft: string; workingFootBoth: string; bio: string; statPace: string; statDribbling: string; statShooting: string; statPassing: string; statPhysical: string; statDefending: string; }
Dictionary['trophies'] = { title: string; items: Array<{ name: string; competition: string; }>; }
Dictionary['club'] = { title: string; name: string; description: string; crestPlaceholder: string; }
Dictionary['team'] = { title: string; name: string; description: string; crestPlaceholder: string; }
Dictionary['highlights'] = { title: string; intro: string; }
Dictionary['gallery'] = { title: string; intro: string; empty: string; openPhotoAriaLabel: string; }
Dictionary['contact'] = { title: string; intro: string; successHeading: string; successBody: string; labelName: string; placeholderName: string; errorNameMinLength: string; labelPhone: string; placeholderPhone: string; labelMessage: string; placeholderMessage: string; btnSend: string; btnSending: string; spinnerAriaLabel: string; errorSubmit: string; }

Player interface after 04-01 migration (factual fields only):
```typescript
export interface Player {
  fullName: string;       // KEEP — D-13
  workingFoot: 'Right' | 'Left' | 'Both'; // KEEP (code value)
  dateOfBirth: string;    // KEEP
  nationality: string;    // KEEP
  stats: PlayerStats;     // KEEP
  trophies: Trophy[];     // KEEP (Trophy only has year: number now)
  club: Club;             // KEEP (Club only has logo: string | null now)
  team: Team;             // KEEP (Team only has logo: string | null now)
}
// position: string — REMOVED
// bio: string — REMOVED

export interface Trophy { year: number; } // name and competition REMOVED — they are in dict.trophies.items[i]
export interface Club { logo: string | null; } // name and description REMOVED
export interface Team { logo: string | null; } // name and description REMOVED
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate HeroSection, AboutSection, TrophiesSection</name>
  <files>components/sections/HeroSection.tsx, components/sections/AboutSection.tsx, components/sections/TrophiesSection.tsx</files>
  <read_first>
    - components/sections/HeroSection.tsx (full current state — locate data.position and 'Contact Me' and aria-label template)
    - components/sections/AboutSection.tsx (full current state — locate the stats array with hardcoded labels, bio paragraph, bio grid dt elements, data.workingFoot usage)
    - components/sections/TrophiesSection.tsx (full current state — locate trophy.name and trophy.competition reads in the map)
    - lib/getDictionary.ts (confirm Dictionary type export name — used in Props interfaces below)
  </read_first>
  <action>
HEROECTION.tsx changes:
1. Add import: `import type { Dictionary } from '@/lib/getDictionary'`
2. Add `dict: Dictionary['hero']` to the Props interface (keep `data: Player` — fullName is still from data)
3. Update function signature to destructure `{ data, dict }`
4. Replace `{data.position}` in the heroPosition paragraph with `{dict.position}` — the `data.position` field was removed in 04-01 and would cause a TypeScript error
5. Replace the CTA button text `Contact Me` with `{dict.cta}`
6. Replace the CTA aria-label template `\`Contact ${data.fullName}\`` with `{dict.ctaAriaLabel}` — the ctaAriaLabel key in dictionaries already contains the player name, per the planner decision to hardcode it

ABOUTSECTION.tsx changes:
1. Add import: `import type { Dictionary } from '@/lib/getDictionary'`
2. Add `dict: Dictionary['about']` to Props interface (keep `data: Player`)
3. Update function signature to destructure `{ data, dict }`
4. Replace `{data.bio}` in the bio paragraph with `{dict.bio}` — data.bio was removed in 04-01
5. Replace `'About'` h2 text with `{dict.title}`
6. In the bioGrid, replace these dt text strings with dict lookups:
   - `'Name'` dt text → `{dict.labelName}` (dd still renders `{data.fullName}` — fullName stays in player.ts per D-13)
   - `'Position'` dt text → `{dict.labelPosition}`
   - `'Working Foot'` dt text → `{dict.labelWorkingFoot}`
   - `'Date of Birth'` dt text → `{dict.labelDob}`
7. The Position dd value: replace the `{data.position}` read (removed field) with `{dict.labelPosition}` — the position display string is the dict label itself (e.g., "Central Midfielder" / "Центральний півзахисник")
8. The Working Foot dd value: replace `{data.workingFoot}` with a switch expression: `{data.workingFoot === 'Right' ? dict.workingFootRight : data.workingFoot === 'Left' ? dict.workingFootLeft : dict.workingFootBoth}` — data.workingFoot is a code value ('Right'|'Left'|'Both') that stays in player.ts; the display string comes from dict
9. Replace the hardcoded stats array labels in the stats const array with dict keys:
   - `{ label: 'Pace', value: data.stats.pace }` → `{ label: dict.statPace, value: data.stats.pace }`
   - `{ label: 'Dribbling', ... }` → `{ label: dict.statDribbling, ... }` — repeat for all 6: statShooting, statPassing, statPhysical, statDefending
10. The `aria-label` on each stat row currently reads `\`${label}: ${value} out of 100\`` — keep this pattern; the `label` variable now contains the dict string so the aria-label is automatically translated

TROPHIESSECTION.tsx changes:
1. Add import: `import type { Dictionary } from '@/lib/getDictionary'`
2. Change Props interface: replace `trophies: Trophy[]` with `trophies: Trophy[]; dict: Dictionary['trophies']`
3. Update function signature to destructure `{ trophies, dict }`
4. Replace `'Trophies'` h2 text with `{dict.title}`
5. Inside the trophies map, convert it to use index: change `.map((trophy) =>` to `.map((trophy, i) =>` — the index is needed to look up dict.trophies.items[i]
6. Replace `{trophy.name}` with `{dict.trophies.items[i]?.name ?? ''}` — the name now comes from the dictionary, not the trophy object (which no longer has a name field after 04-01)
7. Replace `{trophy.competition}` with `{dict.trophies.items[i]?.competition ?? ''}` — same pattern for competition
8. Keep `{trophy.year}` — year is a numeric fact that stays in player.ts data
9. Update the key prop: replace `key={\`${trophy.name}-${trophy.year}\`}` with `key={trophy.year}` — name field no longer exists on trophy object; year is sufficient as a key since all three years are unique (2024, 2023(cup), 2023(mvp) — if two share a year, use index: `key={i}`)
  </action>
  <verify>
    <automated>grep -n "data\.position\|data\.bio\|trophy\.name\|trophy\.competition" components/sections/HeroSection.tsx components/sections/AboutSection.tsx components/sections/TrophiesSection.tsx | grep -v "//" | wc -l | xargs -I{} sh -c 'test {} -eq 0 && echo "No removed fields referenced" || echo "ERROR: removed field reads still present"'</automated>
  </verify>
  <done>
    - HeroSection.tsx: imports Dictionary, has dict: Dictionary['hero'] prop, uses dict.position / dict.cta / dict.ctaAriaLabel — no data.position or hardcoded 'Contact Me'
    - AboutSection.tsx: imports Dictionary, has dict: Dictionary['about'] prop, bio paragraph renders dict.bio, dt labels from dict.label*, workingFoot dd uses ternary with dict.workingFoot*, stats labels from dict.stat*
    - TrophiesSection.tsx: imports Dictionary, has dict: Dictionary['trophies'] prop, map uses index i, name from dict.trophies.items[i]?.name, competition from dict.trophies.items[i]?.competition, year from trophy.year
    - No TypeScript errors in any of these three files when running npx tsc --noEmit
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate ClubSection, TeamSection, HighlightsSection, GallerySection, ContactSection</name>
  <files>components/sections/ClubSection.tsx, components/sections/TeamSection.tsx, components/sections/HighlightsSection.tsx, components/sections/GallerySection.tsx, components/sections/ContactSection.tsx</files>
  <read_first>
    - components/sections/ClubSection.tsx (full current state — locate club.name, club.description, 'Club crest' caption)
    - components/sections/TeamSection.tsx (full current state — locate team.name, team.description, 'Team crest' caption)
    - components/sections/HighlightsSection.tsx (full current state — locate 'Highlights' h2 and intro paragraph)
    - components/sections/GallerySection.tsx (full current state — locate 'Gallery' h2, intro paragraph, 'No photos yet.', open aria-label template)
    - components/sections/ContactSection.tsx (full current state — ALL setNameError call sites in handleNameBlur and handleSubmit, all JSX string literals, the error message paragraph with email link)
  </read_first>
  <action>
CLUBSECTION.tsx changes:
1. Add import: `import type { Dictionary } from '@/lib/getDictionary'`
2. The Props interface currently has `club: Club` — Club interface now only has `logo: string | null`. Update Props to add `dict: Dictionary['club']`
3. Update function signature to `{ club, dict }`
4. Replace `'Club'` h2 text with `{dict.title}`
5. In the logo img alt attribute: replace `\`${club.name} crest\`` with `\`${dict.name} crest\`` — dict.name provides the club name string
6. Replace `'Club crest'` span text with `{dict.crestPlaceholder}`
7. Replace `{club.name}` club name display with `{dict.name}`
8. Replace `{club.description}` club description with `{dict.description}`
The club.logo read (`club.logo ?`) continues to work — logo is still in the Club interface

TEAMSECTION.tsx changes — mirror the ClubSection pattern:
1. Add import: `import type { Dictionary } from '@/lib/getDictionary'`
2. Update Props: add `dict: Dictionary['team']` alongside `team: Team`
3. Update function signature to `{ team, dict }`
4. Replace `'Team'` h2 text with `{dict.title}`
5. In img alt: replace `\`${team.name} crest\`` with `\`${dict.name} crest\``
6. Replace `'Team crest'` span text with `{dict.crestPlaceholder}`
7. Replace `{team.name}` with `{dict.name}`
8. Replace `{team.description}` with `{dict.description}`
The team.logo read continues to work

HIGHLIGHTSSECTION.tsx changes:
1. Add import: `import type { Dictionary } from '@/lib/getDictionary'`
2. Update Props: add `dict: Dictionary['highlights']` alongside `videos: VideoEntry[]`
3. Update function signature to `{ videos, dict }`
4. Replace `'Highlights'` h2 text with `{dict.title}`
5. Replace the hardcoded intro paragraph text `'Watch training sessions and match clips from the pitch.'` with `{dict.intro}`

GALLERYSECTION.tsx changes:
1. Add import: `import type { Dictionary } from '@/lib/getDictionary'`
2. Update Props: add `dict: Dictionary['gallery']` alongside `photos: GalleryEntry[]`
3. Update function signature to `{ photos, dict }`
4. Replace `'Gallery'` h2 text with `{dict.title}`
5. Replace the intro paragraph text `'Behind the scenes on the pitch and in training.'` with `{dict.intro}`
6. Replace `'No photos yet.'` in the empty state paragraph with `{dict.empty}`
7. Replace the photo button aria-label template `\`Open ${photo.alt} in fullscreen\`` with `{dict.openPhotoAriaLabel}` — using the generic non-template string per the planner decision (sufficient since the lightbox shows the full alt text)

CONTACTSECTION.tsx changes (most complex — 15 strings + imperative setNameError calls):
1. Add import: `import type { Dictionary } from '@/lib/getDictionary'`
2. ContactSection currently has no Props. Add a Props interface: `interface Props { dict: Dictionary['contact']; }` and update function signature to `{ dict }: Props`
3. Replace `'Contact'` h2 text with `{dict.title}`
4. Replace the intro paragraph text with `{dict.intro}`
5. In the success state: replace `'Message Sent'` with `{dict.successHeading}` and replace `"Your message has been sent. We'll be in touch soon."` with `{dict.successBody}`
6. In the Name field: replace label text `'Name'` with `{dict.labelName}`; replace placeholder `"Your full name"` with `{dict.placeholderName}`
7. In the Phone field: replace label text `'Phone (optional)'` with `{dict.labelPhone}`; replace placeholder `"+380 XX XXX XXXX"` with `{dict.placeholderPhone}`
8. In the Message field: replace label text `'Message'` with `{dict.labelMessage}`; replace placeholder `"Tell us what you have in mind..."` with `{dict.placeholderMessage}`
9. In the submit button: replace `'Send Message'` (idle state) with `{dict.btnSend}` and replace `'Sending...'` button text (loading state) with `{dict.btnSending}`
10. The Loader2 spinner aria-label: replace `aria-label="Sending..."` with `aria-label={dict.spinnerAriaLabel}`
11. In the error state paragraph: replace the entire hardcoded error message JSX — the current JSX is `'Something went wrong. Please try again or email us directly at' + <a href="mailto:dimakyh@ukr.net">dimakyh@ukr.net</a>` — replace with `{dict.errorSubmit}` as plain text (the dict value includes the email address as plain text in the string; the email link HTML is acceptable to render as plain text since dict.errorSubmit is a string — alternatively, keep the anchor tag and use dict.errorSubmit for only the non-link text). Best approach: render dict.errorSubmit directly as the paragraph text content and leave the anchor separate only if needed. Given that dict.errorSubmit already contains the email as text ("...at dimakyh@ukr.net"), render it as `{dict.errorSubmit}` and remove the separate anchor element.
12. CRITICAL — update ALL setNameError call sites (not just JSX):
    - In handleNameBlur function: find `setNameError('Name must be at least 3 characters')` and replace with `setNameError(dict.errorNameMinLength)`
    - In handleSubmit function: find `setNameError('Name must be at least 3 characters')` and replace with `setNameError(dict.errorNameMinLength)`
    There are exactly two setNameError call sites with the hardcoded string — both must be updated (RESEARCH.md Pitfall 6 warning)
  </action>
  <verify>
    <automated>grep -rn "Club crest\|Team crest\|Highlights\|Gallery\|No photos yet\|Contact Me\|Send Message\|Sending\.\.\.\|Message Sent\|Name must be\|Watch training\|Behind the scenes\|Get in touch" components/sections/ | grep -v "//\|\.module\.scss" | wc -l | xargs -I{} sh -c 'test {} -eq 0 && echo "No hardcoded strings remaining" || echo "WARNING: hardcoded strings still present — count: {}"'</automated>
  </verify>
  <done>
    - ClubSection.tsx: dict.title h2, dict.name in img alt and display, dict.description, dict.crestPlaceholder — no club.name or club.description reads
    - TeamSection.tsx: dict.title h2, dict.name in img alt and display, dict.description, dict.crestPlaceholder — no team.name or team.description reads
    - HighlightsSection.tsx: dict.title h2, dict.intro paragraph
    - GallerySection.tsx: dict.title h2, dict.intro, dict.empty empty state, dict.openPhotoAriaLabel on photo button
    - ContactSection.tsx: all 15 dict.contact.* keys in use; BOTH setNameError calls in handleNameBlur AND handleSubmit use dict.errorNameMinLength (not the hardcoded string)
    - npx tsc --noEmit produces zero errors in any section component file
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dict prop → rendered strings | Dictionary values flow from server-loaded JSON into rendered HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | dict values in section components | accept | Dictionaries are static JSON bundled at build time — no runtime user input reaches dict values |
| T-04-05 | Information Disclosure | errorSubmit string contains email dimakyh@ukr.net | accept | Email is already publicly displayed on site in ContactSection; no new exposure |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
After both tasks complete:
1. `grep -rn "data\.position\|data\.bio\|club\.name\|club\.description\|team\.name\|team\.description\|trophy\.name\|trophy\.competition" components/sections/ | grep -v "//"` returns no output (all removed fields are gone)
2. `grep -rn "dict\." components/sections/ | grep -v "//"` returns matches in all 8 section files
3. `npx tsc --noEmit 2>&1 | grep "sections/" | grep "error TS"` returns no output (all TypeScript errors from 04-01 resolved)
4. `grep -n "setNameError" components/sections/ContactSection.tsx` shows both call sites using `dict.errorNameMinLength` (not the hardcoded string)
5. `grep -n "trophies\.items" components/sections/TrophiesSection.tsx` returns a match
</verification>

<success_criteria>
- All 8 section components export a dict prop typed as the corresponding Dictionary slice
- Zero hardcoded English UI strings remain in any section component (honeypot "Website" label excepted — hidden from users)
- TrophiesSection correctly zips player.trophies[i].year with dict.trophies.items[i].name and dict.trophies.items[i].competition
- Both setNameError call sites in ContactSection reference dict.contact.errorNameMinLength
- npx tsc --noEmit exits 0 (zero TypeScript errors across all section files)
</success_criteria>

<output>
Create `.planning/phases/04-bilingual-support-polish/04-03-SUMMARY.md` when done
</output>
