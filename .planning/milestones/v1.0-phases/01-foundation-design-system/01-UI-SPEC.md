---
phase: 1
slug: foundation-design-system
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-19
revised: 2026-05-19
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1: Foundation & Design System.
> This phase delivers a developer-facing design system demo page — not a scout-facing page.
> Generated by gsd-ui-researcher. To be verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — custom SCSS design token system |
| Preset | not applicable |
| Component library | none — bespoke primitives only |
| Icon library | none in Phase 1 — defer to Phase 2 |
| Heading font | Oswald (Latin + Cyrillic subsets) via next/font/google |
| Body font | Roboto (Latin + Cyrillic subsets) via next/font/google |

Source: CONTEXT.md D-08, D-09, D-10, D-11. CLAUDE.md explicitly prohibits Tailwind CSS and any component library.
No shadcn gate: project uses SCSS Modules, not shadcn.

---

## Spacing Scale

Declared values (all multiples of 4):

| Token | CSS Custom Property | px Value | Usage |
|-------|--------------------|---------:|-------|
| 1 | `--space-1` | 4px | Icon gaps, tight inline padding |
| 2 | `--space-2` | 8px | Compact element spacing, badge padding |
| 3 | `--space-3` | 12px | Input vertical padding |
| 4 | `--space-4` | 16px | Default element spacing, card padding |
| 6 | `--space-6` | 24px | Card gap, stacked label spacing |
| 8 | `--space-8` | 32px | Section inner padding (mobile) |
| 12 | `--space-12` | 48px | Section inner padding (desktop) |
| 16 | `--space-16` | 64px | Major section breaks |
| 24 | `--space-24` | 96px | Full-section vertical padding (desktop) — extended scale for cinematic section padding; premium athletic site requires generous vertical rhythm at desktop viewport |
| 32 | `--space-32` | 128px | Hero vertical padding — extended scale for cinematic section padding; premium athletic site requires generous vertical rhythm at desktop viewport |

Exceptions: Nav height is 64px (`--space-16`) — aligns with 8px grid but is a named exception for touch-target compliance. Touch targets on interactive nav items must be minimum 44px tall (WCAG 2.5.8).

Source: RESEARCH.md "Design Token System Structure" section. Values follow 8px base-unit grid.

---

## Typography

Two fonts only: Oswald (headings) + Roboto (body). No third font.

### Type Scale (Full Token Set)

The full mathematical scale (10 tokens) exists in `_tokens.scss` as the complete major-third progression. These tokens are available for implementation flexibility but the design contract restricts *component usage* to 4 semantic tiers — not 10 equal active choices.

| Token | CSS Custom Property | px Value | rem |
|-------|--------------------|---------:|-----|
| xs | `--text-xs` | 12px | 0.75rem |
| sm | `--text-sm` | 14px | 0.875rem |
| base | `--text-base` | 16px | 1rem |
| lg | `--text-lg` | 20px | 1.25rem |
| xl | `--text-xl` | 25px | 1.563rem |
| 2xl | `--text-2xl` | 31px | 1.953rem |
| 3xl | `--text-3xl` | 39px | 2.441rem |
| 4xl | `--text-4xl` | 49px | 3.052rem |
| 5xl | `--text-5xl` | 61px | 3.815rem |
| hero | `--text-hero` | 80px | 5rem |

Scale ratio: major third (1.25) from 16px base. Source: RESEARCH.md type scale.

### Typography Usage Tiers (Design Contract)

The 10 tokens map to 4 usage tiers. Executors choose within the tier range — not from all 10 tokens freely.

| Tier | Token Range | px Range | Usage |
|------|-------------|----------:|-------|
| **Caption/Label** | `--text-xs` / `--text-sm` | 12–14px | Navigation labels, captions, badge text, metadata |
| **Body** | `--text-base` / `--text-lg` | 16–20px | Body paragraphs, descriptions, intro text |
| **Subheading** | `--text-xl` / `--text-2xl` | 25–31px | Section subheadings, card titles, H5–H6 |
| **Display/Heading** | `--text-3xl` through `--text-hero` | 39–80px | H1–H3, section titles, hero player name |

### Font Roles

Two weights only: **Regular (400)** and **Bold (700)**. No intermediate weights.

| Element | Font | Size Token | Weight | Line Height |
|---------|------|------------|--------|-------------|
| Body paragraph | Roboto | `--text-base` (16px) | 400 — Regular | 1.6 |
| Large body / intro | Roboto | `--text-lg` (20px) | 400 — Regular | 1.6 |
| Label / nav link (inactive) | Roboto | `--text-sm` (14px) | 400 — Regular | 1.4 |
| Caption / metadata | Roboto | `--text-xs` (12px) | 400 — Regular | 1.4 |
| Nav link (active state) | Roboto | `--text-sm` (14px) | 700 — Bold | 1.4 |
| H6 | Oswald | `--text-xl` (25px) | 400 — Regular | 1.2 |
| H5 | Oswald | `--text-2xl` (31px) | 400 — Regular | 1.2 |
| H4 | Oswald | `--text-2xl` (31px) | 700 — Bold | 1.2 |
| H3 | Oswald | `--text-3xl` (39px) | 700 — Bold | 1.15 |
| H2 | Oswald | `--text-4xl` (49px) | 700 — Bold | 1.1 |
| H1 | Oswald | `--text-5xl` (61px) | 700 — Bold | 1.05 |
| Hero display | Oswald | `--text-hero` (80px) | 700 — Bold | 1.0 |

Font weights loaded: Oswald 400, 700. Roboto 400, 700 (normal + italic).

Weight rationale: 500 (former Roboto label weight) and 600 (former Oswald H5/H6) are dropped. These intermediate weights add complexity without meaningful visual distinction at typical viewport sizes. Both fonts use only 400 and 700.

Body line-height: 1.6 (optimized for readability at 16px on screen).
Heading line-height: 1.05–1.2 (tightens as size increases — compressed display style for athletic aesthetic).

Responsive scaling rule: headings reduce one step on mobile (375px). H1 uses `--text-4xl` (49px) on mobile; H2 uses `--text-3xl` (39px) on mobile. Hero display uses `--text-5xl` (61px) on mobile.

---

## Color

### Dark Theme (Default — first visit and no saved preference)

Source: CONTEXT.md D-07. RESEARCH.md Pattern 3.

| Role | CSS Custom Property | Hex | Usage |
|------|--------------------|----|-------|
| Background (60%) | `--color-bg` | `#0D1B2A` | Page background, root surface |
| Elevated surface (30%) | `--color-bg-elevated` | `#162336` | Nav background, cards, demo swatches container |
| Body text | `--color-text` | `#F0F4F8` | All body copy, labels |
| Muted text | `--color-text-muted` | `#8A9BB0` | Captions, placeholder, secondary labels |
| Accent (10%) | `--color-accent` | `#E5002B` | CTAs, active nav indicator, focus rings, theme toggle active state, section title underline on demo page |
| Accent hover | `--color-accent-hover` | `#FF1A42` | Hover state on accent elements |
| Border | `--color-border` | `rgba(240, 244, 248, 0.10)` | Dividers, card outlines, nav separator |
| Overlay | `--color-overlay` | `rgba(13, 27, 42, 0.80)` | Modal backdrop, lightbox overlay (Phase 3) |

### Light Theme

Source: CONTEXT.md D-03, D-04, D-06. RESEARCH.md Pattern 3.

| Role | CSS Custom Property | Hex | Usage |
|------|--------------------|----|-------|
| Background (60%) | `--color-bg` | `#FFFFFF` | Page background, root surface |
| Elevated surface (30%) | `--color-bg-elevated` | `#F5F7FA` | Nav background, cards, demo swatches container |
| Body text | `--color-text` | `#111111` | All body copy, labels |
| Muted text | `--color-text-muted` | `#555F6D` | Captions, placeholder, secondary labels |
| Accent (10%) | `--color-accent` | `#E5002B` | Same as dark theme — accent is identical across themes |
| Accent hover | `--color-accent-hover` | `#C40023` | Hover darkens in light theme (vs. lightens in dark theme) |
| Border | `--color-border` | `rgba(17, 17, 17, 0.12)` | Dividers, card outlines |
| Overlay | `--color-overlay` | `rgba(255, 255, 255, 0.85)` | Modal backdrop (light) |

### Accent Reserved For

The accent color `#E5002B` is reserved exclusively for:

1. Primary CTA buttons (background fill)
2. Active nav link indicator (left border or underline)
3. Focus rings on interactive elements (2px solid `--color-accent`)
4. Theme toggle active state indicator
5. Section title underlines on the design system demo page
6. Scroll progress indicator (Phase 2+)

Accent is NOT used for: body text, background fills other than CTAs, decorative elements, borders except focus rings.

### Destructive Color

No destructive actions exist in Phase 1. The destructive color token is pre-defined for Phase 3 (form error states):

| Role | CSS Custom Property | Hex | Usage |
|------|--------------------|----|-------|
| Destructive | `--color-destructive` | `#CC0020` | Form validation errors only (Phase 3) |

This token is declared in `_tokens.scss` in Phase 1 so Phase 3 never needs to modify the token file.

---

## Implementation Contract: CSS Custom Properties

The complete token system lives in `styles/_tokens.scss`. This file is auto-imported into every SCSS module via `sassOptions.additionalData` in `next.config.ts`. Components reference tokens via `var(--token-name)` — never hardcode hex values in component files.

### SCSS Token Structure

```scss
// Dark theme is the :root default (D-07)
:root,
[data-theme='dark'] {
  /* Color tokens */
  --color-bg:           #0D1B2A;
  --color-bg-elevated:  #162336;
  --color-text:         #F0F4F8;
  --color-text-muted:   #8A9BB0;
  --color-accent:       #E5002B;
  --color-accent-hover: #FF1A42;
  --color-border:       rgba(240, 244, 248, 0.10);
  --color-overlay:      rgba(13, 27, 42, 0.80);
  --color-destructive:  #CC0020;

  /* Font family tokens */
  --font-heading: var(--font-oswald), 'Arial Narrow', sans-serif;
  --font-body:    var(--font-roboto), 'Arial', sans-serif;

  /* Type scale tokens (full mathematical scale — component usage governed by 4-tier contract above) */
  --text-xs:   0.75rem;
  --text-sm:   0.875rem;
  --text-base: 1rem;
  --text-lg:   1.25rem;
  --text-xl:   1.563rem;
  --text-2xl:  1.953rem;
  --text-3xl:  2.441rem;
  --text-4xl:  3.052rem;
  --text-5xl:  3.815rem;
  --text-hero: 5rem;

  /* Spacing tokens */
  --space-1:  0.25rem;
  --space-2:  0.5rem;
  --space-3:  0.75rem;
  --space-4:  1rem;
  --space-6:  1.5rem;
  --space-8:  2rem;
  --space-12: 3rem;
  --space-16: 4rem;
  --space-24: 6rem;
  --space-32: 8rem;
}

[data-theme='light'] {
  --color-bg:           #FFFFFF;
  --color-bg-elevated:  #F5F7FA;
  --color-text:         #111111;
  --color-text-muted:   #555F6D;
  --color-accent:       #E5002B;
  --color-accent-hover: #C40023;
  --color-border:       rgba(17, 17, 17, 0.12);
  --color-overlay:      rgba(255, 255, 255, 0.85);
  --color-destructive:  #CC0020;
  /* Font and spacing tokens are theme-independent — inherited from :root */
}
```

---

## Interaction Contracts

### Theme Toggle (ThemeToggle component)

| State | Visual |
|-------|--------|
| Dark active | Sun icon, `--color-accent` fill on icon or pill |
| Light active | Moon icon, `--color-accent` fill on icon or pill |
| Hover | Scale 1.05, transition 150ms ease |
| Focus | 2px solid `--color-accent` focus ring, 2px offset |
| Press (active) | Scale 0.95, transition 100ms ease |

Animation owner: Framer Motion (mount + hover + press — per CLAUDE.md animation rule).
Icon: Sun / Moon SVG inline (no icon library in Phase 1 — hand-code the two SVGs or use Unicode placeholder).

### Stub Nav

| Element | State | Visual |
|---------|-------|--------|
| Nav bar | Default | `--color-bg-elevated` background, `--color-border` bottom border, 64px height |
| Nav link | Default | `--color-text-muted`, `--text-sm`, Roboto 400 |
| Nav link | Hover | `--color-text`, transition 150ms ease |
| Nav link | Active (current section) | `--color-accent`, Roboto 700, left border 2px solid `--color-accent` OR underline 2px `--color-accent` |
| Nav bar | Scrolled | Backdrop blur 12px, background opacity increases to 95% |
| Mobile | < 768px | Collapse to hamburger menu (hamburger icon = 3 lines, 24px, `--color-text`) |

Nav hover animation owner: Framer Motion.
Nav scroll behavior: CSS transition on background/backdrop-filter only (no GSAP).

### Demo Page Scroll Fade-In (GSAP Verification Section)

| Trigger | Behavior |
|---------|----------|
| Element enters viewport (top 80%) | Fade in from `opacity: 0, y: 40px` to `opacity: 1, y: 0` |
| Duration | 0.8s per element |
| Stagger | 0.1s between elements |
| Easing | GSAP default (`power1.out`) |
| ScrollTrigger `scrub` | false — fires once, not tied to scroll position |

Animation owner: GSAP (scroll-triggered — per CLAUDE.md animation rule).
This section verifies Lenis + GSAP ScrollTrigger proxy is wired correctly.

### Focus Rings (All Interactive Elements)

2px solid `--color-accent` with 2px offset. Applied via `:focus-visible` only (not `:focus`) to avoid rings on mouse click. All interactive elements must be keyboard-navigable in Phase 1.

---

## Demo Page Layout Contract

The Phase 1 demo page (`app/[lang]/page.tsx`) is a vertical stack of specimen sections. It is a developer reference — visual design is utilitarian but uses the real token system.

Primary focal point: the "Design System" H1 at the top of the typography section — the largest text element, first visible after nav.

### Sections (top to bottom)

1. **Nav** — stub nav with real section anchors, theme toggle, responsive collapse
2. **Typography Specimens** — H1 through H6 in Oswald, body paragraph in Roboto, small/label text. Each labeled with token name and px value. Background: `--color-bg`. Title: "Typography Scale" in H2. The "Design System" H1 renders at `--text-5xl` (61px) Oswald 700 and is the primary focal point of the demo page.
3. **Color Palette Swatches** — Two columns (Dark theme / Light theme). Each swatch: 80px circle or 120×64px rectangle, token name below, hex value below that. Background: `--color-bg-elevated`.
4. **Scroll Test Section** — Tall section (min-height: 80vh) with 5–6 fade-in items (text boxes). Heading: "Scroll Animation Test". Each item fades in as it enters viewport (GSAP verification). Background: `--color-bg`.
5. **Footer** — Minimal: "Design System Demo — Phase 1" label in `--text-xs`, `--color-text-muted`. Not a real footer.

### Layout Constraints

- Max content width: 1280px, centered with `margin: 0 auto`
- Section horizontal padding: `--space-8` (32px) on desktop, `--space-4` (16px) on mobile (< 768px)
- Section vertical padding: `--space-24` (96px) top/bottom on desktop, `--space-16` (64px) on mobile
- Grid for swatches: CSS Grid, auto-fill columns min 160px

---

## Responsive Breakpoints

| Token | px | SCSS mixin |
|-------|----|-----------|
| sm | 640px | `@include respond-to('sm')` |
| md | 768px | `@include respond-to('md')` |
| lg | 1024px | `@include respond-to('lg')` |
| xl | 1280px | `@include respond-to('xl')` |
| 2xl | 1440px | `@include respond-to('2xl')` |

Mobile-first approach: base styles are for 375px, breakpoints add overrides upward.

---

## Copywriting Contract

Phase 1 is a developer demo page. Copy is utilitarian and descriptive — not marketing copy.

| Element | Copy |
|---------|------|
| Demo page `<title>` | "Design System — Future Legend Dev" |
| Demo page `<h1>` | "Design System" |
| Typography section heading | "Typography Scale" |
| Typography section subheading | "Oswald (headings) · Roboto (body)" |
| Color section heading | "Color Tokens" |
| Color section subheading | "Dark theme (default) and light theme values" |
| Scroll test heading | "Scroll Animation Test" |
| Scroll test body | "These elements fade in as they enter the viewport. Confirms Lenis smooth scroll and GSAP ScrollTrigger are correctly wired." |
| Theme toggle label (aria) | "Toggle theme" |
| Theme toggle tooltip (dark mode) | "Switch to light" |
| Theme toggle tooltip (light mode) | "Switch to dark" |
| Nav brand/logo text | "Future Legend" |
| Footer label | "Design System Demo · Phase 1" |
| Empty state heading | N/A — no empty states in Phase 1 demo page |
| Error state | N/A — no user input in Phase 1 |
| Destructive confirmation | N/A — no destructive actions in Phase 1 |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — not initialized | not applicable |
| Third-party registries | none declared | not applicable |

No shadcn. No third-party component registries. All components are hand-built. Registry safety gate: not required for this phase.

---

## Animation Ownership Reference

Per CLAUDE.md animation rule: Framer Motion owns mount/unmount and hover. GSAP owns scroll-triggered sequences. Never animate the same element with both.

| Element | Owner | Trigger |
|---------|-------|---------|
| Theme toggle (hover, press) | Framer Motion | User interaction |
| Nav link (hover) | Framer Motion | User interaction |
| Nav bar (mount) | Framer Motion | Component mount |
| Scroll fade-in items | GSAP + ScrollTrigger | Scroll position |
| Page-level transitions (Phase 2+) | Framer Motion | Route change |

---

## FOUC Prevention Contract

First visit with no saved preference → page opens in dark theme immediately, before any JS executes.
Implementation: `ThemeProvider` with `defaultTheme="dark"` and `enableSystem={false}`. CSS `:root` declares dark values so the page renders dark even before the anti-FOUC script runs.

```
ThemeProvider config:
  attribute="data-theme"
  defaultTheme="dark"
  enableSystem={false}
  storageKey="fl-theme"
```

`suppressHydrationWarning` on `<html>` (not `<body>`). Source: RESEARCH.md Pattern 3.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Source Traceability

| Decision | Source |
|----------|--------|
| Dark theme default | CONTEXT.md D-07 |
| Navy `#0D1B2A` background | CONTEXT.md D-04, RESEARCH.md Pattern 3 |
| Crimson `#E5002B` accent | CONTEXT.md D-05, RESEARCH.md Pattern 3 |
| Body text `#111111` (light) | CONTEXT.md D-06 |
| Oswald heading font | CONTEXT.md D-08 |
| Roboto body font | CONTEXT.md D-09 |
| Two-font system only | CONTEXT.md D-10 |
| Next.js font optimization | CONTEXT.md D-11 |
| Demo page content (demo not scout-facing) | CONTEXT.md D-12, D-13 |
| Nav anchors: Home, About, Highlights, Gallery, Trophies, Club, Team, Contact | CONTEXT.md D-14 |
| Type scale (major third, 1.25 ratio) | RESEARCH.md Design Token System |
| Spacing scale (8px base) | RESEARCH.md Design Token System |
| Elevated surface tones | RESEARCH.md Pattern 3 (researcher discretion) |
| Breakpoint values | RESEARCH.md Pattern 7 |
| Accent hover lightens in dark / darkens in light | Researcher discretion (contrast principle) |
| Line heights (body 1.6, heading 1.05–1.2) | Researcher default — premium athletic design standard |
| Focus ring spec (2px solid accent, :focus-visible) | WCAG 2.1 AA + researcher default |
| Touch target minimum 44px | WCAG 2.5.8 — researcher default |
| 4-tier typography usage contract (not 10 equal choices) | Checker revision 2026-05-19 — D4 BLOCK fix |
| 2 weights only: 400 + 700 (dropped 500 and 600) | Checker revision 2026-05-19 — D4 BLOCK fix |
| Extended spacing justification (96px, 128px) | Checker revision 2026-05-19 — D5 FLAG fix |
| Primary focal point declaration (demo page H1) | Checker revision 2026-05-19 — D2 FLAG fix |
