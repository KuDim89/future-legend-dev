---
phase: 03-media-contact
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/sections/GallerySection.tsx
  - components/sections/GallerySection.module.scss
autonomous: true
requirements: [MEDIA-02, MEDIA-03]

must_haves:
  truths:
    - "Photos render in a masonry grid: 3 columns desktop, 2 at ≤768px, 1 at ≤480px"
    - "Clicking any photo opens a fullscreen lightbox with keyboard navigation and zoom"
    - "Closing the lightbox returns the visitor to the grid"
    - "All gallery images use loading=lazy"
  artifacts:
    - path: "components/sections/GallerySection.tsx"
      provides: "Masonry photo grid + yarl lightbox with Fullscreen + Zoom plugins"
      contains: "Lightbox"
    - path: "components/sections/GallerySection.module.scss"
      provides: "Global masonry-grid wrappers + photo styles"
      contains: ":global(.masonry-grid)"
  key_links:
    - from: "components/sections/GallerySection.tsx"
      to: "yet-another-react-lightbox/styles.css"
      via: "JS CSS import"
      pattern: "yet-another-react-lightbox/styles.css"
    - from: "components/sections/GallerySection.tsx"
      to: "lightboxIndex state"
      via: "openLightbox(index) → Lightbox open/index props"
      pattern: "setLightboxIndex|lightboxIndex"
---

<objective>
Build the gallery vertical slice: a responsive masonry grid of photos that opens a fullscreen lightbox (with zoom and keyboard navigation) when any photo is clicked. After this plan, a visitor can browse the photo gallery and open any photo fullscreen without a page-load penalty (MEDIA-02 + MEDIA-03).

Purpose: Delivers the lightbox media-browsing capability. The two correctness details are (1) the yarl CSS must be imported as a JS import (not in the SCSS module) or the lightbox renders unstyled, and (2) the `react-masonry-css` class names must be `:global()` in the SCSS module or columns collapse.
Output: `components/sections/GallerySection.tsx` (+ `.module.scss`). page.tsx wiring is deferred to plan 03-04.
</objective>

<execution_context>
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmytro.kukharuk/projects/stydy/future-legend-dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-media-contact/03-CONTEXT.md
@.planning/phases/03-media-contact/03-RESEARCH.md
@.planning/phases/03-media-contact/03-PATTERNS.md
@.planning/phases/03-media-contact/03-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. GalleryEntry comes from plan 03-01. -->

From content/gallery.ts (created in 03-01):
```typescript
export interface GalleryEntry {
  src: string;   // full path INCLUDING basePath: /future-legend-dev/images/gallery/photo-0N.webp
  alt: string;
  category: 'match' | 'training' | 'official';
}
```

From yet-another-react-lightbox v3.32.0 (installed in 03-01):
```typescript
import Lightbox from 'yet-another-react-lightbox';
import Fullscreen from 'yet-another-react-lightbox/plugins/fullscreen';
import Zoom from 'yet-another-react-lightbox/plugins/zoom';
import 'yet-another-react-lightbox/styles.css';   // MUST be a JS import — NOT @import in .module.scss
// slides: { src, alt }[]; open: boolean; index: number; close: () => void; plugins: [Fullscreen, Zoom]
```

From react-masonry-css v1.0.16 (installed in 03-01):
```tsx
import Masonry from 'react-masonry-css';
// <Masonry breakpointCols={{ default: 3, 768: 2, 480: 1 }} className="masonry-grid" columnClassName="masonry-grid_column">{children}</Masonry>
// Applies the literal class names "masonry-grid" / "masonry-grid_column" directly to DOM — they must be :global() in SCSS.
```

From lib/animations/useScrollReveal.ts (existing): see plan 03-02 interfaces block — same hook, `.reveal-item` targets within `{ scope: containerRef }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build GallerySection with masonry grid and yarl lightbox</name>
  <files>components/sections/GallerySection.tsx</files>
  <read_first>
    - components/sections/TrophiesSection.tsx (section shell + useScrollReveal pattern to mirror)
    - .planning/phases/03-media-contact/03-RESEARCH.md (Pattern 2 full GallerySection code lines 417-495; Pitfall 3 yarl CSS import; Pitfall 4 masonry :global)
    - .planning/phases/03-media-contact/03-PATTERNS.md (GallerySection.tsx imports lines 402-414, JSX structure lines 418-464)
    - .planning/phases/03-media-contact/03-UI-SPEC.md (GallerySection structure block, photo card states table, lightbox config table: plugins [Fullscreen, Zoom], no Download; copywriting: h2 "Gallery", intro "Behind the scenes on the pitch and in training.", photo wrapper aria-label "Open {alt} in fullscreen", empty state "No photos yet.")
  </read_first>
  <action>
    Create `components/sections/GallerySection.tsx` as `'use client'`. Imports in order: `useState, useRef` from react; `Masonry` from `react-masonry-css`; `Lightbox` from `yet-another-react-lightbox`; `Fullscreen` from `yet-another-react-lightbox/plugins/fullscreen`; `Zoom` from `yet-another-react-lightbox/plugins/zoom`; `import 'yet-another-react-lightbox/styles.css'` (CRITICAL — JS import, not SCSS); `motion` from `framer-motion`; `useScrollReveal` from `@/lib/animations/useScrollReveal`; `import type { GalleryEntry } from '@/content/gallery'`; `styles` from `./GallerySection.module.scss`.

    Module-scope const: `const breakpointCols = { default: 3, 768: 2, 480: 1 }` (D-15).
    Signature: `interface Props { photos: GalleryEntry[] }` then `export function GallerySection({ photos }: Props)`. Inside: `const containerRef = useRef<HTMLElement>(null); const [lightboxIndex, setLightboxIndex] = useState(-1); useScrollReveal(containerRef);` and `const slides = photos.map((p) => ({ src: p.src, alt: p.alt }));`.

    Render `<section id="gallery" ref={containerRef} className={styles.section}>` containing: `<h2 className={\`${styles.sectionTitle} reveal-item\`}>Gallery</h2>`; `<p className={\`${styles.intro} reveal-item\`}>Behind the scenes on the pitch and in training.</p>`; then a `<Masonry breakpointCols={breakpointCols} className="masonry-grid" columnClassName="masonry-grid_column">` mapping each photo (with index) to a `motion.button key={photo.src} className={\`${styles.photoWrapper} reveal-item\`} onClick={() => setLightboxIndex(index)} aria-label={\`Open ${photo.alt} in fullscreen\`} whileHover={{ scale: 1.02 }} transition={{ duration: 0.2, ease: 'easeOut' }}` containing `<img src={photo.src} alt={photo.alt} loading="lazy" className={styles.photo} />` and a `motion.div className={styles.photoOverlay} aria-hidden="true" initial={{ opacity: 0 }} whileHover={{ opacity: 0.5 }} transition={{ duration: 0.2 }}`. After the Masonry, render `<Lightbox open={lightboxIndex >= 0} close={() => setLightboxIndex(-1)} slides={slides} index={lightboxIndex} plugins={[Fullscreen, Zoom]} />`.

    Use `<button>` (motion.button) wrappers for keyboard accessibility (Enter/Space opens lightbox) per the accessibility contract — NOT `<div onClick>`. The `index` prop and `open={lightboxIndex >= 0}` give the controlled open/close behavior (index -1 = closed). The `reveal-item` class is on each photo wrapper button + h2 + intro; the Lightbox is NOT a reveal-item.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/GallerySection.tsx` first line is `'use client';`
    - Contains `import 'yet-another-react-lightbox/styles.css'`
    - Contains `import Fullscreen from 'yet-another-react-lightbox/plugins/fullscreen'` and `import Zoom from 'yet-another-react-lightbox/plugins/zoom'`
    - Contains `breakpointCols` equal to `{ default: 3, 768: 2, 480: 1 }`
    - Renders `<Masonry` with `className="masonry-grid"` and `columnClassName="masonry-grid_column"`
    - Photo wrappers are `motion.button` elements with `onClick={() => setLightboxIndex(index)}` and an `aria-label` containing `Open` and `fullscreen`
    - Each gallery `<img>` has `loading="lazy"`
    - Renders `<Lightbox` with `open={lightboxIndex >= 0}`, `index={lightboxIndex}`, and `plugins={[Fullscreen, Zoom]}`
    - Contains `import type { GalleryEntry } from '@/content/gallery'`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GallerySection renders a masonry photo grid that opens a Fullscreen+Zoom lightbox on click; compiles clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build GallerySection.module.scss with global masonry wrappers</name>
  <files>components/sections/GallerySection.module.scss</files>
  <read_first>
    - components/sections/TrophiesSection.module.scss (.section + .sectionTitle pattern to copy verbatim)
    - .planning/phases/03-media-contact/03-PATTERNS.md (GallerySection.module.scss full pattern lines 476-527)
    - .planning/phases/03-media-contact/03-UI-SPEC.md (masonry CSS global scope block; photo card states; spacing — column gap var(--space-6))
    - styles/_tokens.scss (confirm tokens: --space-6, --color-overlay, --color-accent, --color-text-muted, --text-base)
  </read_first>
  <action>
    Create `components/sections/GallerySection.module.scss` with the standard header comment (`// GallerySection.module.scss` / `// Mixins auto-injected via sassOptions.additionalData — no @use needed` / `// Tokens: use var(--token) — NOT $scss-variable`).

    Copy `.section` and `.sectionTitle` (with `::after` accent underbar) verbatim from TrophiesSection.module.scss.

    CRITICAL masonry global wrappers (Pitfall 4 — react-masonry-css injects these literal class names onto DOM; CSS Modules would hash a scoped class so they MUST be `:global()`):
    - `:global(.masonry-grid)` → `display: flex; margin-left: calc(-1 * var(--space-6)); width: auto;`
    - `:global(.masonry-grid_column)` → `padding-left: var(--space-6); background-clip: padding-box;`

    Add a comment above the global block explaining why `:global()` is required.

    Other classes: `.intro` (font-size var(--text-base), color var(--color-text-muted), line-height 1.5, margin-bottom var(--space-12)); `.photoWrapper` (position relative, display block, padding 0, no border, no background, cursor pointer, `&:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px }`); `.photo` (width 100%, display block, border-radius 4px, margin-bottom var(--space-6) for column item gap); `.photoOverlay` (position absolute, inset 0, background var(--color-overlay), border-radius 4px, pointer-events none).
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/GallerySection.module.scss` contains `:global(.masonry-grid)` with `display: flex`
    - Contains `:global(.masonry-grid_column)` with `padding-left: var(--space-6)`
    - Contains a `.section` and `.sectionTitle` block copied from the TrophiesSection pattern (sectionTitle has an `::after` with `var(--color-accent)`)
    - `.photoWrapper` contains a `:focus-visible` outline rule
    - `.photo` contains `loading`-independent styling: `width: 100%`, `display: block`, `margin-bottom: var(--space-6)`
    - All color/spacing values use `var(--token)` — no bare hex or px-only color values
    - `npx next build` exits 0 (validates SCSS compiles AND GallerySection renders during static export prerender — also surfaces any react-masonry-css React 19 incompatibility, research assumption A1)
  </acceptance_criteria>
  <done>SCSS compiles with global masonry wrappers; `npx next build` exits 0 confirming GallerySection prerenders under static export.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gallery image src → GitHub Pages basePath | Image paths must resolve under /future-legend-dev or render broken |
| Third-party lightbox (yarl) → DOM | yarl owns its own focus trap / fullscreen API surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Performance | GallerySection initial render | mitigate | All gallery `<img>` use `loading="lazy"`; lightbox renders only when `open={lightboxIndex >= 0}` (no slide DOM until a photo is clicked). Satisfies MEDIA-03. |
| T-03-06 | Tampering (XSS) | photo alt / src rendering | accept | `src` and `alt` come from build-time content/gallery.ts (developer-authored, trusted), rendered as React attribute/text nodes — no `dangerouslySetInnerHTML`, no untrusted input. |
| T-03-07 | Availability | react-masonry-css React 19 compatibility | mitigate | Research assumption A1 (unverified, last published 2022). `npx next build` in Task 2 verify is the gate — if the component errors at build/prerender, executor falls back to CSS `columns` layout (documented fallback in 03-RESEARCH.md Alternatives Considered) and records the deviation in the SUMMARY. |
</threat_model>

<verification>
- `npx next build` exits 0 (SCSS compiles + GallerySection prerenders).
- Manual/visual (deferred to end-of-phase human check in 03-04): photos lay out in 3/2/1 masonry columns by viewport; clicking a photo opens a styled fullscreen lightbox with working arrow-key navigation, zoom, and Escape-to-close.
</verification>

<success_criteria>
- GallerySection renders a responsive masonry grid (3/2/1 columns).
- Clicking a photo opens the yarl lightbox with Fullscreen + Zoom; close returns to grid.
- yarl CSS imported via JS import; masonry classes are `:global()`.
- All gallery images use `loading="lazy"`.
- `npx next build` exits 0.
</success_criteria>

<output>
Create `.planning/phases/03-media-contact/03-03-SUMMARY.md` when done. If the CSS-columns fallback was used (react-masonry-css incompatibility), document it.
</output>
